"""Nodify — logic and evidence distilled into a durable node tree.

The context window holds only the frontier; the tree holds the truth.
Authority order: schemas (frozen, loaded at runtime) > tests > docs.
"""

__version__ = "0.1.0"
