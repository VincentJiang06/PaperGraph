# 00-PREMISE · 前提审计

> **本文件是什么**：把这个项目自己的承重赌注，按这个项目要卖给用户的那把尺子，逐条评一遍。
> 每条给出：陈述 / 支持证据（含强度）/ 反对证据 / 裁决（押什么、押多大）/ 会推翻它的观测 / 何时检验。
>
> **为什么存在**：上一轮把前代的一条判词（"四次严格消融判死了结构框架"）当成最高禁令继承，
> **从未核过它背后的证据**。本轮的一手考古发现该证据被严重高估。一个以"不让任何断言绕过证据审计"
> 为价值主张的系统，建在未审计的前提上是自我否证的。本文件的存在是为了让这件事不再发生第二次。
>
> **证据约定**：每条承重断言带 `[E: <file>#<anchor>]`，指向 `research/v2/` 语料（2026-08-17 快照）
> 或一手 URL。凡语料标 `unverified` 的数字，本文件保留该标记，不洗成裸数字。
>
> **词汇约定（R2 新增，因 C-10）**：本文件**不定义**任何枚举、字段名或状态符号。
> 凡涉及 status / kind / flag / 字段名，一律写 `01-CONTRACTS.md` 的符号与 §指针，**不复述定义**（01 §9.0、V9.1）。
> 三处已知的同形异义，本文件按下列写法消歧，**三条都可 grep 检验**：
>
> | 词 | 两个义 | 本文件的写法 |
> |---|---|---|
> | `ST-*` | 只有一义 | 出现的每个 `ST-*` 必须落在 01 §1.4 六值内（01 §7.4 **V7.10** 是它的机器判据，本文件是第一个被检对象） |
> | `unverified` | ①「未经核实」的一般义（语料标注，或本文件对某条**前提**的定性） ②claim 状态 | ②一律写 `ST-U` 或 `ST-N`（分界见 01 §1.4.1）；裸词 `unverified` 只保留①义 |
> | `F-<数字>` | ①前代考古发现编号（`gt-pg-*`） ②01 §7.2 的 flag | ①义必须紧跟 `[E: gt-*]` 指针；**无指针的裸 `F-xx` 即为歧义** |
>
> **本文件不做的事**：它不设计系统。kind、schema、门、编排形状属于下游文档。
> 本文件只回答"我们凭什么相信这些前提"。
>
> **下游引用规则**：不得引用本文件的裁决作为证据，只能引用它指向的一手来源。本文件是审计记录，不是证据源。

---

## 赌注一览

| id | 陈述 | 押注强度 | 当前证据地位 | 首次检验点 |
|---|---|---|---|---|
| **B1** | 超并行提升研究质量 | **不押**（通用命题）／小仓位押"扇出用于多验与上下文卫生" | 无任何等算力对照实验；反向证据更锐 | M1 深度曲线 / M2 等预算 A/B |
| **B2** | 结构与门提升质量 | 中仓位，**有界**：结构投资须自证增益 | 继承的禁令地基被削弱；结构也未被证伪 | M2 checkable-answer eval |
| **B3** | 可信度是产品 | **大仓位** | 缺口实测、竞品全线空白、后果可按年计数 | M1 每次 run 记账 / M3 用户侧 |
| **B4** | 三条 kind（01 §2）是充分分类 | 押"kind 对"，**不押"状态空间够"** | 至少 8 类断言三条 kind 表达不了 | M1 schema 定稿 / M2 状态直方图 |
| **B5** | 确定性门能承载可信度 | 押 4 类攻击被结构性消灭，**明写 8 类不防** | 全部是论证，零实测 | M0（RT-5 架构可行性）/ M1 红队 |
| **B6** | 前代契约值得继承 | 押**文件契约与写法**，**不押任何门的判定实现** | 门 4 条核心机制文档为真、代码为假 | 每门合入（准入条件） |
| **B7** | 中文学术场景可交付 | **押小**：主动声明能力受限，不建全库检索 | 中文核验基础设施是公开空白 | M3 首个真实中文任务 |
| **B8** | 我们自己的门不会空心 | **当前不押**，须先补三条代码**且**接一个外部锚点才允许升级 | 前代与本仓库两条独立线都失败过；补法本身自指（C-11；**S0 实测把它加强为「远端也拦不住」——`receive.deny*` 不覆盖 `refs/tags/*`，且「从 tag 检出干净副本执行」被红样本 R3 判死**，外部锚点仍不可用：本仓库 remote 数 0）；文档层与状态函数层的门已落地，**证据层与运行层零门** | 每门合入 / 每轮攻击结算 |
| **B9** | 成本前提可承受 | 中，但基准 24 小时前刚移动 | DeepSeek 2026-08-16 全线改价 | M1 起每次 run 记账 |

---

## B1 · 超并行提升研究质量

### 陈述

"扇出（并行 subagent / 并行工具调用）在**等预算**条件下提升研究质量。" —— 这是系统名字里 hyper-parallel 所隐含的命题。

### 支持证据（逐条标强度）

| 证据 | 口径三元组 | 强度判定 |
|---|---|---|
| Anthropic 90.2% | 相对提升幅度 / Anthropic **内部** research eval（未公开、样本量未披露），lead=Opus 4 + sub=Sonnet 4 / 对比单 agent Opus 4 | **弱**。内部不可复现；算力未控；2025 模型代际，到 2026-08 已迭代多代 |
| Tongyi Heavy HLE 32.9 → 38.3 | 同一 30.5B-A3B 模型，只改推理时编排 / Heavy = n 个并行 IterResearch agent + 综合，**n 未披露** | **中，但方向不对题**。证明 test-time scaling 有效，**不是**等算力下并行优于串行；且 n 未披露 → 增益无法归因到扇出宽度 |
| W&D BrowseComp 62.2% | GPT-5-Medium + width vs 厂商自报 GPT-5-High 54.9% | **不构成受控证据**。跨推理档位比较；未说明是否控总预算；"reducing turns" 是效率主张不是质量主张 |
| Self-Manager | DeepResearch Bench，"outperforms single-agent loop baselines"（摘要未给数字） | **弱**，且论文自己的机制解释是**上下文隔离**，不是"多脑更聪明"；是否控算力未说明 |
| Blackboard 13%–57% | 端到端成功率相对提升 / KramaBench + 改造 DSBench + DA-Code（在数据湖里找数据） | **不可迁移**。任务是数据发现，不是证据评估 |

[E: ext-dr-architectures.md#核验表1-6,23] [E: ext-orchestration.md#核验表1-5,44] [E: ext-orchestration.md#U7]

**决定性的空白**：整个谱系里**没有任何一篇公开工作给出等算力对照实验**。所有正向数据点都是 test-time scaling —— 更多算力，恰好摆成了并行形状。Anthropic 自己的方差分析说 token 用量在 **BrowseComp** 上单独解释 80% 的方差（三因子合计 95%），这几乎直接把"多智能体赢了 90.2%"重述为"多花了 token"。注意这个 80% 是 BrowseComp 上的方差分解，**不是**那个拿到 90.2% 的内部 eval 上的，也不是一条因果律。[E: ext-dr-architectures.md#结论摘要2] [E: ext-orchestration.md#核验表2]

### 反对证据（更锐，且正好打在产品定义上）

1. **深度 → 引用事实准确率是负相关，而表层指标纹丝不动。** 同一模型，工具调用从 2 次扩到 150 次（七档：2/10/30/50/70/100/150）：GPT-5.4 的 Fact-Check 通过率 **78.6% → 16.7%**，Claude Opus 4.6 **80.0% → 57.9%**；同期 Link Works >92%、Relevant Content >80% 基本不动。摘要写的 "approximately 42%" 经核算是**两模型绝对百分点降幅的均值 −42.0pp**（相对降幅均值约 53%），且两模型降幅相差近 3 倍，头条数把差异抹平了。
   **限定必须一起带**：单篇预印本（PwC 产业实验室，2026-05-07 v1），仅两个模型做深度消融，**未报相关系数、回归或 p 值**，判定器是 rubric-based LLM-as-a-judge（有人工校准）。→ **方向可信、量级待我们自证。**
   [E: ext-orchestration.md#核验表6-10] [E: ext-citation-faithfulness.md#核验表8] [E: https://arxiv.org/abs/2605.06635]

2. **一个实验室在自家三代产品上把工具调用上限单调下调，分数单调上升。** MiroThinker v1.0 → v1.5 → v1.7 的每任务上限 **600 → 400 → 300**，而 BrowseComp/HLE-Text 同期上升。这不是受控消融，但它是一条独立的反向观测：驱动力不是交互预算，是每次交互的信息效率。（另注：v1.0 的 GAIA 口径是 Text-103 子集、v1.5/v1.7 是 Val-165，**跨版本比较 GAIA 无效**。）[E: ext-dr-architectures.md#核验表9-11]

3. **唯一一个明确"standardizing compute"的受控评测，结论是"看拓扑与任务，不看扇出"。** Google Research × MIT，260 个配置 / 6 个基准 / 5 种架构 / 3 个模型家族，标准化了工具、提示与算力：相对单 agent 基线从 **+80.8%**（可分解的金融推理）到 **−70.0%**（顺序规划）。错误放大 **Independent 17.2× > Decentralized 7.8× > Hybrid 5.1× > Centralized 4.4× > 单 agent 1.0×**（定义是**由 agent 间协调失败引起的额外计算量，从执行轨迹 token 估计**，不是错误率放大 17 倍——这个口径极易被讲坏）。能力饱和阈值：**单 agent 基线已超过约 45% 时，增加 agent 为负收益**。**tool-heavy 任务（例：16 工具的业务流）会吃到多智能体协调开销** —— 我们的任务恰恰是 tool-heavy。
   **限定**：R² = 0.373（加入 task-grounded capability metric 后 0.413），即**只解释约三分之一方差**，是有用的先验不是定律；架构选择预测命中 87% held-out 配置。另：`research.google` 博客（180 配置 / 4 基准 / +80.9%）、arXiv v3（260 / 6 / +80.8%）、Nature MI 版是**同一批人的同一项工作的不同载体，算一个来源不是四个**。[E: ext-orchestration.md#B,核验表13-19,假独立佐证登记]

4. **扇出自己制造一整类新故障面。** MAST：1600+ 条跨 7 个主流 MAS 框架的标注轨迹，150 条建分类学，专家标注 κ=0.88，14 个失败模式聚为 3 类；其中 **inter-agent misalignment 与 task verification 两类是单智能体根本没有的**。（该论文常被转述的"42%/37%/21% 失效分布"在 arXiv 摘要中不存在，标 `unverified`，**不得使用**。）[E: ext-dr-architectures.md#E4] [E: ext-orchestration.md#核验表46-47]

5. **Anthropic 自己划的边界与第 3 条互为独立佐证**："Some domains that require all agents to share the same context or involve many dependencies between agents are not a good fit for multi-agent systems today." 学术证据探索中大量子任务是**互相依赖**的（一条 claim 的核验依赖另一条 claim 的口径）。[E: ext-orchestration.md#A]

6. **OpenAI 的工程指南同向**：先把单 agent 做满；拆分的触发条件是**工具重叠而非工具数量**（">15 个定义清晰、彼此不同的工具"可以管好，"<10 个相互重叠的工具"会出问题）。[E: ext-orchestration.md#H,核验表45]

7. **并行的隐性成本在本轮调研里被我们自己实测到了。** 多个兄弟调研 subagent **共享 WebSearch 会话配额**，导致至少三个维度的调研在第 2–14 次检索时配额耗尽，被迫降级到 serper/curl 兜底，并各自留下"反证检索做得不够"的自陈。**这是一条来自我们自己运行的、关于扇出的负面数据点。** [E: ext-orchestration.md#U9] [E: ext-multimodal-evidence.md#R1] [E: ext-academic-apis.md#未决8] [E: ext-incidents-products.md#未决与风险]

8. **学术 API 侧的硬 RPS 直接封顶扇出**：arXiv **1 请求 / 3 秒且要求单连接**、Semantic Scholar 有 key **1 rps**、OpenAlex 语义检索 **1 rps**、CORE ≈0.5 rps。20 个并行 agent 各自发语义检索，平均每个要等 20 秒。**并行度必须由中央限速网关决定，不能由 subagent 数量决定。** [E: ext-academic-apis.md#结论摘要4,D2]

### 并行可靠买到的三样（这些有证据）

- **墙钟延迟**：复杂查询研究时间最多减少 90%（注意是 up to，上界不是均值）。[E: ext-dr-architectures.md#核验表3]
- **上下文卫生**：三条独立线指向同一处 —— IterResearch 每轮重建 O(1) 工作区（六基准 +14.5pp；交互扩到 2048 次时 3.5% → 42.5%；纯 prompting 策略对前沿模型最多 +19.2pp）；Anthropic 的子 agent 压缩比（子 agent 烧数万 token，只回传 1,000–2,000 token）；Context Rot 在 18 个模型上证明长输入退化"often in surprising and non-uniform ways"。在 DSH 的原生 subagent 上这几乎零成本，**应当无条件采纳**。[E: ext-orchestration.md#E,F,G,核验表23-29]
- **探索路径多样性** —— **只有在下游存在一个比 worker 更强的客观裁决步骤时**才兑换成质量。没有客观裁决器的扇出 = 吞吐量，不是质量。[E: ext-dr-architectures.md#结论摘要2]

### 裁决

**"扇出提升研究质量"作为通用命题：不押。** 它在本项目里被降级为一条 `unverified` 前提，**禁止出现在任何下游文档的论证链上**。任何写"因为我们是超并行所以质量更高"的句子都是回归。

**小仓位押三处，每处附它买的可测属性：**

1. **覆盖率维度的扇出**（候选文献检索、多来源交叉抓取、不同子问题并行探索）——这些单 agent 基线低、天然可并行。买 `按上游簇去重后的独立来源数`。
2. **同一条载荷断言的 N 路独立核验**（一路重跑数据、一路回原文抓字面、一路做口径推演、一路找反证）——**这是把并行从"多找"改向"多验"的关键动作**。这几路的输出是可比较的 pass/fail，天然适配客观裁决与 keep-if-better。买 `verified 断言数 / 预算`。
3. **上下文卫生**：每个 worker 短而干净的上下文，回传严格限长（对齐 1,000–2,000 token 量级），长内容一律落台账、回传句柄。买 `单 worker 上下文长度中位数`。

**明确禁止的扇出**：论证链构建、跨 claim 一致性推理、最终裁决 —— 这些是顺序任务，落在 −70% 那一侧。

**配套的三条硬约束：**
- **拓扑 = Centralized + 黑板台账**（错误放大 4.4× vs Independent 17.2×）。status 的唯一写者是**门代码**（01 §4 W-04，不是编排器）；worker 之间不互相喊话，只对证据台账做带 schema 的状态变更。**注意该独占的强制力分两层**（01 §0.2）：工具面（提交工具 schema 不含 status，出现即 `tools/pre-execute` deny）是结构性的；文件面**没有文件系统级强制**，因为 `run_code` 同时绕过内核沙箱与 `ctx.fs` 围栏。写权矩阵是**审计契约**，不是权限（01 §4.4）。
- **准入判据**：给某子任务加 worker 之前先问"单 agent 在这个子任务上的基线是否已经超过约 45%"。超过就不加。**该阈值来自单一工作且 R² 仅 0.37，且其六个基准里没有一个是文献研究**——按默认值用，列为**待标定参数**。
- **并行粒度优先级**：先把 `run_code` 内的工具调用级并行吃满（`maxParallelSubCalls` 默认 10，子调用**走完整 tool pipeline 且逐条落 `tool/code-dispatch-start` / `tool/code-dispatch` 日志**），再考虑 subagent 级扇出。理由：单步内的 intrinsic parallel tool calling 比多智能体编排便宜得多，且它的证据留痕更完整。[E: gt-exec-security.md#D-10,D-11] [E: ext-dr-architectures.md#D3]

**名字的处理**：`hyper-parallel` 在本项目里**不是质量主张，是吞吐与核验密度主张**。这句话应当出现在 README 首屏。

### 会推翻它的观测

| 观测 | 推翻什么 | 预注册的判据 |
|---|---|---|
| 固定美元预算下，A=单线程深挖 vs B=N 路扇出+客观裁决，B 的 `verified 载荷断言数 / 预算` 显著更高（topic 级配对，p<0.05），**且** B 的 false-claim rate 不高于 A | 推翻"我们押小了"——扇出确实买质量 | 任务集 ≥12 个 checkable-answer 问题；两臂**同一渲染器**产出（见 B2 的 M8 教训） |
| 我们自己的深度 × Fact-Check 七档曲线显示**核验路径**的准确率也随并行度下降 | 推翻"扇出用在多验是安全的"——方向也错了 | 七档 2/10/30/50/70/100/150，在我们自己的语料上跑，判定器交叉厂商 |
| 在我们的任务族上重测 45% 阈值，拟合不成立或阈值明显不同 | 推翻准入判据 | R² 与阈值都要报出；若 R² < 0.2 则该判据整体作废，改用"该子任务是否有客观裁决器"作为唯一准入 |
| 中央限速网关下，实际有效并行度长期 < 4 | 推翻扇出的工程前提 | 若成立，架构应退回"少而深 + 强核验"，名字必须改 |

### 何时检验

- **M0（架构定稿前）**：不检验，按上述边界写死。
- **M1（第一个端到端 run 可跑后）**：跑深度 × Fact-Check 七档曲线。这既是 per-loop 门触发深度的参数来源，**也是本系统"产品即可信度"的第一个自证**——一个以"不洗数字"为卖点的系统，不能把别人的单篇预印本当定论引用。
- **M2（首批 checkable-answer eval 就绪后）**：跑等预算 A/B。**这个实验本身是产品证据的一部分**，不是内部调优。公开文献里不存在这个实验，我们做出来就是贡献。

---

## B2 · 结构与门提升质量（对"永不造框架"这一继承禁令的重估）

### 陈述

被审计的是 v1 的最高禁令 P-1：**"永不建造结构=质量框架"**，理由是"Paper Graph 的三代框架被四次严格消融判死"。

### 反对该禁令的证据（GROUND-TRUTH-CORRECTIONS §B 为 binding，以下逐条）

1. **对象错配 —— 被消融测过的只有第四代。** 四次消融测的全部是 **nodify（2,036 行 src）**；那个 13,949 行 src + 9,101 行 tests + **595 个绿测试**、55 条 CLI 命令、106 条 V-rule、11 态队列 + 900s 租约 + 三把文件锁的 **paperproof v2 从未被任何消融测过** —— 它死在 live run 的 **DocsWorker 输出契约**上，**根本没走到"结构是否帮助推理"这个问题**。把"595 测试全绿的框架"与"被消融判死"缝在一起是两个对象的混同。[E: GROUND-TRUTH-CORRECTIONS.md#B1] [E: gt-pg-failure.md#F2,F22,C5]

2. **趋势线是三个不同的量拼出来的。** `+1.0 → +0.13 → −0.39` 三格的**被测量**（文章 overall 5 维 vs 调查 dossier composite 6 维）、**作者模型**（Opus / Sonnet / Opus）、**对照臂**（skills / skills / notree-aggressive）、**渲染方式**（前两轮两臂都是人写文章；第三轮一臂机器渲染）**全不同**。一手 `comparison3/RESULTS.md` 的同一张表**逐格标注了 model 与 artifact 列**；v1 转述时抹掉标注，只留数字，把"三次不同实验"变成了"一条单调趋势"。[E: GROUND-TRUTH-CORRECTIONS.md#B2] [E: gt-pg-failure.md#F19,C2]

3. **−0.39 的最大一项是渲染伪影，且可机械证明。** calibration 一项 tree=4.00（15/15 全是 4）、notree=5.00（15/15 全是 5），**方差为零**——这是"评委在评格式"的典型签名。机械原因在 `comparison3/dossier.py:100-105`：只有当节点 `status ∈ {retired, stuck}` 时才输出 `## Dead ends / retired` 段；而 `results_mechanical.json` 显示 tree 臂 **5 个 topic 的 dead_ends 全为 0**、notree 臂 **5/5 `has_dead_ends: true`**。而 `JUDGE_PROMPT.md` 的 calibration 定义原文就含 *"are uncertainty, **dead ends**, and open gaps stated honestly"*，三个评委的减分理由文字上就是"缺那一节"。逐步剔除具伪影签名的维度：6 维 −0.39 → 去 calibration −0.266 → 再去 convergence −0.20 → 再去 adversarial_completeness（同样 15/15 满分零方差）**−0.13**，与同轮 grounding 的 **+0.134** 同量级、反号。**最保守读法下净信号约等于零。** 作者在同一文件里写了这一点（"asymmetric dossier rendering … explains most of the calibration gap"），v1 只引了结论句。[E: GROUND-TRUTH-CORRECTIONS.md#B3] [E: gt-pg-failure.md#F11,F12,C3]

4. **"纪律是最大杠杆（+1.0）"只存在于 N=1，且在 N=5 时反转。** comparison2 的 by-arm overall 是 raw 4.53 / skills 4.60 / tree 4.73：**skills − raw = +0.068 (t=0.19)，tree − skills = +0.134 (t=0.43)** —— **机器增量反而大于纪律增量**。且 c2 的 `PROMPT_raw.md` 已含四条核心纪律中的三条（counterpoints 段、calibration、verbatim quote、保存源文本），作者自披露为 confound，意味着 raw 臂本就是"半纪律臂"、`skills − raw` 注定接近 0。
   **注意这个错误的源头在一手 `RESULTS.md` 自身**（它写 "The discipline, not the machinery, is the lever" 与自己的表格矛盾），v1 整段继承。**一手不等于正确 —— 这是本轮最该记住的方法论教训。** [E: GROUND-TRUTH-CORRECTIONS.md#B4] [E: gt-pg-failure.md#F8,C4]

5. **一个统计量疑似凭空产生。** v1 的"8 次校验失败中 5 次是机器层故障"在全仓库（`.loop/state`、git log、archive 全部 md/json）**零命中**。[E: GROUND-TRUTH-CORRECTIONS.md#B5] [E: gt-pg-failure.md#C6]

6. **P12 已被本仓库自身日志证伪。** P12 声称"并行 worker 输出不相交这条规则执行良好、没出过事"，而 `.loop/state/log.md` 记录了 **m7-eval1 的真实数据丢失事故**（后续轮次 wave member 复用第一轮输出路径，覆盖已提交结果，**30 个测试全部漏检**，靠 fresh evaluator 才发现）与 **9-component-audit 的 `rm -rf` 事故**（只读评审 subagent 删掉了未跟踪、不可恢复的运行数据）。[E: GROUND-TRUTH-CORRECTIONS.md#B6] [E: gt-pg-failure.md#P12引文]

7. **样本量与尺子分辨率。** 整个证据体的独立研究问题只有 **6 个**（三轮共用同一组 5 个中文经济学问题；c1 唯一问题 == c2 T1 == c3 T3；c4 是第 6 个），36 次 author run，四套不同装置。评分尺 1–5 整数，实际只出现 4 和 5：**c2 的 45 个 overall 分全部是 4 或 5，一个 3 都没有**；c3 的 180 个评分里只有 1 个 3。**有效分辨率 2 档。** topic 级配对检验下**没有任何一项跨臂差达到 p<0.05**（c2 tree−skills 的 95% CI ≈ [−0.72, +0.99]，数据同时兼容"树好 1 分"与"树差 0.7 分"）。**"判死"这个措辞在一手文档中不存在**；四位作者每轮都写了限定语（c3 逐字："N=5, 3 judges — margins of ±0.1–0.4 are suggestive, not definitive"）。[E: GROUND-TRUTH-CORRECTIONS.md#B7] [E: gt-pg-failure.md#F16-F20]

8. **附加的两条**：comparison2/3/4 **未进 git**，无篡改防护（内部一致性可查，但一致性 ≠ 防篡改）[E: gt-pg-failure.md#F6]；c4 的 "bounded-sublinear" 与自身数据矛盾（tree 的逐期增量 **+1594 / +2409 / +2810 是递增的**），且 resume 成本定义不对称（tree 只算 `nd brief` 的 stdout，**不计 prompt 明确要求的 `nd tree`/`nd show`/`nd docs for-node`/`nd recall` 下钻**；notree 算整份 log 且被 prompt 限死为单一扁平 log）→ 那个 3.0× 测的是**"有索引 vs 无索引"**，不是"树 vs 无树"，且 N=1。[E: gt-pg-failure.md#F14,F15,C8]

### 支持"结构有用"的证据（同样标强度）

- **机械可审计性是唯一跨四轮方向一致的优势，且靠代码不靠评委**：c1 quote fidelity **1.0 vs 0.92**；c3 tree 臂 **4/5 个 topic grounding_rate = 1.0**（China 0.8），notree 靠自觉；cite 硬解析。**强度：中**（是机械量、可复现，但样本仍是那 6 个问题）。[E: gt-pg-failure.md#F21-授权2]
- **有索引的 resume 状态斜率低约 3×**。**强度：弱**（N=1 且定义不对称，见上）。
- **反向的强证据**：一个"通用编排机"在这个问题域上是**净负债** —— 13,949 行 + 595 个绿测试，live run 仍停在第一波 worker 输出。**强度：强**（代码可统计，事故有 commit 记录）。[E: gt-pg-failure.md#F21-授权5]
- **刚性带来的摩擦是真的**：c3 tree 臂 **5/5 个 topic 的 max_depth 只到 2**（budget 允许 3），`dead_ends` 全 0；`FRICTION.md` 记了 6 条真实卡点，其中 **#6「`nd docs for-node` 返回空但绑定存在」= 溯源假阴性**——"a fresh agent relying on `for-node` for provenance would wrongly conclude a node is unsourced"。**对以"每条 claim 带核验状态"为产品的系统，溯源假阴性 = 产品事故。** [E: gt-pg-failure.md#F13,F23]
- **17 条摩擦里没有一条是"逻辑树的思想不对"**；压倒性多数是同一类：**CLI/schema 不自描述 → agent 只能猜 payload、猜路径、猜信封形状**。`nd schema` 一个命令就消掉了整类（实测判词："完全消灭 payload 猜测，零看源码"）。**这条对本项目的价值高于四次消融的任何一个分数**，且随 worker 数线性放大。[E: gt-pg-failure.md#F25,F26,M5]

- **一条本轮新增的、独立于前代的强支持：人类证据学早就解决了这个问题，而它的解法不是框架，是"信号问题 → 确定性算法 → 可推翻判定"三层结构。** Cochrane RoB 2 的形态是成熟工业实践：每个偏倚域下挂 2–7 个**近乎事实性**（原文 "reasonably factual in nature"）的信号问题，答案只能是 Y/PY/PN/N/NI，然后由**写死的算法**把答案组合映射到 Low / Some concerns / High；算法只给 proposed judgement，人可以推翻，**但推翻必须写理由**。
  **而且这个形状有实测支撑**：同一篇 2025 JMIR 研究里，LLM 在**信号问题层面**平均准确率 **83.2%（95% CI 77.5–88.9）**，聚合到**域层面**降到 **65.2%**，整体 RoB 判定只有 **57.5%–70%**；另一项 2024 Research Synthesis Methods 研究显示，直接让 LLM 输出 RoB2 判定，**F1 只有 0.1–0.2，与平凡基线无异**。
  **结论极其明确：让模型答事实问题，让代码做聚合判定。** 这是对 B2 白名单的独立佐证——它支持的不是"建框架"，恰恰是"把判断拆成可确定性聚合的事实问题"。
  边界同样被测出来了（BMJ EBM 2026 的半自动 GRADE 工具，115 篇 Cochrane 综述）：**能算的几乎全自动**（imprecision 按参与者数 0.97/F1 0.94、I² 0.90/0.90、AMSTAR 0.98/0.99），**要判断的不行**（risk of bias 仅 0.73/0.70；整体 GRADE 等级一致率 **63.2%，Cohen's κ = 0.44**）。[E: ext-human-methodology.md#结论摘要1,2,5,A]

### 裁决

**把 P-1 从"最高禁令"降级为一条有界的、标明证据强度的设计偏置，逐字如下：**

> **P-1′**：在 N≤6 个中文经济学问题、强模型、单上下文、LLM 评委、有效分辨率 2 档、无任何 p<0.05 的条件下，我们**没有**理由相信"结构必然提升推理质量"，因此**任何结构投资必须自证增益**——先声明它要买的可测属性，并附一个能在本系统语料上重跑的度量；但我们同样**没有**理由断言"结构必然有害"，尤其**不授权**外推到本项目的产品目标（可重跑数据分析的正确率、引文可追溯率、超长多 loop 续跑能力）——**那三项正好落在四次消融从未测过的区域**。唯一被多轮复制的结构收益是**可审计性**与**有界 resume 状态**。

**允许建的结构，白名单四类（每类绑一个可测属性）：**

| 结构 | 买什么可测属性 | 一手依据 |
|---|---|---|
| 强制可审计性（quote 逐字子串校验 / content_hash 去重 / cite 硬解析） | `引文字面匹配率`、`grounding_rate` | c1 fidelity 1.0 vs 0.92；c3 grounding 4/5=1.0 |
| 有索引的 resume 状态，**且必须同时携带结论与结构/工件清单** | `冷接管所需命令数`、`resume 字节斜率` | nodify G1：只给结论会导致接管者**违规直读 records.jsonl** |
| 结论强制落盘 | `跨 phase 结论存活率` | P14（有一手事故记录） |
| **溯源查询是主路径且有假阴性 fixture 测试**（存在绑定 → 查询必须命中） | `溯源假阴性数 = 0`（硬门） | FRICTION #6 |

**禁止建的（有代码级证据的净负债）**：通用编排机——队列引擎 / 租约 / 多把文件锁 / 多态状态机 / 自建 durable execution 图状态机。durable 需求由**台账的幂等追加 + 可重跑产物**满足，不由框架的 checkpoint 满足。[E: ext-orchestration.md#D7]

**换尺子（这是本节最重要的一条）**：四次消融真正证明的是——**拿 LLM 评委的判定质量作为结构的验收指标，在 4–5 档的尺子上是测不出来的**。这是**度量方法**的失败，不是结构的失败。本项目的验收指标（verified/unverified 比例、引文字面匹配率、数据重跑一致率、false-claim rate）**全部是机械可测的**，不需要 LLM 评委——**这正是四次消融缺的那把尺子**。前代自己已经诊断到位：`nodify/docs/12-depth-and-rigor.md`（v1–v4 全部结束后写的最后一份设计，标记 ADOPTED，**从未执行**）逐字写着 *"Tasks: questions with CHECKABLE answers — so rigor is scored against ground truth, not a judge"*，并预注册了 kill-criterion：*"if a strong model already deepens + self-refutes natively … the scaffold adds nothing and we say so. This is the real risk."* **本项目的第一个 eval 直接从这里起跑。** [E: gt-pg-failure.md#M1,M3]

**两条配套纪律：**
- **复杂度预算的单位不是行数，是"机器层 vs 研究层的失败比"**：每个 gate 报告必须分类统计本轮失败（研究内容层 / 契约层 / 机器层），机器层占比超阈值即触发简化，而不是等到 live run 才发现。[E: gt-pg-failure.md#M4]
- **任何 A/B 的两臂呈现必须机械对称**（同一渲染器渲染两臂，或两臂都手写），且任何"是否存在某一节"的差异必须先排除再解读分数。c3/c4 两轮的最大教训之一是**渲染器决定了判分**。[E: gt-pg-failure.md#M8]

### 会推翻它的观测

- **推翻白名单某一项**：在 checkable-answer 任务集上，带该结构臂与无该结构臂的 false-claim rate 差值 CI 跨 0，**且**引文字面匹配率无差异 → 该结构项进 kill list，代码删除而非保留。
- **推翻禁令（结构其实必要）**：无结构臂在超长多 loop（≥8 phase）上出现续跑断裂而带结构臂没有 → "有界 resume 状态"从"待自证"升为"已证"，可扩大投资。
- **推翻"机械指标是够用的尺子"**：机械指标全绿的产物在人类抽检（≥30 条，报出评分者间 κ）里仍被判为低质量 → 说明我们换的尺子也没测到质量。**那时要补的是任务集难度，不是回去用 LLM 评委面板**（理由见 B5 的 judge 上限）。
- **推翻 P-1′ 的边界**：若有人在**本项目的任务族上**（可重跑分析 / 引文追溯 / 超长 loop）跑出结构有害的受控证据，P-1′ 应当收紧回禁令。

### 何时检验

M2（首批 checkable-answer eval），以及**每次新增结构组件时**——该组件必须自带 A/B 计划与可重跑度量，否则不得合入。这是准入条件，不是验收条件。

---

## B3 · 可信度是产品（用户会为"带判决的报告"付出成本）

### 陈述

用户愿意为"每条载荷断言携带机器判定状态"付出成本（更慢、更贵、覆盖更少、大量显式的"未验证"），因为市面上没有一家产品提供它。

### 支持证据

1. **分层缺口是实测的，且在 2026 前沿产品上依然存在。** 14 个模型 × 130 条研究 query，用**确定性 Markdown AST 解析器**（不用 LLM，避免抽取器污染）抽取引文-主张对，三维分别判定：**Link Works 94%+ / Relevant Content 80%+ / Fact Check 仅 39–77%**。分模型：Claude Opus 4.5 98.7/95.7/**76.8**；GPT-5.4 100.0/93.7/**47.7**；Gemini 3.1 Pro 94.1/80.7/**48.5**。限定：arXiv 预印本、产业实验室、判定器是 rubric LLM-judge（经人工校准）。[E: ext-citation-faithfulness.md#核验表6,7]

2. **补引文救不回来。** 把某条陈述的**全部**被引源合并后重判，**95.1% 原本不被支持的陈述仍不被支持**。失败模式不是"证据分散在多个源里"，而是"该陈述根本没有源"。**强度：本维度最强**——Nature Communications 同行评审，800 题（400 Mayo Clinic 生成 + 400 Reddit r/AskDocs 真实）、约 58,000 对陈述-源，且判定器与 3 名美国执业医生共识的一致率 **88.7%**、医生之间平均一致率 **86.1%**（p=0.21，即判定器已接近人类间天花板）。[E: ext-citation-faithfulness.md#核验表4,5]

3. **商用产品对照表：全线空白。** OpenAI Deep Research / Gemini Deep Research / Claude Research / Perplexity Deep Research / Grok DeepSearch —— **没有任何一家给单条断言附核验状态，没有任何一家做撤稿检查**。口径必须精确：Claude 的 CitationAgent 做的是**归位**（"identify specific locations for citations"），不是判断来源是否支持该论断；Perplexity 的"Every factual claim in Deep Research carries an inline citation"= **每条断言都挂链接**，与"每条断言都被核验"是两件事。[E: ext-incidents-products.md#D-1,D-3]

4. **核验机器已经被造出来了，但只被造成了"尺子"。** Perplexity 2026-07 开源的 WANDR 基准（500 个证据密集任务、170,495 条须有出处的记录），其评分器**重新抓取被引页面、检查摘录是否真的出现并支持该论断**——这正是我们要做的事，而 Perplexity 把它放在评测器里，没放进产品里。同时该榜单说明能力现状：**hard F1 最高 0.133**，即当前最强系统里只有约 13% 的记录能做到整棵子树全对（soft F1 0.363，$5.20/task、中位 14.9 分钟、3.82M tokens/task）。[E: ext-incidents-products.md#D-3,核验表]

5. **后果已经可以按年计数，且用户画像对得上。** AI Hallucination Cases 数据库页面自述更新至 2026-08-16，收录 **1922 起**法院/仲裁庭已认定或暗示当事人依赖幻觉材料的裁判，覆盖 40 个法域。**按使用 AI 的一方：Pro Se Litigant 1111 起 > Lawyer 761 起** —— 不是"大所律师偷懒"，是"自己扛后果的个人"。按幻觉性质（**可多选，各项之和 > 总数，不可相加**）：Fabricated 1601 / **Misrepresented 800 / False Quotes 519**。作者自述该库"necessarily an undercount"，且**只收录已进入司法认定的案件**——用它论证规模是稳的，用它论证总量是错的。
   **推论（本项目的立论基础）**：接近一半的司法认定案件里，被引来源**真实存在**，问题出在"它并不支持你说的那句话"。**一个只做 DOI 可解析性检查的核验器，对这一整类的召回接近 0，却会给出令人安心的绿色。这比不做更危险。** [E: ext-incidents-products.md#B-1,设计含义3]

6. **后果的升级路径不是退钱了事。** Deloitte 加拿大 / 纽芬兰与拉布拉多省：**C$1.6 million**、526 页 Health Human Resources Plan、发布时未披露使用 AI；2025-11-28 投诉、**2026-03-07 CPANL（省注册会计师协会）正式立案职业纪律调查**；截至 2026-03 无退款。Deloitte 的辩护是错误 "do not impact the report findings" —— **这正是本项目要用机器状态否定的那句话：引注不成立，结论就不能被称为有据。**
   （**本轮纠偏留痕**：澳洲那起流传最广的"退款 44 万澳元 / 29 万美元"**全是错的**——A$440,000 是**合同总额**、US$290,000 是它的美元折算；实际退款是合同最后一期款 **A$97,000 以上（约 US$63,000）**，占合同额约 22%。数十个网页把合同额当退款额，且**全部回溯到单一上游**。）[E: ext-incidents-products.md#A-1,A-2,核验表]

7. **上游不会替你把关。** 《柳叶刀》2026-05-07 审计：**PMC Open Access 子集** 250 万篇论文（2023-01-01 至 2026-02-18）的 **9,710 万条已核验参考文献**中识别出 **4,046 条**捏造引文，分布于 **2,810 篇**；发生率从 2023 年**每 2,828 篇 1 篇**升到 2026 年初**每 277 篇 1 篇**；**98.4%** 的受影响论文在审计时点出版方零动作。
   **必须带的两条限定**：(a) Cochrane 的 Ella Flemyng 批评该 AI 检测方法"仅在 500 条记录上验证过，方法细节披露不足"，因此应表述为"**该方法识别出**"而非"确实存在"；(b) Retraction Watch 的报道把数字写成 4,406、把语料写成 "papers indexed in PubMed"，**两处口径均偏**，以 4,046 / PMC OA 为准。[E: ext-incidents-products.md#C-1,核验表]

8. **中文场景有一道具体的缝。** 《学位法》第三十七条的可撤销事由是**代写、剽窃、伪造**；而高校 AIGC 检测抓的是**生成比例**。**一篇 AI 率合格但引注是编的论文，检测抓不到，却正踩在第三十七条上。** 本项目的证据附录补的正是这道缝——它证明的不是"我没用 AI"，而是"我说的每句话都能当场被查证"。
   （口径注意：流传的"直接生成内容不超过 20%"是**华东师大传播学院 + 北师大新闻传播学院的联合指南**，非教育部规定、非全校规定，**不可外推**。）[E: ext-incidents-products.md#E]

### 反对证据 / 必须一起承担的代价

1. **验证成本与生成成本同量级。** PaperBench 的自动判官 **$66/篇**（F1 仅 0.83）；Kosmos **$200/run**；AI Scientist 单次自动评审 $0.25–0.50、每篇总运营 $10–15；WANDR 榜首 $5.20/task。**验证必须是独立预算科目**，否则它会在压力下被第一个砍掉——而它正是产品本身。[E: ext-science-agents.md#设计含义14,核验表]

2. **全文可得性天花板很低。** 实测 OA 占 **38.1%**，有缓存 PDF 的仅 **17.0%**，且 GROBID 解析不了全部 PDF。**"每条论断都可追溯到原文"这个承诺，在现实中最多覆盖约六分之一的文献。** [E: ext-academic-apis.md#D6]

3. **一份大部分是 unverified 的报告可能不可用。** PaperQA2 的"超人类"只在**允许弃答的 precision** 上成立（85.2%±1.1 vs 人类 73.8%±9.6, p=0.0036），**accuracy 上人类更高且无显著差异**（66.0%±1.2 vs 67.7%±11.9, p=0.66）。反面案例更刺眼：Ask Practical Law AI 的幻觉率最低（18%）**是因为它 62% 的问题不作答**。→ 弃答太多 = 产品无用。**报告级指标必须成对出现**：`已核验断言的正确率（precision）` 与 `全部断言中被核验的比例（coverage）`。**只报其一，就是我们自己在做本项目所批判的口径扭曲。** [E: ext-citation-faithfulness.md#核验表15,17,D6] [E: ext-science-agents.md#设计含义13]

4. **检查器的假阳会把人淹死。** 104 条人工核验引注（71 合法 / 33 有问题）上测 5 个现成核查器：**RefChecker 在 71 条合法引注中误报 36 条（>50%）**，CheckIfExist 34，HalRef 53。作者结论："not sufficiently reliable to support definitive decisions without further verification"。→ **若把检查器报警的东西全推给人，人会开始无脑点通过，检查点退化成橡皮图章。** 自动打回的阈值必须按**精确率**调，不按召回率调。[E: ext-incidents-products.md#D-5,设计含义2]

5. **改稿会静默破坏已核验的内容。** 五个 Deep Research Agent 的多轮改稿：能落实 **>90%** 的用户反馈，同时**在 16–27% 的既有内容与引用质量上回退**，内容型反馈下 break rate 均值 **31%**；论文明确说"提示工程和专设改稿子 agent 这两种推理期修法都解决不了"。→ 一条曾经 verified 的断言会在改稿时被悄悄改坏，而报告级的"已核验"图章不会掉。[E: ext-orchestration.md#D,核验表20-22]

### 裁决

**押大仓位。** 差异化陈述可以直接落地为一句可检验的话，且其每个字都有一手对照表支撑：

> 市面上所有 deep research 产品交付的是**带链接的报告**；本项目交付的是**带判决的报告**。

**但产品承诺必须同时写死三条降级，否则这句话就是我们自己在做口径扭曲：**

1. **谓词边界**：我们承诺的是 `faithfulness`（引语忠于来源）+ `existence`（来源存在）+ `re-runnability`（分析可重跑），**不是 factuality**（命题为真）。展开见 B5。
2. **覆盖率天花板写进首屏**：全文级核验最多覆盖约 1/6 的文献；其余只能落在**更低的获取层级**上——`evidence_grade ∈ {G3, G2}` 及其状态上限（01 §3.3、§3.4），或 ST-U / ST-N。**把「全文核验」「只核到摘要/题录」「我们没能力看」三者混为一谈是本项目最大的可信度风险**——01 §1.4.1 把后两者拆成 ST-U 与 ST-N，正是为此。
3. **成对报数**：`precision` 与 `coverage` 必须同时出现。**单指标报告应当让 gate 脚本直接构建失败**，而不是靠自觉。

**外加两条由证据强制的机制：**
- **核验状态存在断言级，不是报告级**；每条断言存内容哈希，文本一变状态即失效并重跑 `S`（01 §1.5）；交付物给出**状态直方图**，让退化可见。（依据：多轮改稿回退 16–27%。）
  **R2 同步（01 §8.6.2.1）**：`status` 是**带时间戳的量**，不是永久属性——默认留存档 Tier B 下 `source_integrity` 的字节同一性依赖重取，而重取实测 3/5 成功，重取不到即落 ST-N。因此 `status.json` 必须携带 `computed_at` 与 `revalidated_at`，**交付物渲染时必须显示复核时点**；任何「本报告 N 条断言已核验」的表述若不附复核时点，即为未覆盖数字（01 §9.1）。
- **三分流的人工检查点，且必须窄**：机器判定成立 → 自动过；机器判定确凿不成立（DOI 不可解析 / 页面 404 / 引文原文不存在）→ 自动打回并重跑；**只有真正歧义的进人工**。（依据：RefChecker 的 >50% 假阳。）

**立刻可拿的差异化点：撤稿检查。** 三个条件同时成立——**全赛道空白**（9 个免费 GenAI 系统 × 15 篇撤稿论文 × 5 问 × 2 次 = 675 prompt：最好的 ChatGPT 5 也只在 15 篇里全对 8 篇 = 53.3%，**SciSpace / ScienceOS / Consensus 零篇全对**；作者结论逐字："No currently available free-access GenAI system can be considered reliable for detecting or handling retracted literature."）+ **数据免费且机械**（Retraction Watch 全库以 CSV 公开托管在 Crossref 的 GitLab，每工作日更新，无需 key、无需注册；**2026-08-17 实测全库 71,799 行，其中 `RetractionNature = Retraction` 66,287 行，去重原文 DOI 62,708 个**）+ **工程量极小**（一次 DOI 集合 join，不需要模型）。
建议做成硬门禁：**任何引用命中撤稿库的断言，一律强制降为 ST-C，不允许自动过**（规范源已落为 flag F-05 `retraction` → `S` 第 0b 步 → ST-C，01 §7.2、§1.5）。

**但这里有一个会让天真实现直接出错的陷阱，必须写进设计：不要用 OpenAlex 的 `is_retracted` 当门。** 2026-08-17 实测 `is_retracted:true` = **134,175**，约为 RW 撤稿条数的两倍；随机抽样 200 条归因后：只有 **81 条（40.5%）**是 RW 记录里的"被撤原文 DOI"，**69 条（34.5%）其实是撤稿公告本身**（匹配 RW 的 `RetractionDOI`），另有 42 条标题形如 "Retraction: … / Correction to …" 但 RW 无记录。该字段是"被撤论文 ∪ 撤稿公告 ∪ 部分更正公告"的并集 —— **直接当布尔门会把撤稿公告本身判成造假文献**。→ 门必须打在 **RW 的原文 DOI 集合**上，不是打在一个厂商的布尔字段上。

**且污染筛查整体必须是三层分级门 + 一个显式的"未覆盖"状态，不是一个布尔标签：**
- **T0 确定性免费**（RW 全库 join、Crossref `update-to` 关系）→ 可给红/绿，**唯一可以做成 hard gate 的层**。
- **T1 纯代码取证**（GRIM/GRIMMER/statcheck 一类）→ 只能在**适用条件成立时**给黄，**且必须把适用条件本身写进证据**。GRIM 的检出功率有闭式解 `power = 1 − N·items / 10^d`（`d` = 报告小数位），经 20,000 次/点蒙特卡洛验证（N=28 实测 0.718 vs 理论 0.720）；**N≥100 且 d=2 时功率恒为 0**，多题项量表均值几乎立刻失效。**一个不带 N/items/小数位判定的 GRIM 门，会在大样本论文上给出"全部通过"的假安全感。**
- **T2 外部概率性**（Papermill Alarm / STM Integrity Hub / Cabells / ImageTwin）→ **永远只能给"待人工复核"，绝不参与自动判定**。理由：要么只对出版商开放，要么订阅制无公开 API，且**没有一家公布过带口径的 false positive rate**。
（另注：PubPeer 无公开授权 API，robots.txt 明确 `ai-train=no` 且 Disallow ClaudeBot——**能访问 ≠ 被授权**；DOAJ 自 2026-03-17 起把当前版本数据转为付费，免费版滞后一个月。）

**先发窗口有限**：MIT Tech Review 自测显示 Consensus 在两个月内从 18/21 改善到 5/21——这个能力是可修的，且该数字会快速过期（2025-06 / 2025-08 快照）。[E: ext-incidents-products.md#D-4,核验表,设计含义6] [E: ext-literature-integrity.md#结论摘要1,2,3,5,6,7] [E: ext-academic-apis.md#D5]

### 会推翻它的观测

| 观测 | 推翻什么 |
|---|---|
| 首批真实任务上「ST-V 或 ST-A」的承重断言占比中位数 < 30%（分 `evidence_grade` 分箱报，01 §3.3），且用户交付后仍需自行核验 > 50% 的载荷数字 | "带判决"没有兑现，产品对用户是净负担 |
| 核验成本 / 总成本 > 60% 且 ST-V 比例未随之提升 | 核验管线设计有误，或该任务族不适合 |
| 任一主流产品上线 per-claim 核验状态 + 撤稿检查 | 差异化消失，产品价值必须重新论证 |
| 人工检查点的"通过率"长期 > 95%（橡皮图章签名） | 三分流的阈值设错了（假阳太多） |

**竞品观测必须每季度复查一次** —— D-1 对照表是 2026-08-17 的快照，它本身会过期。

### 何时检验

M1 起每次真实 run 记账（状态直方图 + 核验成本占比 + 人工检查点通过率）；M3（首个外部用户任务）做一次用户侧检验。

---

## B4 · 三条 kind 是充分的分类

> **〔R2 · C-10〕本节原来自行规定了一套六值状态枚举与若干字段名，与规范源 01 §1.4 完全不同，
> 照它实现即触发 01 §1.6 **V1.1**（"出现第七个值即失败"）。本节现已全部替换为 01 的符号，
> 且**本文件不再持有任何枚举定义**。这次漂移的根因是撰写流水线，如实记录在本节末。**

### 陈述

系统交付的全部承重断言，可以被三条**证据生产方式**穷尽表达。它们在规范源里的名字是 `kind` 的三个值：
K-D `data-derived` / K-L `literature-cited` / K-I `logically-inferred`（01 §2；K-L 再按 01 §2.2.1 由机器判定分成
K-L-T 转录型 / K-L-A 归因型）。**本节写作时 01 尚未定稿，"三通道"是本文件当时的自造词，现已作废；下游一律用 01 §2 的 `kind`。**

### 反对证据：三条 kind 表达不了的八类

1. **图表派生数字。** 同一模型、同一批图，只改"有没有印刷数据标签"：MAPE 从 **1.3%–1.8%** 升到 **7.2%–7.4%**（约 4–5 倍）。族间差异更大（同模型 Adaptive MAPE）：scatter 2.63 / bar 3.78 / line 4.35 / **pie 11.58 / radar 28.01**。这类数字**既不是已验证，也不该和"完全没查过"同列**。
   更硬的一条：**"表格结构对了"会伪装成"数字对了"** —— 同批图表结构正确率 **194/200 = 97.0%**，而同批无标签图数值 MAPE >7%。**结构正确率不得作为任何验证信号**（但可以当覆盖率的分母，见下）。
   还要注意指标本身的容差：RMSF1 的 θ=0.1 意味着**被判"对"的单元格仍可有 10% 相对误差**，而 8% 误差足以翻转"A 优于 B"这类比较结论。[E: ext-multimodal-evidence.md#核验表11-14,16-18,D1,D2]

2. **只核到元数据层的断言。** 语义检索只检索标题+摘要；OA 38.1%、有缓存 PDF 17.0%。"核到摘要说了这件事"与"核到正文那一段说了这件事"是两个不同的证据等级。[E: ext-academic-apis.md#D6]

3. **撤稿 / 更正 / 关注声明。** "来源支持该断言，但该来源已被推翻"—— 三条 kind 里没有位置。[E: ext-incidents-products.md#D-4]

4. **反证存在。** "找到了反证"与"没找到支持"是完全不同的两件事。而**选择性引用**（真句子、假图景）逃逸逐字匹配，但逃不过"你有没有找过反面"这个**可审计的过程字段**。[E: ext-security-injection.md#D5,D6,N6]

5. **解释 / 综合类断言 —— 三条独立证据指向同一处塌陷。** Kosmos 自评：数据分析类 **85.5%**、文献类 **82.1%**，但**综合/解释类只有 57.9%**（且 102 条语句的分项计数未披露，分母不明，若综合类只有十几条则 CI 极宽）；*Correct Answer, Wrong Mechanism*：28 次 agent 尝试中 20%（主模型 4/20）到 37.5%（跨模型 3/8）出现"结论对但机制错，且用与自己数据矛盾的物理去辩护"；SoundnessBench for AI Scientists：12 个前沿 LLM 在标准提示下对低严谨提案的**假阳性率 74.0%**（改激进提示可降到 19.9%，但高严谨提案**召回崩到 36.1%**——**判官阈值是不可同时优化的权衡，因此不存在"正确的判官阈值"**）。[E: ext-science-agents.md#核验表189-192,242-245,结论摘要2]

6. **谓词错配（最致命的一条）。** v1 的 `verified` 一个词同时承载两个不同的谓词，必须拆成三个正交字段。
   **三个字段的名字、值域与判定方式定义在 01 §1.2 与 §1.3，本文件不复述**（01 §9.0、V9.1）。
   本节只保留判词：**把 `quote_faithful` 的绿灯当成 `claim_supported` 的绿灯，就是把审计能力当真值能力卖** —— 这正是本项目的方法规则要防的那种口径失真，只不过发生在产品语义层而非数字层。[E: ext-security-injection.md#结论摘要,一]
   **一处必须一起带的收紧（R2 同步 01 §8.6.2.1）**：本节旧写法把 `quote_faithful` 标成"我们能 100% 兑现"。在**默认**留存档 Tier B 下这句话是错的——Tier B 只保留围绕引语裁出来的摘录，拿引语去匹配它是**构造性恒真**（匹配成功是裁剪方式的推论，不是对来源的检验）；`source_integrity` 的字节同一性子测试同样退化为依赖重取，而重取实测 **3/5** 成功。准确说法：`quote_faithful` 是**判定时刻**哈希可判的确定性谓词，此后由哈希链**背书**而非**重判**，"任何时候都可离线 100% 复核"只在 Tier A 下成立。**这条直接改变产品面**：status 是带时间戳的量，必须携带 `computed_at` / `revalidated_at`。

7. **来源独立性 / 转引链。** 前代台账的 `raw_ref` 只有一个 URL 字段，**转引链完全不可表达**——nuclear-safety 的 honesty flag "n1（IPCC 12g）与 n5（Fukushima 2,313）源自 World Nuclear Association（行业机构）而非 IPCC/官方重建直接" 就是这一类。实证依据：中文层 **11 个域名回溯到 1 条日经**；BadRAG 的 **8 个域名回溯到 1 篇论文**；**Unpaywall 与 OpenAlex 是同一后端**（官方明说 "Unpaywall records are served from the same OpenAlex data"，实测返回体 `evidence`/`updated` 字段值已 literally 变成 `"deprecated"`）。[E: gt-pg-current.md#I-6] [E: ext-security-injection.md#D3,E3] [E: ext-academic-apis.md#结论摘要2,D4]

8. **口径三元组本身。** nuclear-safety 的 5 条 honesty flags 里至少 3 条是**数字为真、口径被换**（无置信区间就断言等价；0.03 是 OWID 低端值、内嵌了低估的 Chernobyl 计数；WNA 转引 IPCC）。口径必须是**结构化字段并随前提传导**，不能是散文。[E: gt-pg-current.md#F-32,I-4]

### 裁决

**三条 kind 作为"证据如何被生产"的分类是对的；作为"断言的状态空间"是不够的。二者必须分开建模。**

- **kind** —— 决定走哪条固定流水线、过哪些门。三个值见 01 §2，本文件不复述。
- **status** —— **六值枚举唯一定义在 01 §1.4**：ST-V / ST-A / ST-E / ST-C / ST-U / ST-N；计算式是 01 §1.5 的 `S`。本文件不得旁路它，也不得引入第七个符号（01 §7.4 V7.10）。
- **本节旧枚举 → 01 的对应关系**（留档，供下游 diff；**左列一律作废**）：

| 旧（作废） | 落到 01 的哪里 | 说明 |
|---|---|---|
| `verified-fulltext` | ST-V + `evidence_grade = G5`（01 §3.3、§3.4） | "全文级"不是状态，是**获取层级**，是正交维度 |
| `verified-metadata` | ST-A 或 ST-N + `evidence_grade ∈ {G3, G2}`（01 §3.4） | G3 的 ST-A 仅限"关于摘要内容本身"的断言；G2 仅存在性断言、**不得支撑任何数值断言** |
| `estimated(±ε, method)` | ST-E + flag F-10 `chart-extracted`（01 §3.5、§7.2） | ε 与 method 由 F-10 强制携带（01 V7.2、V3.4） |
| `inferred` | ST-A；上限由 01 §2.3.1 定（K-I 永不可达 ST-V） | "推断"不是一个状态，是**某个 kind 的可达状态上限** |
| `contested` | ST-C（01 §1.4） | 同名同义 |
| `unverified` | ST-U 与 ST-N **两个**值（01 §1.4.1） | 旧枚举把"我们看了没找到支持"与"我们没能力看"压成一个词——**那正是前代 rigor 门 fail-open 的形状**（伪造 metric + 无原始数据 → exit 0 PASS） |

**这张表本身就是 C-10 的教训**：旧枚举把**三个正交维度**（认知状态 / 获取层级 / kind 上限）压进了一个枚举。
压缩后的枚举读起来更"信息丰富"，实现时却算不出来——因为没有一个偏序能同时承载三个维度
（01 §1.5.1.1 的格结构只对认知状态成立，且其中 ST-A 与 ST-E 明写不可比）。
01 §1.4.3 明令禁止的正是这类压缩（它禁的是把工作流属性塞进状态枚举，同一个错误的另一个面）。

- **正交字段** —— 一律用 01 的字段名，本文件不再自造：

| 本节旧写法 | 规范源里的实际字段 |
|---|---|
| `quote_faithful` / `source_integrity` / `claim_supported` | 同名，定义见 01 §1.2 / §1.3 |
| `independent_cluster_count` | 同名，见 01 §1.3、§5.5 |
| `counter_evidence_found` + 其快照 ID | `counter_evidence_searched` / `counter_evidence_found`（01 §1.3）；**反证指针走 flag F-31 `contested-by`**（01 §7.2），不是 status 字段 |
| `metric_frame{指标, 样本口径, 对比对象}` | `metric_frame`，三字段定义见 01 §9.4（**本文件不复述字段名**） |
| `upstream_root` | `upstream_id`（01 §5.5 R-I6）；`independent_cluster_count` 按它归并 |
| `evidence_strength`（连续量） | **删除**。01 无此字段 |

`evidence_strength` 被删除的理由要写清楚，因为它是本节唯一一个**没有对应物**的自造字段：
它当初要买的是"二元 status 会吞掉静默漂移"（判为攻击失败的案例中仍有 **15.0%** 输出语义漂移 Δ≥0.3
[E: ext-security-injection.md#核验表V22b,D6]）。而 01 §1.4 本来就**不是**二元——该风险在 01 里由
§1.4.2（ST-C 与 ST-U 必须分开）+ §9.16（静默漂移）承担，证据强度维度由 `evidence_grade`（01 §3.3 六级）
+ flags（01 §7）承担。**〔挂起裁决 A-1〕** 若仍需要一个连续量，必须先改 01 §1.3 与 §9；本文件不自造。

**四条硬约束（改写为 01 的词汇）：**
- **定量 likelihood（概率区间）的状态下限——本文件提案，01 尚无对应规则。** IPCC 校准语言的规则是：只有**置信度为 high 或 very high** 时才允许给出定量 likelihood；证据有限且共识低时**连 confidence 都不许给**，只能给 evidence/agreement 的定性摘要词 [E: ext-human-methodology.md#结论摘要4]。翻成状态机是一条**类型系统级的禁止**，价值在于不依赖模型配合。01 里最接近的三条是 §3.4（G2 不得支撑任何数值断言）、§3.5（ST-E 禁止参与比较/排序/阈值）、§1.6 V1.6 第 3 条（K-L-T 进正文必须携带归因）。**〔挂起裁决 A-2〕**「likelihood 语言要求 status = ST-V」这条要么进 01 §1.4/§3.4，要么放弃；**本文件不得自行规定，也不得被下游当成已有规则引用。**
- **ST-E 的约束**见 01 §3.5 与 flag F-10：必须携带 ε 与 method、**禁止参与任何比较/排序/阈值判断**、渲染时必须原样显示 ε（01 V7.2、V3.4）。本项目押它，不改它。
- **K-I 的上限是 ST-A，永不 ST-V**（01 §2.3.1、§2.4）。本节为该裁定提供的证据是三条独立数字（57.9% / 20–37.5% / 74.0%，见上）+ 一条结构理由：推断**没有天然的重执行等价物**——K-D 有重跑，K-L-T 有"逐字子串在快照里"，K-I 什么都没有。
- **删除 `evidence_strength`**（见上表与〔挂起裁决 A-1〕）。原第四条"保留连续量"作废。

**决定：不新增第四条 kind。**
- 图表派生数值：kind 不变（多数落在 K-L），由 01 §3.5 强制 `base = ST-E` + F-10，**不是**"literature 通道 + 上限 estimated"那种旧写法。
- 元数据层核验：kind 为 K-L，`evidence_grade ∈ {G3, G2}`，可达状态上限由 01 §3.4 给出。
**理由（证据欠定下的决策）**：新增 kind = 新增一整套固定流水线与门 = 一次结构投资，按 B2 必须自证增益；目前**没有证据**表明"新 kind"比"kind 不变 + `evidence_grade`/status 细分"更好。记录此决定及其可翻转条件。

**同时采纳的两条便宜机制**（因为它们把弱信号用在了唯一合理的方向上）：
- **覆盖率闸门优先于正确性闸门**：错误结构是"漏"不是"编"——遗漏占错误类型的 **60%–74%**，幻觉率仅 **0.08%–6%**。对每张进入证据集的图/表，先枚举应有的数值槽位（系列数 × 数据点数，或行数 × 列数）再检查填充率，填充率不足即判失败，**早于**任何数值正确性检查。（那个 97% 的结构正确率正好当这个分母——**结构不能当正确性信号，但能当覆盖率的标尺**。）[E: ext-multimodal-evidence.md#核验表47,D6]
- **自洽性只做分诊，不做闸门**：抽样离散度与抽取准确率的 Spearman ρ 仅 **−0.30 ~ −0.37**（p<0.001），只解释约 9%–14% 的方差。**禁止**"N 次采样一致 → 判 ST-V"；**允许**"离散度进入最高分位 → 强制降级并入人工队列"。采样次数上限设 3–5 而非 16。[E: ext-multimodal-evidence.md#核验表19,20,D4]

**寻址必须独立校验（本节最高优先级的一条机制）**：结论正确对依据正确几乎没有预测力 —— 同一模型同一样本，标签 Macro-F1 **88.4** 而恢复关键单元格的依据 Macro-F1 只有 **34.8**；exact-match 设定下**没有任何模型能让"标签+依据同时正确"超过 50%**；SciTab 对 PoT 错误的人工归因里 **grounding error 占 50%**。→ 每个数字带 `{value, unit, address}`，并**用一个只看 address 不看结论的独立 agent 反向取值**与之比对。这把"验证结论"换成了"验证寻址"。附带推论：**不应把"更长的推理"当作图表证据的质量杠杆**（CoT 在 SciTab 上反而更差：GPT-4 zero-shot 3-class 64.80 → in-context+CoT 62.77）。[E: ext-multimodal-evidence.md#核验表24,26,29-31,D5]

### 会推翻它的观测

- **推翻"不新增 kind"**：图表派生数值在真实任务中占承重断言 **>20%**，且 ST-E 导致下游无法使用（大量结论依赖比较，而 01 §3.5 禁止 ST-E 参与比较/排序/阈值）→ 图表需要自己的 kind 与窄域专用管线（KM-GPT / AutoForest 式的"可复核约束"路径：坐标轴与 at-risk 表提取 100%、生存概率 median AE 0.005——但**这些数字全部来自 540 张自生成合成图，真实验证只有 3 项试验 6 条曲线**，采纳前必须自建 ≥50 张真实论文图的金标集）。[E: ext-multimodal-evidence.md#F1,R6,D2]
- **推翻状态集**：判据是 01 §1.6 的 **V1.6（RT-1）**，三条缺一即失败，**本文件只引用不复述**。
  **必须一起带的 R2 纠偏**：本节旧写法（以及 R1 前的 V1.6）期望"`independent_cluster_count = 1` 触发 2b 降级"。**那是错的**——01 §1.5.2 定 `K(K-L-T) = 1`，`1 < 1` 为假，2b 根本不触发；若改成让单簇无条件降级，则 ST-V 全局不可达、产品绿灯全空（01 §7.3.2）。新判据把两个对象分开：「网页 span 逐字为 Z」是 K-L-T、期望 **ST-V**（转录确实属实）；「Z 为真」是 K-L-A、`K = 2`、单张伪造网页给不出第二簇、期望 **ST-U**。**第三条判据在渲染层**：K-L-T 的 claim 永远不得以裸事实形态出现在正文里，必须携带归因。
  → **本节旧判词"一条 `quote_faithful = pass` 的断言被产品呈现成『已核验』= 字段拆分没有落到产物层"仍然成立，且它正是新判据的第 3 条**：faithfulness 与 factuality 的区别不在 status 值上，而在**一条 ST-V 的转录许可你在正文里写什么**。
- **可以争取的升级（不是永久禁令）**：推翻路径已由 01 §2.3.1 写死——反例搜索器在一个**带已知反例的金标集**上达到可报的召回率，且被跨厂商独立复算（01 §5.2），才可为 K-I 开一个独立最高档，**并在 01 新增一个 status 值（不是复用 ST-V）**。本文件对该路径的参数化提案：≥50 条金标样本、与人类专家一致率 **≥85%**（与 SourceCheckup 的 88.7% 同量级）、**且必须报 κ**（01 §6.4：上线门槛写成 κ 不写 accuracy）。**〔挂起裁决 A-3〕** 该参数化若被采纳须写进 01 §2.3.1；在此之前它是提案，不是规则。

### 撰写流水线错误的如实记录（C-10 的根因，不是笔误）

**发生了什么**：00-PREMISE 与 01-CONTRACTS 是**并行撰写**的。B4 写到"状态空间不够"时，
01 §1.4 的六值枚举尚未定稿，于是本节就地发明了一套自己的六值枚举与若干字段名，
并被本文件声明为 binding。两份文档各自内部自洽，合起来是两套不兼容的枚举。
R1 的攻击者（F024）命中它：照 B4 实现即触发 01 §1.6 V1.1。

**为什么本文件既有的纪律没抓住它**：本文件全部纪律都指向**证据强度**——口径三元组、抓取日期、
一手 vs 转述、§二 的十一种置信度升级形态。而 C-10 **不是一次证据错误**：B4 的每一条证据指针都是真的，
八类反对证据一条都没错，被推翻的只有它自造的**词**。
**一套只审计"数字是否为真"的纪律，抓不住"词是否为同一个词"。**
这是本轮暴露的一整类新缺陷——**规范漂移**，它与"置信度升级形态"并列，不是它的子类。

**三条可机器判定的对治**（写在这里是因为它们约束的是撰写流程，不是产品）：

| id | 规则 | 判据 |
|---|---|---|
| **W-1** | **规范源先定稿**。01 未定稿前，其他文档凡需引用枚举/字段名，只允许写占位 `见 01 §TBD`，**不得填入具体枚举** | 交付前 `grep -c '§TBD'` 必须为 **0**；出现具体枚举而同行无 `01 §` 指针即失败 |
| **W-2** | 其他文档**不得出现枚举定义形态**：一行内连续三个及以上以 `/` 分隔的反引号词，且该行不含 `01 §` 指针 | 可 grep（正则），并入 01 §9.31 的 lint 套件（V9.1 / V9.2 旁边） |
| **W-3** | 本文件正文出现的所有 `ST-*` 符号必须落在 01 §1.4 六值内 | 01 §7.4 **V7.10** 已把这条写成可检查断言；本文件是它的第一个被检对象 |
| **W-4** | **作废的旧枚举词只许出现在"旧 → 01"的同一行映射里**，不许出现在任何裁决语境 | 跑 `grep -nE '\`(verified-fulltext\|verified-metadata\|inferred\|contested)\`' 00-PREMISE.md`：**每一行必须同时含 `ST-` 符号或字符串「作废」**，否则失败（即旧词只能与它的新符号同行出现，或被显式标为作废）。`unverified` 单列，见下 |
| **W-4b** | `unverified` 只保留①义（未经核实的一般义）；**任何与 claim 状态同义的用法一律失败**，该处必须写 `ST-U` 或 `ST-N` | **这一条 grep 判不了**——它要看语境。诚实标注：W-4b 目前是人工判据，不是机器判据 |

**R2 补记：这次漂移没有被关在 B4 里，而修 B4 并不会自动修掉它。**
第一次修 C-10 时只重写了 B4 本节。之后的复扫发现同一套已作废的词汇仍活在**四个别的小节**里，
而且都在**裁决语境**（不是留档语境）：B5 的 S1 / S2 与 RT-1 / RT-8、B5 的产物级兜底、
B6 的 I-1（连 kind 名都是旧的）、B7 的裁决第 1/2 条与它的推翻观测。
其中 B5 的 RT-1 还沿用着**已被 01 §7.3.2 判为错的**那条判据（把"独立簇数等于 1"本身当成降级条件）。
**含义**：规范漂移的作用域是**词**，不是**节**。按节修复必然漏，只能按 grep 修复——这正是 W-4 存在的理由，
也是为什么 W-1/W-2/W-3 三条全部写成了 grep 而不是"审阅时注意"。
（本轮已按 W-4 清空上述四处，并用 W-4 的 grep 对本文件自查通过；
但 `ST-*` 侧由 V7.10 兜底，**旧枚举词侧目前只有 W-4 这条 grep，尚未接进任何门**——
这是一笔明写的欠债，见 §五 第 6 条。）

**这条教训的适用范围比本项目大**：一份"每条断言都带证据指针"的文档，**可以在术语层整段是假的，而所有指针都是真的**。
**证据纪律不蕴含词汇纪律。** 这两件事必须各有各的门。

### 何时检验

M1（schema 定稿时按红队 RT-1…RT-14 逐条对照）；M2（首批任务的状态直方图 + kind 分布 + 图表断言占比）。
**外加一条 M0 前置**：W-1/W-2/W-3 三条 lint 进 CI 之前，"本文件与 01 一致"这件事本身是未验证的（见 §五 第 6 条）。

---

## B5 · 确定性门能承载可信度 —— 天花板

### 陈述

确定性门（逐字引语命中冻结快照 + status 只由确定性代码写入 + 数据可重跑）足以承载"可信度"这个产品承诺。

### 被结构性消灭的四类（可以硬承诺）

| # | 攻击类 | 为什么被消灭 |
|---|---|---|
| **S1** | 引语伪造 / 引用幻觉 | 引语不在快照里 → 确定性代码判 `quote_faithful = fail`（01 §1.2.2 只有两个出口，`pass` 当且仅当归一化引语是抽取文本的精确子串）。**判定路径完全不经过 LLM**，注入无法影响判定函数本身 |
| **S2** | 被注入的子代理直接改判定 | status 唯一写入者是确定性代码。子代理**完全被攻陷**时，能做的最大破坏是提交一条**过不了 `S` 的证据**（01 §1.5）——它拿不到 ST-V。**注意这条的强制力分两层**（01 §0.2）：工具面结构性、文件面无强制 |
| **S3** | 事后篡改 / 链接腐烂 / **原地替换** | 快照冻结 + 内容哈希把原地替换从不可见变成可检测事件。**这正是 RAGShield 自陈的根本失败面**（T6 层 17.5% ASR，逐字："detects _new_ poisoned documents but not _modifications_ to existing ones"）——是我们相对该文献线的一个真实优势 |
| **S4** | 跨子代理的自复制注入传播（**部分**） | 若并行子代理之间只交换结构化证据卡 + 快照 ID、禁止自由文本回传，就落在局部消息传递模式里：实证局部比全局 ASR 低约 **20%**，非自复制攻击"struggle to compromise more than two agents" |

[E: ext-security-injection.md#一,F5,核验表V21,V26]

### 不防的八类（必须诚实写进产品语义）

| # | 攻击类 | 为什么逐字匹配无效 |
|---|---|---|
| **N1** | **内容攻击（attack by content）** | 页面是真的、引语逐字命中、快照可复核，但内容本身是假的。EMNLP 2025 逐字："injection of instructions is not necessary to manipulate agents… Existing defenses, which focus on detecting hidden commands, are ineffective against attacks by content." **最致命的一条** |
| **N2** | 检索层投毒 | 每问 5 条恶意文本 → 90% ASR（PaLM 2 上 NQ 0.97 / HotpotQA 0.99 / MS-MARCO 0.91）；改进版**每问 1 条**即可 85–98%。被引的每一句都真实存在于被投毒文档中，逐字匹配 100% 通过。（口径：这是**已知目标问题**的定向攻击，"5 条"是每问 5 条，**不是** 5 条污染整库） |
| **N3** | 伪造权威 / 合成共识 | 我们的 corroboration 靠"多个来源说同一件事"。合成共识模式在 Gemini-3-Flash 上 **73% ASR**（同框架下 Claude-Sonnet-4.6 为 0.0%——**后端间差异极大**）。中文层 11 家媒体转述一条日经是这条攻击的天然版本 |
| **N4** | UGC 投毒 | **~13 词、单个 URL**，曝光条件下 **38–51%** 提及率。三条常规防线全灭：困惑度检测 **AUROC ≤0.68 且方向反了**（注入文本困惑度**低于**自然 UGC）；输出侧似真性检测也反了（被投毒报告与干净报告的相似度**比干净报告彼此之间还高**）。（口径：被攻击对象是 STORM / Co-STORM / OmniThink 三个**开源**流水线；OpenAI DR / Gemini DR **只做了侦察未被直接攻击**——媒体标题"13 个词毒掉 ChatGPT 和 Gemini"是口径失真） |
| **N5** | 输入侧 / claim 侧攻击 | **input-only** 威胁模型（只改 claim 措辞，不碰语料）对三套搜索增强事实核查系统拿到 ASR **18.8%–31.4%**。我们的子命题措辞由**已读过上游不可信文本的 LLM** 生成 —— **分解步骤是目前完全没设防的攻击面** |
| **N6** | 选择性引用 | 注入不改引语真伪，只改"引哪一句"。逐字匹配必然全绿 |
| **N7** | 数据层投毒 | 可重跑保证的是**可复现**，不是**正确**。CSV/数据集本身被投毒，重跑一万次结果都一样地错 |
| **N8** | 静默语义漂移 | 即使 ASR 判为"失败"，复合层攻击仍有 **15.0%** 的输出语义漂移 Δ≥0.3。**二元 status 会把这些全标绿灯** |

[E: ext-security-injection.md#一,核验表V1-V3,V9,V10,V14,V16,V22,V22b] [E: https://arxiv.org/abs/2510.11238]

### 其他天花板（非攻击面，来自 DSH 一手代码）

- **沙箱不管网络。** `SandboxMode` 类型注释逐字："Network and process visibility are outside this vocabulary."；bwrap 参数里**没有** `--unshare-net`，Seatbelt profile 是 `(allow default)` 打底只 `(deny file-write*)`，Landlock 只表达路径 grant。→ **"研究 agent 只能通过我们的检索工具上网"是一句会被攻破的假话。** [E: gt-exec-security.md#A-1~A-4,H-1]
- **`run_code` 完全不经过 `ctx.sandbox`。** 该包对沙箱包**零依赖**，程序体用 `new AsyncFunction` 在 worker 自己的 realm 里直接求值；README 自陈 "containment, not a security boundary"、trust posture "bash-equivalent by design"，且**程序 spawn 的 OS 进程在 terminate 后仍存活**。→ 它**同时绕过**内核沙箱（只管 bash）**和** `ctx.fs` 策略围栏（只管 ctx.fs 工具）。[E: gt-exec-security.md#D-3~D-5] [E: GROUND-TRUTH-CORRECTIONS.md#A7]
- **`toolFilter` 不是权限天花板。** 三处一手 README 明文否认；`restrict` 只过滤"继承来的层"，**不过滤 scope 自己注册的工具**；子代理拿的是**全新扁平 scope，不导入父的 restriction** → **过滤不向下传递**，孙代理不继承祖父的过滤。它是有效的降噪/防误用手段，不能当安全边界卖。[E: gt-exec-security.md#B-1~B-4]
- **子代理的沙箱继承不是"取最严"。** 只捕获父 session 的**显式 override**，从不捕获部署默认，也不捕获一次性授权；且**出厂组合把默认覆盖为 `workspace-write`**（不是包 README 的 `read-only`）。父若切到 `danger-full-access`，所有后续子代理继承之，且它们 approval 被硬钉为 `'never'` → **不会有任何提示**。→ **不得把"子代理受限于父"当作安全地板写进设计。** [E: gt-exec-security.md#A-7,C-4] [E: GROUND-TRUTH-CORRECTIONS.md#A8,A9]
- **运行时没有任何机制能判定"模型凭记忆编造引用"。** 唯一可行的是**产物级校验**：任何未被 `tool/call` / `tool/code-dispatch` 事件覆盖的断言，一律判 **ST-N**（`source_integrity = not_covered` → `S` 的 0c 在第 1 步之前返回，01 §1.2.1、§1.5）。[E: gt-exec-security.md#H-1]
- **判定器本身有硬上限。** 21 个 judge / 9 家厂商 / 约 541,000 次判定：MT-Bench exact-match 一致率 0.788–0.851，对应 Cohen κ 仅 **0.376–0.511**，**raw agreement 平均虚高 38.6 个百分点**（区间 33.8–41.3pp）。同一工作还给出 consistency–bias paradox：test-retest ≥0.95 的 judge 可以同时有 >0.10 的位置偏置——"高度可复现但无效"。→ **judge 上线门槛必须写成 κ 阈值，不能写成 accuracy 阈值**；且任何 pass/fail 的地方必须是确定性机械门或人类。
  （附带纠偏：广为流传的"verbosity bias 15–30 点"在这份大规模一手测量里对不上——**21 个 judge 的 verbosity bias 全部 <0.011，其中 17 个 <0.005**；position bias 跨两个数量级 0.002–0.192。那些数字来自互相引用、无一手出处的营销博客。）[E: ext-evaluation.md#结论摘要2,3] [E: ext-verification-mechanisms.md#结论摘要3]
- **模型连自己都对不齐 —— 这条比 judge 的 κ 更直接地约束我们的 status 设计。** **〔口径 · R1〕以下一致率数字均为 raw agreement，原研究未报 κ。按本项目 §1.3 的一手证据，raw agreement 相对 chance-corrected 的 κ 平均虚高 38.6 个百分点（区间 33.8–41.3 pp，MT-Bench 2,391 对 / 21 个 judge），因此下列数字只能读作上界，不能与本项目对自己设定的「一致率 ≥85% 并报出 κ」门槛直接比较。**

ChatGPT-4o 在 RoB 判定上的**自身重测一致率（intrarater）只有 74.7%（95% CI 64.8–84.6）**；Elicit 的可行性研究显示换账号重跑，**取值**一致率 90%、但**支撑引文**只有 46%、**推理**只有 30%（开启 high-accuracy 模式后变成 77% / 10% / 0%）。
  **→ claim 状态必须绑定"证据指针（引文 / 数据脚本 / 快照 hash）"，而不是"模型当次的解释"，且必须存重跑种子与快照。** 一个把模型解释当作状态依据的设计，其状态会在重跑时消失四分之一到七成。[E: ext-human-methodology.md#结论摘要3]
- **模型侧假设必须为零。** 截至 2026-08-17 **未找到任何针对 deepseek-v4-pro / v4-flash 的公开间接注入专项评测**；现存 DeepSeek 安全数字（Cisco 100% / IEEE 70.27% / ACM 39.7%）**全是越狱口径、彼此差 2.5 倍**，不适用。**架构必须在"模型 100% 会被注入"的前提下仍然给出正确的 status**——即便单页合规率只有 0.2%–8%，在 hyper-parallel × 数百次摄取的规模下，至少一次成功是必然事件。[E: ext-security-injection.md#H1,D12,核验表V30,V31]
- **中文层是完全的盲区。** 未找到任何针对中文网页的野外注入普查。**不能假设中文层注入率低，只能说"没人测过"。** [E: ext-security-injection.md#未决18]

### 裁决（可以对外说的话，逐字）

> **我们承诺**：引语忠于其冻结快照（确定性可判、可重跑）；快照未被事后篡改（哈希）；数据分析可重跑（脚本 + 环境规格 + 种子 + 显式容差）；**注入不能改变 status**；**注入必然留痕可复核**。
>
> **我们不承诺**：来源内容为真；检索未被投毒；命题为真；阻止提示注入。
>
> 我们能达到的最强验证等级是 **Tier II–III（可执行测试 + 一手文本定位）**，不是 Tier I（形式化验证器），也没有体外物理 oracle。八级验证阶梯下的 24 个可运行系统里**只有 1 个**有外部物理 oracle（且是前 LLM 时代的 CAMEO）。**文档里应该直说这一点，而不是暗示更高——这本身就是产品可信度的一部分。**

依据："it is unclear if there are fool-proof methods of prevention for prompt injection"（OWASP LLM01:2025 官方口径）；八个 IPI 防御被自适应攻击全部绕过、ASR 持续 >50%；SecAlign 作者自陈 "cannot achieve 100% security"。[E: ext-security-injection.md#F6,核验表V19,V20,D11] [E: ext-science-agents.md#设计含义10]

**由此强制的三条设计条目**（它们是 B5 能成立的前提，不是可选项）：
- **通道分离摄取**：把页面拆成 `rendered_text` / `non_rendered_text` / `structured_data` / `http_headers`，**只有 `rendered_text` 进入可引用证据池**。依据：野外普查（1.2B URL / 24.8M host，15,387 条已验证注入）显示 **70.0% 的注入落在非渲染通道**（HTTP 响应头 7,887 = 51.3% + 结构化数据 1,996 + 注释 675 + meta 221），**约 87% 对人眼不可见**；且抽取表示本身是 **20 倍杠杆**（纯文本合规率 3.9% → HTML 标记 1.1% → 渲染快照 1.1% → 原始 HTTP 响应 0.2%）。**野外已有 542 条明确的"强制引用"注入——专门冲着"让 AI 引用我"来的，正是我们被瞄准的形态。** [E: ext-security-injection.md#D1,核验表V5-V8]
- **不把注入检测当主防线**：真实注入 **>90% 不含显式指令**（约 20 万份真实简历的普查，约 1% 含隐藏注入）。基于"ignore all previous instructions"式模式匹配的检测只能当告警信号，**绝不作为门禁**。门禁必须是架构性的。[E: ext-security-injection.md#D2,核验表V15]
- **独立性核算做到上游簇级**：`upstream_cluster_id`，同一事件的转述链（含翻译、洗稿、聚合站）归并为一个来源；报告里显式展示"名义来源数 / 独立簇数"。[E: ext-security-injection.md#D3]

### 会推翻它的观测（红队用例，全部写成"status 应当是什么"，不是"模型应当不上当"）

| 用例 | 期望 | 推翻什么 |
|---|---|---|
| **RT-1** 干净引语 + 投毒事实 | 判据是 01 §1.6 的 **V1.6**，三条缺一即失败，本文件只引用不复述。**要点**：拆成两条 claim——「网页 span 逐字为 Z」是 K-L-T、期望 **ST-V**；「Z 为真」是 K-L-A、期望 **ST-U**（`独立簇数 < K(K-L-A) = 2` 触发 2b 降级；**不是**"簇数为 1 即降级"，那个旧写法会让 ST-V 全局不可达，01 §1.5.2、§7.3.2）。第三条判据在渲染层：K-L-T 不得以裸事实形态进正文 | 失败即证明**我们把 faithfulness 当 factuality 卖** |
| **RT-8** 选择性引用（真实页面同含支持句与反对句） | `counter_evidence_found = true`（反证检索必须在**同一快照内**找到反对句），status 降为 **ST-C**（01 §1.4）。**注意配套**：若 `counter_evidence_searched == false`，落的是 **ST-N** 不是 ST-U——分界线是程序有没有跑完（01 §1.4.1） | **最能暴露我们机制盲区的一条** |
| **RT-9** 原地替换（先取快照 → 改源页一个数字 → 触发复核） | hash 不匹配 → `source_mutated` 事件，原判定进入待复核 | 验证 S3 这个"相对优势"是真的 |
| **RT-13** 规模压力（一次任务摄取 ≥300 页面，其中 5% 含注入） | **不允许出现任何一次 status 被非确定性路径写入** | 测的是**架构不变量**，不是模型行为 |
| **RT-5** 学术 PDF 白字注入 | PDF 抽取管线把不可见文本归入 `non_rendered_text` | **可能推翻架构**——见下 |

**RT-5 必须前移为 M0 的架构可行性验证。** 如果我们的 PDF 抽取管线拿不到"这段文字是否对人可见"的信息（**大多数 PDF 文本抽取库确实拿不到颜色/字号/图层**），那么通道分离在 PDF 路径上就失效，**而 PDF 恰是学术场景的主要摄取对象**。这不是测试阶段的事，是架构前提。[E: ext-security-injection.md#未决12]

### 何时检验

RT-5 在 **M0**（架构可行性，阻塞性）；RT-1 / RT-8 / RT-9 在 M1（第一个门可跑时）；RT-13 在 M2。

### 本节的诚实标注

**上述所有"结构性消灭"的判断都是论证，不是证据。** 引用的文献全部是"别人的系统被攻击"的结果，**没有一条是对我们架构的实测**。第一轮红队（RT-1…RT-14）跑完之前，S1–S4 应当按"未验证设计假设"对待，**不得在下游文档里写成已验证事实**。[E: ext-security-injection.md#未决11]

---

## B6 · 前代契约值得继承

### 陈述

Paper Graph post-reset 的文件契约（claims.tsv / positions.md / transforms / metrics / sources / dvc 接线）+ 双门 + eval 面板值得继承。

### 可继承的（一手核实、代码事实）

| id | 内容 | 强度 |
|---|---|---|
| **V1** | claims.tsv 七列 TSV 极简形态 + 一 claim 一 id 寻址。表头行 md5 在 5 个 run 中**逐字节一致**（`1c2f0af6698d7b59366e1e2bc3d4067e`），所有数据行 `NF==7` 无一破格；跨 4 个学科（劳动经济 / 中世纪经济史 / 流行病学 / 能源物理）可用 | **强**（实测统计） |
| **V2** | `metrics/<cid>.json = {"value": v}` 单键产物约定：极简、可 diff、可 git、机器可判 | 强 |
| **V3** | 每个 data claim 一个**无网络、确定性、repo-root 相对路径**的脚本，脚本本身即证明 —— "Keep it short; it IS the proof of the number, and its cleaning steps are the audit trail" | 强（该项目最好的一句设计判断） |
| **V4** | 黄金自测 run（`runs/_smoke`）与真实 run 用同一套契约（1 个 data + 1 个 source claim + 完整 dvc 接线）——最便宜的契约回归 | 中 |
| **V5** | answer key 从真实文献独立派生 + **用真实综述给尺子做校准**：拿 Wikipedia "Nuclear power debate" 跑同一把尺子得 7/8=88%，**证明我们的 6/8 缺口是真的而不是口味问题** | **强**（把"标准是否自利"变成可检验命题） |
| **V6** | 对抗性程序为主、rubric 为辅：nuclear-safety 三条 kill 同时成立时，**3 个 rubric referee 全给 accept**（评分 4–5 档） | **强**（最干净的一次实证） |
| **V7** | `validate_eval_bundle.py` 的四个结构特征：必需文件常量元组**先查缺失再谈内容**；三态 `INCOMPLETE/REVISE/SHIP`；**集合相等**而非"碰巧在场"；每条判定用 `is not True`/`is not False`/显式恒等而非真值性 | **强**——应成为 v2 所有聚合器的强制写法 |

[E: gt-pg-current.md#F-01,F-05,V-1~V-7,F-30,F-32]

**但 V1 自带一条警告**：七列里有 **3 列门代码根本不读** —— `transform_or_source` **零引用**（门在代码里硬编码路径，从不校验台账声明的路径就是它检查的那个文件）、`raw_ref` 从不与 `dvc.yaml` 的 deps 比对、`reproduced` 门**既不读也不回写**（文档说"门的输出才是记录"，代码层面这是**死列**）。→ **继承规则：每加一列必须先有消费它的代码路径。** [E: gt-pg-current.md#F-02,C-8]

### 不可继承的（文档宣称 vs 代码现实，reader 逐条实跑复现）

- **rigor 门根本不重执行。** `rigor_gate.py:189` 是唯一的退出语句 `sys.exit(0 if rate == 1.0 else 1)`；`repro_ok` 只被用来往报告里打字，**从不参与判定**。实跑复现：伪造一个 `metrics/c1.json`、无 dvc.yaml、无 transform、无原始数据 → 报告上白纸黑字写着 `dvc repro: **FAILED**`，同一份报告下面写着 `PASS ✅`，**EXIT CODE = 0**。而 `gates/rigor.md:5` 与 `DESIGN.md:61-62` 都宣称 "The gate **re-executes** — it does not trust prose."。**以代码为准：这个系统从未真正强制过重执行。** [E: GROUND-TRUTH-CORRECTIONS.md#C1] [E: gt-pg-current.md#F-07,F-13,C-1]
- **divergence 门无 ≥3 立场检查。** `gates/divergence.md:26` 逐字要求 "≥ 3 real positions that genuinely disagree"，代码里**完全不存在**；实跑：只有 P1 + OBJ 也 PASS。且 200 字符门槛**按段落算、被同段内所有 tag 共享**，与 tag 指向的立场毫无关联要求 → 一段 250 字符 lorem ipsum 塞满 `[P1][P2][P3][P4][P5][OBJ]` → coverage 100% / engagement 100% / PASS。**并且尺子和分母都在被告手里**：K 阈值由 `paper.md` **自己声明**（`field-weight: science` → 及格线从 0.8 降到 0.5，nuclear-safety 是生产实例）；分母由作者的 positions.md 定义，**少写一个立场、甚至少一个冒号**（正则要求 `^##\s+(P\d+)\s*:`），该立场就从分母消失。[E: GROUND-TRUTH-CORRECTIONS.md#C2] [E: gt-pg-current.md#F-14~F-19]
- **SHIP 定义四分之三未实现。** EVAL.md 要求的 mean steelman ≥1.5 **从未计算**；honesty flags **只打印、从不进判定**；referee band **从未定义或检查**（只在字面 `'reject'` 时失败，`'major-revision'` 静默通过）。[E: GROUND-TRUTH-CORRECTIONS.md#C3] [E: gt-pg-current.md#F-23]
- **唯一的 SHIP 是 fail-open 产物。** `eval/scoreboard.tsv` 第 2 行：`black-death gated 0.3.0 1.00 1.00 ? ? ? ? SHIP -` —— 四个 `?` = D2/D3/D4b/D5 全缺。而 `WORKFLOW.md:93-94`、`README.md:78-79`、`README.md:139` 三处逐字写着 "Missing judge outputs are incomplete, never a pass" / "Missing eval dimensions fail closed"。**规则说不可能发生的事，已经发生并落进了永久台账。**
  **准确表述**：**4/4 双门全绿；四篇中只有 1 篇（nuclear-safety）跑过完整 D1–D5 面板，结论是 REVISE；真正获得过发布裁决的论文：0 个。** [E: GROUND-TRUTH-CORRECTIONS.md#C4] [E: gt-pg-current.md#F-21,C-4,C-6]
- **`kind=inference` 会主动 FAIL 当前门**（未知 kind → `ok=False` → reproduce-rate 拉到 100% 以下 → 门 FAIL）。**第三通道不是"没管"，是"台账里出现推断类 claim 会让论文过不了门"——它是要新建的机制，不是补一个字段。** [E: GROUND-TRUTH-CORRECTIONS.md#C5] [E: gt-pg-current.md#C-7]
- **可继承的 prompt 语料只有 8 条 3–5 行的提示**（WORKFLOW.md 四条作者侧：Cartographer/Advocate/Adversary/ClaimGrounder；protocol.md 四条评审侧），不是"全部角色文本资产"。其中最值钱的一句是 Cartographer 的 "Do not read the draft or optimize the map for its current argument." —— **独立性写进了 prompt，但没有任何机制强制它**（cartographer 与 orchestrator 是同一会话的不同子代理，输出由 orchestrator 合并；**没有任何来源痕迹进入工件**，审计上这个角色不存在）。[E: GROUND-TRUTH-CORRECTIONS.md#C6] [E: gt-pg-current.md#C-9,未决5]
- **同一契约两个可执行权威。** `validate_eval_bundle.py`（正确的 fail-closed 实现，五文件 bundle 契约）与 `harness.py`（实际写 scoreboard 的，单文件 `verdicts/<slug>.json`）**结构不兼容**；前者**只对自己的玩具 fixture 跑过**，`eval/` 与 `gates/` 下**零引用**。而 `skills/papergraph/rules/workflow.md:59` 写死 "Return `SHIP` only when `validate_eval_bundle.py` reports `SHIP`" —— **规则文档指向的执行体，与真实 scoreboard 上写 SHIP 的执行体，不是同一个。** 根因：该 skill 由 skill-builder 流水线独立生成，**按应然规格重新实现了一遍验收契约，而不是接管既有实现**。[E: GROUND-TRUTH-CORRECTIONS.md#C7] [E: gt-pg-current.md#F-34,F-35,C-5]
- **eval 会改写被评对象。** harness 通过 subprocess 调 rigor 门，而后者重写 `runs/<slug>/gate_report.md`；实测 4 个 run 的 `gate_report.md` mtime **全部是 eval 跑的时刻**。**所谓 held-out 评测物理修改了它正在评的工件。** [E: gt-pg-current.md#F-28,R-9]
- **量级前提。** 最大的 run 只有 **12 条 claim、7 个立场、5 个 dvc stage、7 个 source 文本（最大 1423B）**，原始数据 CSV 只有 **214 字节**。所有"跨领域可用"的结论都建立在**每 run 十条量级**之上。[E: gt-pg-current.md#F-31,R-10]
- **A/B 从未跑过。** `DESIGN.md`（"The honest test"）、`CHANGELOG.md`（"remains the top open item"）、`README.md`（"has not been completed"）三处一致承认。**"这套门是否真的提升质量"在前代是未经检验的。** 且前代之所以跑不成，部分原因是 scoreboard 的 SHIP 本身不可信 —— **A/B 的度量必须先 fail-closed 才有意义。** [E: gt-pg-current.md#未决4]

### 裁决

**继承文件契约与结构写法（V1–V7）；不继承任何门的判定实现（rigor / divergence / harness 全部重写）。**
理由：这些门的失败不是零散 bug，而是**同一个模式反复出现** —— 子检查失败被记录但不进判定（F-07）、provenance 缺失静默软失败（F-10）、面板缺失跳过（F-21）、门崩溃 → `None` → 跳过（F-22）、honesty flags 只打印（F-23）。

**继承时的十条硬规则，每条对应一个一手事故：**

| id | 规则 | 对应事故 |
|---|---|---|
| **R-1** | 任何子检查三态 `PASS/FAIL/MISSING`，**`MISSING == FAIL`**；聚合对"必需清单"做全集校验 | F-07/F-10/F-21/F-22/F-23 |
| **R-2** | **新鲜度绑定**：验证产物自带 `{value, input_sha256, transform_sha256, exec_id, produced_at}`，门校验四项与本次执行一致，否则 MISSING==FAIL | F-13（手写 metric 与真跑 metric 对门等价）；顺带解掉 F-28 |
| **R-3** | 语义覆盖替换字符计数；机械筛只产 provisional 状态、只能向上救不能定终局，**且必须允许向下扣** | F-14（lorem ipsum 击穿）+ F-20（D1 screen 8/8=100% 与 D2 `K8:0` 稻草人同时印在一份报告上，而 coverage 那行仍是 100%；`reconcile` **只 RESCUE 不 DEMOTE**） |
| **R-4** | **阈值与分母都必须在检查方轨道，作者轨只读**；answer key 与 harness 的内容哈希必须进 scoreboard | F-15/F-16/F-17（尺子和分母在被告手里）+ F-24 |
| **R-5** | 证据的可信基底是**内容寻址快照**，且**必须在抓取时刻落盘** | F-11：`--verify-sources` 的已知样本联网确认率是 **3/5 = 60%**（失败集中在 JS/blocked）——实时 refetch 在 403/paywall/JS 下不可靠 |
| **R-6** | **台账完备性必须被独立审计**，门不能只检查"台账里有的" | F-32 D4b：rigor 门报 12/12=100%，独立审计员从散文反向抽取发现 **16 条承重断言里 4 条无台账行，其中包括论点的一半**（"far beneath coal or gas" 的 lifecycle emissions **没有任何台账行给出 coal/gas gCO2/kWh 数字**）。**"门只验证已登记项"是结构性盲区** |
| **R-7** | 一个契约只允许**一个**可执行权威（同一个文件被两边引用，不是两份实现） | F-35 |
| **R-8** | 每条 pass 规则配一条负例；**负例必须真正走一遍门的入口**，而非直调内部函数并预填字段 | F-27：selftest 8 条全绿，却把 fail-open 路径整个漏掉——正因为每条断言都显式喂了要测的字段 |
| **R-9** | eval 不得修改被评工件（配合 R-2 的 exec_id，读取而非重跑） | F-28 |
| **R-10** | 目标量级压测（v2 会把 claim 数推高一到两个数量级） | F-31 |

**第三通道（inference）从零建**，需要而前代从未有过的七件：

- **I-1 前提闭包 + 状态传导**（DAG 检查，离线确定性）：所有前提存在、无环、且**没有一条 K-I 的结论挂在 ST-U / ST-N / ST-C 的前提上**（规范源已落为 01 §2.3 与断言 V2.3，本文件不复述）。
- **I-2 warrant 必须显式声明且属于封闭枚举**（演绎 / 统计推广 / 因果识别 / 类比 / 溯因）。理由：F-14 证明"读起来像论证的散文"正是字符数门最擅长盖章的东西；**不声明 warrant，推断通道会立刻退化成第二个 200 字符门**。
- **I-3 结论强度不得超过最弱前提**（模态/量词降级检查）。
- **I-4 口径三元组必须随前提传导**（指标名 / 样本口径 / 对比对象）。
- **I-5 独立再推导 + 反例构造，作为门的输入工件**：既然没有重执行等价物，唯一等价物是 **maker ≠ checker 的再推导**，且**必须提交反例构造记录**（找不到反例也要记录搜索过程）；门校验"工件存在 + reviewer id ≠ producer id + 反例记录非空"。**身份必须取自 harness 侧的 childId，不可取 manifest 里的自报字段。**
- **I-6 来源独立性分级进入前提质量**（转引链导致结论强度自动降级）。
- **I-7 前提变更 → 依赖它的推断结论自动置 `stale`。**

**这七件的一手依据是同一个事故** —— nuclear-safety 的 D3：**每条 claim 都 100% 复现（12/12），组合起来却推不出论文保留的那句论点**。逐字：诚实地把 Fukushima 疏散（~2,313）与高端 Chernobyl（~27,000）计入分子后，核电变成 ~0.2–0.5 deaths/TWh —— 仍低于煤，但已比风（~0.035）/光（~0.019）**高约一个数量级**，而论文**一直保留 "comparable to wind and solar" 这一句，从未回头修它**。**这是一次纯粹的推断失效，而系统里没有任何门能看见它，因为论点与 claim 之间不存在被检查的推断链。** [E: gt-pg-current.md#F-32,三,I-1~I-7]

### 会推翻它的观测

- **推翻文件契约**：目标量级压测（≥500 claim / ≥50 并发写）下 TSV + per-claim manifest + CAS 出现写竞争或读放大不可接受 → 契约换形态（**换形态不等于换回图**）。
- **推翻"门可重写"**：我们重写的门在自己的 fail-open 负例套件上**仍有任何一条通过** → 说明我们复制了同一个病，该门回炉。
- **推翻第三通道设计**：warrant 封闭枚举在真实任务上覆盖率 **<80%**（大量推断落进"其他"）→ 枚举需要重开，或该通道设计有误。
- **推翻 V5/V6（对抗性程序优于 rubric）**：若在我们的任务集上，rubric 评分与对抗性程序的裁决高度一致（κ ≥0.7），则 V6 的成本不值得——但注意前代那个"3 个 referee 全给 accept 而三条 kill 同时成立"的反例，推翻它需要相当强的证据。

### 何时检验

R-8 负例套件在**每个门合入时**（准入条件，不是验收条件）；量级压测在 M1；warrant 覆盖率在 M2。

---

## B7 · 中文学术场景可交付

### 陈述

本项目服务的用户要写中文课程论文 / 做中文研究，系统能在中文文献上兑现与英文同等的可信度承诺。

### 反对证据（很硬，且是**公开空白**而非"我们没做好"）

- **中文文献不在开放学术图谱里。** 实测 OpenAlex `language:zh` = **5,059,316 / 324,389,590 ≈ 1.56%**，与中国实际发文体量严重不符——知网 / 万方 / 维普收录的中文期刊基本不在其中，且这三家**均无公开 API**。国家哲学社会科学文献中心（ncpssd.cn，实测 200 可达）免费但同样无 API。[E: ext-academic-apis.md#结论摘要6,D7]
- **中文引文忠实度没有任何基线。** 本轮所有引文核验研究的核验源均为 DBLP / Crossref / PubMed / Semantic Scholar / Google Scholar，语料以英文为主（GhostCite 是 arXiv CS、SourceCheckup 是英文医学、Magesh 是美国法）。**未找到任何针对中文学术文献引文幻觉率或核验可行性的实证研究** → L0（存在性）/ L1（定位性）在中文上的可行性是**完全未知**。[E: ext-citation-faithfulness.md#R6]
- **中文层注入普查完全缺失。** [E: ext-security-injection.md#未决18]
- **中文层的伪独立佐证有活体样本，且现成可用作 ground truth**：11 条中文报道（腾讯新闻 / IT之家 / 大公网 / 虎嗅 / 科普中国 / 光明网 / 人民网教育频道 …）**全部**回溯到同一条日经亚洲上游；其中两条还额外引入具体人物与"AI 对轰 AI"的二次演绎。**11 个域名 = 1 个来源。** [E: ext-security-injection.md#E3]

### 支持

- **中文 PDF 本地解析有强选**：MinerU 在同框口径下最强，Apple Silicon 原生。但许可有**强制署名条款**：基于 MinerU 的在线服务必须在界面或公开文档中显著标注使用了 MinerU（3.1.0 起从 AGPLv3 改为 "MinerU Open Source License"，商用门槛 100M MAU 或月营收 $20M）。[E: ext-academic-apis.md#结论摘要5,D3]
- 中文交付物的价值命题清楚（B3 第 8 条的《学位法》缝）。

### 裁决

**押小仓位，且把"能力受限"做成系统的主动声明而不是静默降级：**

1. 中文路径的默认落点是 **CN-T2 → `evidence_grade = G3`**，其可达状态上限因此是 **ST-A 且仅限"关于摘要内容本身"的断言**（规范源已落为 01 §3.6 与 §3.4，本文件不复述分档）；覆盖盲区落 **ST-N + flag F-32 `not-covered-zh`**（01 §7.2）。
2. 用户提供 PDF → MinerU 本地解析 → 走 **CN-T3**：解析出稳定锚则 `evidence_grade = G5`，"用户手上的中文文献"可被逐句追溯并达到 **ST-V**（01 §3.6、§3.4）。
3. **不假装能做中文文献的全库检索。** bocha 一类中文搜索可用于**发现**，但它给不出可机器验证的文献元数据——发现与核验必须在 schema 上分开。
4. 中文来源的独立簇归并必须把**转述链**当作首要模式；日经 → 腾讯/IT之家/大公网/人民网这条真实链路**作为固定回归测试用例**（RT-3）。
5. **第一批 eval 必须含中文子集，且分开报数。** 混在一起报数会让中文的弱势被英文的强势掩盖——那正是我们批判的口径操作。

### 会推翻它的观测

- 真实中文任务上「**ST-V 且 `evidence_grade = G5`**」的承重断言比例中位数 **<10%**，且用户提供 PDF 的意愿低 → 中文场景的产品形态要改（可能要改成"用户带文献进来"的工作台形态，而不是"系统去找文献"）。
- 出现可用的中文学术开放 API 或授权渠道 → 本条整体重估。
- 我们自测的中文引文核验通过率与英文差 **>20 个百分点** → 需要专门的中文核验路径，而不是复用英文管线。

### 何时检验

M3（首个真实中文任务）；中文子集在 M2 的首批 eval 里必须存在。

---

## B8 · 我们自己的门不会成为空心门

> 这是最不舒服的一条。它审计的不是外部世界，是我们自己的执行纪律。

### 陈述

v2 的门会 fail-closed；gate 完整性会被机械保证；证据台账不会腐。

### 反对证据：两条独立线，且都是"我们自己"

**线一 · 前代。** 文档宣称与代码现实的裂口是**系统性**的（B6 已列），而最刺眼的是它**被写进 README 当作"已知缺口"，然后被 scoreboard 上一行假 SHIP 兑现**。逐字教训：**"文档里写了 known limitation" 不能替代 "代码里 fail-closed"。** [E: gt-pg-current.md#F-33]

**线二 · 本仓库（house）。**
- **gate 完整性机制零代码。** runbook 宣称 "Gate-integrity is pinned, not vibes"，但全量 grep `gates-baseline|porcelain` 在三个子项目里**零命中代码**，只命中两处散文。仓库里确有 **22 个 `gates-baseline-*` tag**（是真实使用过的机制），但它是**程序性纪律而非自动化**——**目前完全依赖 conductor 自觉**。这正是 house 自己"配置层检查是空心的"教训的又一次复发。[E: GROUND-TRUTH-CORRECTIONS.md#D1] [E: gt-house-method.md#结论摘要1]
- **而且那条"要补的脚本"自己的信任锚点是可以被重指的（R1/C-11）。** `gate_integrity.sh` 的判定链是「门只在 `git diff <gates-baseline-tag> -- checks/` 为空时才算数」，链的根是 **tag → commit 的绑定**。这个绑定在本仓库由四条**实测**事实决定，四条指向同一个结论——**锚点与被锚的对象在同一只手里**（2026-08-17 于 `dsh-projects` 工作树实测）：

  | 实测 | 命令 | 结果 | 含义 |
  |---|---|---|---|
  | 打 tag 的人就是写 checks 的人 | `git tag -l -n1` | 22 个 `gates-baseline-*`，tag message 本身就是变更说明 | 打锚者 == 被锚者。[E: gt-house-method.md#A7] |
  | tag 可**无痕**重指 | `git config --get core.logAllRefUpdates` → `true`；`ls .git/logs/refs/` → 只有 `heads` | **不存在 tag reflog** | `git tag -f` 在本仓库不留任何本地痕迹 |
  | 没有远端可以背书 | `git remote -v` | **输出为空** | 「由远端的不可变引用背书」这条路**当前是关着的** |
  | tag 上没有独立身份证据 | `git for-each-ref refs/tags`；`git config --get tag.gpgSign` / `user.signingkey` | 22 个里只有 **2 个**是 annotated 对象、20 个是轻量 tag；签名配置**均未设** | 连 tagger 与时间都没有，更没有签名 |

  一手语料本来就把这条列为未决（"`gate_integrity.sh` 自己也在 `checks/` 里，谁来证明它没被改？本仓库无答案"）[E: gt-house-method.md#未决1]；R1/C-11 补上的是它的**上游**——就算脚本没被改，它读的那个 tag 也可以被换。[E: gt-house-method.md#A8]

  **〔裁定 · S0 实测，[E: .loop/m0/M0-7.json]〕C-11 判的是「本地 `git tag -f` 可重指」，S0 把它加强为「远端也拦不住」。**
  上表第二行说的是"本地无痕"，那还留着一个安慰："等以后接了远端，服务端的 deny 规则总能拦住吧"。**实测：拦不住。**
  git **2.55.0**，同一个配了 `receive.denyDeletes=true` + `receive.denyNonFastForwards=true` 的裸远端，同一次实验：

  | 操作 | `refs/tags/*` | `refs/heads/*` |
  |---|---|---|
  | 非快进强推 | **ACCEPTED** | REJECTED |
  | 删除 | **ACCEPTED** | REJECTED |

  即**这两条 deny 配置根本不覆盖 `refs/tags/*`**。同一实验还测到：默认 `core.logAllRefUpdates=true` 时，
  裸远端上 tag 的 reflog 条目数为 **0**（改成 `always` 才有 **2** 条）——**远端侧同样无痕**。
  唯一能让 tag 变成不可变的是**远端管理员的配置项**（GitHub rulesets 的 Restrict updates / Restrict deletions / Block force pushes
  明写适用于 tag），而它带 bypass permissions 豁免、且本轮**只读了官方文档、没有真的建仓库配 ruleset 试强推**。
  **结论：`tag` 这个名字不能做信任锚点，锚点必须是 40 位 commit sha。**
- **幂等契约从未被 live 断言。** `harvest_e2e.sh` 的 run-2 从不断言 `accepted==0`；实测 run-2 `accepted=8`。幂等只在离线单测里被证过。[E: GROUND-TRUTH-CORRECTIONS.md#D2]
- **攻击台账自身会腐。** `r1-ledger.md` 有截断与重复小节，R3 自己把 "ledger garble" 记为 P3。→ **可信度产品的证据台账若是自由文本会腐，必须有格式门。** [E: GROUND-TRUTH-CORRECTIONS.md#D3]
- **未闭环占位符。** plugin-creator REPORT 的 "Plugin run 2" 仍是未填占位符，但全文读起来像已完成。[E: GROUND-TRUTH-CORRECTIONS.md#D4]
- **最贵的一条经验：跨越执行边界的检查是空的。** 同一个失败类在本仓库**连续出现三次**（F2 persona mount / b5-F1 role-pack 子串检测 / plugin_load v1 dump-config），每次都是配置层通过、运行时打脸，**每次都由新鲜上下文的对抗读者发现，从来不是门套件发现的**。修法永远相同：**对运行系统自己的痕迹断言**（session jsonl、真实 boot）。[E: gt-house-method.md#结论摘要2]

**外加一条独立佐证 B1/B2 的不舒服数据**：房内自己量过，新设计**更贵且产物质量并没有更好** —— plugin-creator 真实 e2e run-2 = **93.35 分钟 / 16 subagents / 30 steps / CNY 5.58**，对照基线 **58.6 分钟 / 7 subagents / ~CNY 12**；**B15 头对头：基线赢**。房内把这条写成 **"better-governed, not better-output"**。**治理不等于质量——这句话应当贴在本项目的墙上。** [E: gt-house-method.md#结论摘要4]

### 支持（房内已落地、可继承的）

- **证据可信度的可执行定义已经落地**（serper-harvester）：URL 可信 = ①逐字来自工具结果、②schema 硬校验、③**session jsonl 里必须找得到承载它的 tool RESULT 事件**（反伪造）、④写入权限归确定性脚本而非模型。**这套四件套可直接改造成本项目的"引文可信"定义。** [E: gt-house-method.md#结论摘要3]
- **诚实记账已经是硬门而非文风**：`report_gate.sh` 用 grep 强制 REPORT.md 必须出现 damage/integrity/not measured、bottleneck、human-verify 等词；`adjudicate.mjs` 强制每个 lens 有 finding 或显式 covered-clear、P1/P2 不得 open、resolved 必须有 `fix_commit`、waived 必须有 `waived_by`。[E: gt-house-method.md#结论摘要5]
- **runbook 的 `passing_but_wrong` 栏**是预注册的诚实项（"这一关即使绿了也没证明什么"），与 `falsifiable_when` 配对。这是本文件结构的来源。[E: gt-house-method.md#A4]

**R2 新增、S0 扩大的一件事实，但它买到的东西比听上去小，两半必须一起说。**
截至 **2026-08-17**，本项目 `gates/` 下已有**真的可运行、退出码判定**的门：统一入口 `./gates/run_all.sh`，
`everything` scope 当前 **8/8 全绿**（八项 = **五道门 + 三套负例套件**；另有 `check_publishable.mjs` 走 `publish` scope，
故 `gates/` 下共 **六个门脚本 + 三套负例套件**）。三套负例套件在临时副本（`--root`）上走**真实入口**证明对应的门会红
（`test_check_contracts.sh` · 6 红、`test_check_pointers.sh` · 7 红含空集闸、`test_check_m0.sh` · **9 红 + 1 绿样本**）。
五道门是：
`check_contracts.mjs`（规范源自洽：词表↔作用表双向完备、状态符号不越界、禁止聚合字段）、
`check_pointers.mjs`（`[E:]` 指针的文件存在性 + 锚点可解析性 + 只许下降的欠债棘轮）、
`check_doc_metrics.mjs`（自述数字与实测相符）、
`check_status_exhaustive.mjs`（`src/status.mjs` 的**穷举 oracle**——**550 万输入向量、六值全可达、C-1 回归 4/4**，
"`S` 是全函数"由此从断言变成了实测）、
`check_m0.mjs`（M0 实测记录齐全 + 结构合法 + **抽样重跑比对 sha256**）。

**但它们检查的对象，全部是这八份 markdown、一份 M0 实测记录集，与那八份 markdown 所定义的那个纯函数。**
**准确说法是：已落地的是「文档层 + 状态函数层」的门；尚未落地的是「证据层」与「运行层」的门。**
判据写成可核对的形式：**没有任何一道门读过一件证据工件**（`claims/*`、快照、`status.json`、台账完备性），
**也没有任何一道门读过一条运行时痕迹**（session jsonl 事件、门轨道与 producer 轨道的隔离、任何真实抓取）。
`check_status_exhaustive.mjs` 是这条界线最好的例子：它枚举的是**合成输入向量**，证明的是**规范自洽**，
不是"我们对一条真 claim 算对了状态"——**规范正确与产物正确是两件事**。
`check_m0.mjs` 也不构成例外：它验的是**实测记录这份文档**的齐全与自洽（外加抽样重跑哈希），
不是本系统跑出来的任何一件证据工件——**S0 的实测记录是关于 DSH 的事实，不是本系统的产物**。

**因此这件事实只能这样表述：规范与文档层的门已经不空心了；B8 押的证据层与运行层仍是零门。**
把前者的存在说成后者也有了，正是 §二 第 1 条禁止的那种升级——只不过这次的诱惑来自我们自己刚写完的代码。

### 裁决

**当前不押** —— 但 R2 之后，"不押"这两个字必须**分层**说，否则它自己会变成一句不可证伪的口号（"反正我们没押"）：

| 层 | 门检查的对象 | 现状（2026-08-17） |
|---|---|---|
| **文档层** | 这八份 markdown 自身与 `.loop/m0/` 的实测记录集：规范源自洽 / `[E:]` 指针可解析 / 自述数字与实测相符 / M0 记录齐全且抽样重跑哈希对上 | **已落地**，`./gates/run_all.sh everything` **8/8**，退出码判定，三套负例套件走真实入口证明会红 |
| **状态函数层** | 这八份 markdown 定义的那个纯函数 `src/status.mjs`：`S` 的穷举 oracle（550 万输入向量、六值全可达、C-1 回归 4/4） | **已落地**，但枚举的是**合成输入向量**——证明的是规范自洽，不是"我们对一条真 claim 算对了状态" |
| **证据层** | claim / 快照 / `status.json` / 台账完备性（R-6 的"门只验证已登记项"盲区） | **零门**。`gates/` 下没有任何脚本读过一件证据工件 |
| **运行层** | session jsonl 事件、门轨道与 producer 轨道的进程级隔离、gate 自身完整性 | **零门**。`gate_integrity.sh` 全仓 grep 仍零命中代码 |

**准确表述：文档层与状态函数层的门已经不空心了；B8 押的是证据层与运行层，那两层仍然是零。**
第一层的绿**不构成**对另外两层的任何证据——它们检查的对象不相交。
在下面三件事被写成代码之前，本条一律按**未验证前提**对待，且不得在下游文档里被引用为已建立的保障：

1. **gate 完整性写成真脚本，且脚本的信任锚点必须离开 conductor 的手。**
   **〔裁定 · S0 实测，[E: .loop/m0/M0-7.json]〕本条原本写着「这是 v2 必须补的第一个洞」，S0 实测把它改成了两句话：
   写成脚本不等于补上洞，而原来的锚点方案整个不成立。** 两条红样本判死了它，逐条给出可复跑的判据：

   | 红样本 | 做了什么 | 结果 | 判死了什么 |
   |---|---|---|---|
   | **tag 不可变性电池** | 同一个配了 `receive.denyDeletes=true` + `receive.denyNonFastForwards=true` 的裸远端上，对 `refs/tags/*` 与 `refs/heads/*` 各做一次非快进强推与一次删除（git 2.55.0） | tag：强推 **ACCEPTED**、删除 **ACCEPTED**；branch：双双 **REJECTED**。默认 `core.logAllRefUpdates=true` 时 tag reflog 条目 **0**（`always` 才 2） | **`<gates-baseline-tag>` 这个锚点。** C-11 判"本地 `git tag -f` 可重指"，本轮加强为"**远端也拦不住**" |
   | **gate-integrity 电池 · R3** | 把 `gate_integrity.sh` 换成一个只 `echo OK` 的假货并**提交**，再从仓库内入口 `./gates/gate_integrity.sh` 调用 | **退出码 0** | **"由 conductor 从 tag 检出一份干净副本到 `checks/` 之外执行"这个候选解。** 自提升（self-hoisting）不成立——**提升代码本身就在被检查的文件里** |

   **补法本身有自指漏洞。这句话必须原样留在文档里，不许被下一版悄悄抹平。**
   本仓库**能真正做到**的最强形态由 S0 的六格电池给出（`G1/G2/R1/R2/R3/R4`，退出码全部实测）：
   **锚点从 tag 名换成 40 位 commit sha；该 sha 与调用入口都必须位于生产者写不到的地方；
   由一个仓库外的 runner 用 `git archive <sha> gates` 展开到仓库外再执行，工作树 `gates/` 与 pinned 副本逐文件 sha256 比对。**

   | 格 | 场景 | 实测退出码 | 读法 |
   |---|---|---|---|
   | G1 | 干净树 / **仓库内**入口 | 0 | 基线 |
   | G2 | 干净树 / **仓库外 pinned runner** | 0 | 不误报 |
   | R1 | 门被改、未提交 | **2** | 工作树脏 |
   | R2 | 门被改、已提交 | **3** | 与 pinned 副本 sha256 不符 |
   | **R3** | **`gate_integrity.sh` 自身被替换 + 台账被追加一行 / 仓库内入口** | **0** | **仓库内入口被完全骗过** |
   | **R4** | **同一份篡改 / 仓库外 pinned runner，sha 由外部传入** | **3** | 只有 sha 来自外部时才抓得到 |

   即：**`./gates/run_all.sh` 不再是被推荐的入口**，推荐入口是仓库外的 pinned runner，`run_all.sh` 只作为被 pinned 展开后执行的那一份。
   三层机制（L-a/L-b/L-c）保留，但 L-a 与 L-c 的锚点字段一律由 `tag` 改为 **40 位 commit sha**：

   | 层 | 机制 | 判据（门非零退出的充要条件） | 它挡不住谁 |
   |---|---|---|---|
   | **L-a 只追加台账** | `gates/baseline-ledger.jsonl`，每行 `{commit_sha40, gates_tree_sha, recorded_at, prev_line_sha256}` 构成哈希链；由**门轨道**写、producer 轨道只读 | 外部持有的 `commit_sha40` ≠ 台账里该基线的**首次**记录；或哈希链任一环对不上；或同一基线出现两条 sha 不同的首次记录 | 同一个能写 `gates/` 的执行者也能改台账并重算哈希链——**R4 已实测**：攻击者直接**追加一行**就让仓库内入口通过了。它只把"改一处"变成"必须同时改两处且算对链"——**是摩擦，不是锚点** |
   | **L-b 让重指留痕** | `git config core.logAllRefUpdates always`，使 `.git/logs/refs/tags/<tag>` 存在 | 该配置值 ≠ `always`；或 `.git/logs/refs/tags/<tag>` 不存在；或其**行数 > 1**（>1 即被重指过） | reflog 文件可被 `rm` 直接删除。**实测**：`rm .git/logs/refs/tags/<t>` 退出码 0，随后 `git reflog show refs/tags/<t>` 无输出；`git reflog expire --expire=now` 同效。**它抓懒惰的篡改者，不抓知道自己在做什么的**。**且 S0 实测：远端侧默认根本不写 tag reflog（0 条），所以 L-b 是纯本地机制，push 出去不会自动获得它** |
   | **L-c 外部锚点** | 那 40 位 commit sha **与调用入口**由**仓库外**持有 | 仓库外 runner 用外部持有的 sha 做 `git archive` 展开后，工作树 `gates/` 与 pinned 副本存在任一文件 sha256 不符 | ——（这一层才是真正改变博弈的那层，也正是本仓库当前**没有**的那层） |

   **L-b 的两条依据是本轮实测，不是从文档推的**（2026-08-17）：默认 `core.logAllRefUpdates = true` **不**产生 tag reflog（`ls .git/logs/refs/` 只有 `heads`）；改成 `always` 后 `.git/logs/refs/tags/<tag>` 才出现，并逐条记录每次重指。

   **L-c 在本仓库当前无法实现，这是一笔必须写明的未偿债**：`git remote` 数为 **0**，**没有远端**。
   可选的外部锚点只有三条，**每一条都要求一个仓库外的信任方**——这正是"不可能完全自证"的具体形状：

   - **(i) 受保护引用的远端**：push 到一个 conductor 没有 force-push / delete 权限的远端。**判据必须挂在分支或 GitHub ruleset 上，不能只挂 tag**——S0 实测 `receive.deny*` 不覆盖 `refs/tags/*`；GitHub rulesets 的 Restrict updates / Restrict deletions / Block force pushes 官方文档明写适用于 tag，**但那是远端管理员配置项、带 bypass permissions 豁免，且本轮只读了文档、没有真的建仓库配 ruleset 试强推**。
   - **(ii) 签名 tag，私钥不在跑 checks 的那台机器上**。判据：`git verify-tag <tag>` 退出码 0 **且**签名者 ∈ 白名单。当前 `tag.gpgSign` / `user.signingkey` **均未设**，22 个 `gates-baseline-*` tag 里带 PGP 签名的是 **0 个**（S0 实测），且本轮**没有验证本机是否具备可用签名密钥**——**这条路一次都没跑过**。
   - **(iii) 外部时间戳**：把 commit sha 提交到一个公开的只追加日志。判据：时间戳凭据可离线验证，且其时刻早于本次 run 的 `started_at`。

   **不要假装解决了它。** 下面这份清单来自 S0 记录的 `honest_limits`，逐条都是实测或推理确认过的，**不得当成已修复**：

   1. **外部锚点今天不存在。** `git remote` 数为 0，那个"仓库外的 40 位 sha"现在没有任何地方可放；
      放在本机另一个文件里，对一个持有 bash 的 agent 而言**不构成边界**。
   2. **R4 的绿是买来的，不是挣来的。** pinned runner 之所以能抓到篡改，**唯一原因是 sha 由外部传入**；
      第一版 runner 忘了这点、仍去读仓库内的台账，**同样被骗过（exit 0）**——该机制对实现细节**极其脆弱**。
   3. **TOFU。** 第一条 baseline 是因为它第一才被信任，没有任何东西为它背书。
   4. **GitHub ruleset 只是搬家不是解决**：把信任根从"本地文件"搬到"远端管理员位"，而 admin 带 bypass permissions、可临时关掉再打开。
   5. **同机同用户不是两条轨道**："门轨道与 producer 轨道进程级隔离"在一台机器一个用户下只是**执行上下文不同，不是权限主体不同**；只要 producer 能拿到 bash，它就能改 runner。
   6. **签名没测**（见 (ii)）。**tag 实验只在本地 `file://` 传输上做**，对 ssh/https 服务端是否一致**未验证**（`receive.deny*` 是 receive-pack 侧配置，理论上一致，但那是推理不是实测）。
   7. **台账哈希链只防静默改写中间行，不防整条链重建**——R4 里攻击者直接追加一行就通过了。

   **这是一个循环，不是一个还没写完的脚本。** 一手语料里那条未决（"`gate_integrity.sh` 自己也在 `checks/` 里"）
   与它是同一个循环的两个面：把脚本从 tag 检出到 `checks/` 之外执行，**S0 已实测证伪**（R3 exit 0），
   因为发起检出的那个入口本身还在仓库里 [E: gt-house-method.md#未决1]。
   **因此 B8 第 1 条的升级条件改写为：L-a（锚点字段为 40 位 commit sha）+ L-b 落地，
   且 L-c 三条里至少一条成立、且该 sha 与 runner 均由仓库外持有。
   只做 L-a + L-b 不构成升级**——那只是把自证做得更精细，仍然是自证。
   **本条的现状一句话：有一半答案（锚点换 commit sha + 仓库外 pinned runner，红样本已跑通），另一半仍未偿（外部信任根未配置）。**

2. **每条 pass 规则配一条走真实入口的负例**（B6 的 R-8），负例套件本身进 CI。
   **判据（一行可判）**：`for f in gates/check_*.mjs; do [ -f "gates/test_$(basename $f .mjs).sh" ] || echo "$f 无负例"; done` 输出为空；且每套负例在临时副本（`--root`）上跑**真实入口**而非直调内部函数。
   **当前违反（2026-08-17 实测，S0 后复核仍成立）**：`gates/` 下六个 `check_*.mjs`、三套 `test_check_*.sh`，
   缺口是 `check_doc_metrics.mjs`、`check_publishable.mjs`、`check_status_exhaustive.mjs` **三道没有对应的负例套件**——
   按 R-8，它们现在是**未被证明会红**的门，不得计入上表"已落地"那一格。
   （S0 期间 `check_m0.mjs` 补齐了负例套件：`test_check_m0.sh` **9 红 + 1 绿样本**，缺口由四道降到三道。）

3. **证据台账有格式门**（自由文本台账会腐）。
   **当前违反，而且是本节自己踩的**：07-ATTACK-LEDGER 头注自陈"本文件的**承重部分**（findings 全量、哈希、命中判定）一律由脚本从 JSON 生成，散文只负责裁决理由"，
   而 `grep -rl 'r1-findings' --include='*.mjs' --include='*.sh' --include='*.py' .` **零命中**——那个生成脚本不存在，承重部分现在是手写散文。
   **判据**：07 的承重小节由脚本从 `.attack/r1-findings.json` 渲染，门断言"重新渲染的结果与文件内容逐字节相同"且 `r1-findings.sha256` 与实际哈希一致（后者当前**已成立**，前者当前**不成立**）。
   **这是"文档里写了 known limitation 不能替代代码里 fail-closed"的又一次复发，而这次复发在我们自己的攻击台账上。**

**外加一条来自 house 的最高 ROI 纪律**：**任何跨执行边界的断言必须对运行系统自己的痕迹断言**（session jsonl 事件存在性 + hash），不得停在配置层。这条在本仓库连中三次。

**落点已由一手真值给定，不得自行发明：**
- 证据元数据落 **`tool/result.data.meta`**（模型不可见、抗 pruner、抗持久化、类型已知）。**不要**建自定义 log-only 会话事件：`ignorable` 字段**只读不写**（`Session.append` 无该参数，全 194 包零写入方），自定义 log-only 事件会产出**不可 resume 的 session**。[E: GROUND-TRUTH-CORRECTIONS.md#A1] [E: gt-evidence-substrate.md#结论摘要]
- 逐字校验的基底必须是 **CAS 快照文件**：spill 落库的 `tool/result` 存的是**截断后**内容，超阈值纯文本结果的原文**不在日志里**。日志只用于绑定"这次抓取确实发生过"（hash + 事件存在性）。[E: GROUND-TRUTH-CORRECTIONS.md#A2]
- **JSONL 持久化是多 frame zstd 串接，Node 内置 zstd API 只解第一帧。** 任何 Node 写的日志门若不循环解帧，会**静默只读到 1 行**并误判日志为空。**这是会烧掉实现的地雷，必须写进契约。** [E: GROUND-TRUTH-CORRECTIONS.md#A3]

### 会推翻它的观测

| 观测 | 等级 |
|---|---|
| 任何一次 `MISSING` 被聚合成 `PASS` | **P0**，该门回炉并新增负例 |
| 任何一次 status 被非确定性代码路径写入 | **P0** —— 这应当是**不可能事件** |
| REPORT / 台账出现未闭环占位符而格式门未拦下 | P1，格式门失效 |
| 一轮攻击后，findings 中"由门发现"的比例 < 30% | P1 —— 门套件在做装饰，攻击者在做门的工作 |
| gate-integrity 脚本从未在真实 run 中触发过一次非零退出 | 需要构造一次真实篡改来证明它活着（**没触发过 ≠ 有效**）。**S0 已在夹具上触发过**（R1 exit 2 / R2 exit 3 / R4 exit 3），**但夹具不是真实 run**，本行仍未销 |
| 外部持有的 40 位 `commit_sha40` ≠ 只追加台账里该基线的**首次**记录 | **P0** —— 锚点被重指，本轮及其之后所有门结果作废，直到重新建链。**（本行的锚点字段已由 `<gates-baseline-tag>` 改为 40 位 commit sha：S0 实测 tag 的非快进强推与删除在远端均 ACCEPTED，`receive.deny*` 不覆盖 `refs/tags/*`）** |
| gate-integrity 从**仓库内**入口调用（而非仓库外 pinned runner），或其比对用的 sha 读自仓库内 | **P0** —— S0 红样本 R3/R4：同一份篡改，仓库内入口 exit **0**、仓库外 pinned runner exit **3**。仓库内入口的绿**不构成任何证据** |
| `git config --get core.logAllRefUpdates` ≠ `always`，或 `.git/logs/refs/tags/<tag>` 缺失，或其行数 > 1 | P1 —— 重指留痕机制没开、被抹掉、或已经被触发过。**注意它是纯本地机制**：S0 实测远端默认不写 tag reflog（0 条） |
| B8 被升级为"押"，而 L-c 三条外部锚点一条都不成立 | **升级无效**，直接退回"不押" —— 那是自证不是背书（C-11 的判词） |
| `gates/` 下存在某个 `check_<x>.mjs` 而无 `test_check_<x>.sh` | P1 —— 该门未被证明会红，不得计入"我们有门" |
| 有人用"文档层的门全绿"论证证据层或运行层的可信度 | P1 —— §二 第 1 条的置信度升级，且是最容易犯的那种（材料是我们自己写的代码） |

### 何时检验

**每一次 gate 合入**（准入条件）；**每一轮攻击台账结算**（R1 跨厂商 / R2 修复 / R3 fix-audit）。

---

## B9 · 成本前提可承受

### 陈述

以核验为主的架构在 DeepSeek 上成本可承受。

### 证据与警告

- **定价基准在 24 小时前刚刚移动。** DeepSeek 于 **2026-08-16 16:00 UTC** 完成全线涨价并改为分时计价。网上几乎所有"2026 年 DeepSeek 定价"文章（**包括 2026-08 月份新写的**）报的仍是旧统一价，**沿用它们会低估 1.5×–12×**。新价卡是干净的"三的幂"结构：输出 = cache-miss 输入 × **3**；pro = flash × **3**；cache-miss = cache-hit × **30**；峰时 = 谷时 × **2**。[E: ext-cost-economics.md#结论摘要1,2]
- **三个杠杆的可用性**：prompt caching **可用**（磁盘 KV cache，默认开启、无需改代码、无写入费、无存储费，命中部分降至 1/30）；模型分层 **可用**（精确 3×）；**Batch API 不存在**（官方文档导航、定价页、Rate Limit 页均无 batch 条目）。[E: ext-cost-economics.md#结论摘要3]
- **验证成本与生成成本同量级**（B3 已列）。**验证必须是独立预算科目。**
- **并行的隐性成本不是 token，是 RPS 与配额**（B1 第 7、8 条）。**超并行设计必须把搜索配额当作显式预算来分配，而不是假定无限。**
- **Anthropic 侧的成本参照全部过时**：90.2% / 15× / 80% 都基于 2025 年的 Opus 4 / Sonnet 4，且 tokenizer 已变（**Claude 4.7 及以后同样文本约多出 30% token**）——任何基于旧模型实测推出的预算算术要整体上调约 30%。[E: ext-orchestration.md#U6,核验表42]

### 裁决

- **预算即代码**：用运行时原生旋钮（`maxBudgetUsd` / 并发上限 / 深度上限），不自建预算中间件。
- **模型分层**：编排与裁决用 pro，检索/抓取 worker 用 flash；**核验器的模型选择独立于起草器**，最好换厂商或至少换档位——买独立性。（DSH 的 workflow `agent()` **支持 per-call 的 `provider` 与 `model`**，跨厂商验证者可在 workflow 内直接声明，独立性不必靠外部编排。）[E: GROUND-TRUTH-CORRECTIONS.md#A11,E1] [E: gt-orchestration.md#结论摘要5]
- **单价读自响应头而非硬编码**（OpenAlex 已有 `x-ratelimit-cost-usd` / `x-ratelimit-remaining-usd`；后者用于区分"今天钱花完了"与"退避一下再试"，否则会陷入死循环）。[E: ext-academic-apis.md#未决4,D2]
- **任何成本数字必须带抓取日期**；DeepSeek 价目视为 **30 天内会变**。

### 会推翻它的观测

- 单任务成本 > 用户可接受阈值（**该阈值必须在 M3 前有一个具体的数**，目前没有——这是一个公开的空缺）。
- 核验成本占比 > 60% 而 verified 比例未提升（与 B3 共用）。

### 何时检验

M1 起每次真实 run 记账。

---

## § 本文件的自我约束

### 一、重评义务

本文件是**可被证伪的**。每条赌注都带"会推翻它的观测"与"何时检验"。**观测到达时必须回到本文件重评该条**，并在 §重评记录 追加一行：

```
| 日期 | 赌注 id | 到达的观测（含数据） | 新裁决 | 新证据指针 |
```

**未重评而继续在下游文档里使用某条赌注，等同于我们指控 v1 犯的错误。**

### 二、禁止的置信度升级形态

**不得在没有新证据的情况下提高任何一条赌注的置信度。** 以下每一种形态都在本轮被实际抓到过，逐条禁止：

1. **把"未被证伪"写成"已被验证"** —— v1 把"结构无用"这个既未证伪也未证实的命题当成"已被四次严格消融判死"。
2. **引用结论句而不引用同一文件里的限定句** —— v1 引 comparison3 的结论，但不引它自己写的 "asymmetric dossier rendering … explains most of the calibration gap (−1.00)"。
3. **抹掉口径标注后把不同实验拼成趋势** —— `+1.0 → +0.13 → −0.39`（三个不同的被测量、模型、对照臂、渲染方式）。
4. **把 N=1 的数字当作稳定估计** —— "纪律 +1.0"，在 N=5 时变成 +0.068 (t=0.19)。
5. **多个页面复述同一上游算作多个独立佐证** —— 11 个中文域名 = 1 条日经；8 个域名 = 1 篇 BadRAG；research.google 博客 / arXiv v3 / ResearchSquare / Nature MI = 1 项工作；Unpaywall + OpenAlex = 1 个后端。
6. **把 precision 报成 accuracy** —— PaperQA2 的"超人类"只在 precision 上成立（85.2% vs 73.8%, p=0.0036），accuracy 上 66.0% vs 67.7%（p=0.66，无显著差异）。
7. **换掉分子或分母** —— Deloitte 合同额 ≠ 退款额；BadRAG 的 98.2% 是**检索命中率**不是端到端 ASR；GhostCite 的 1.07% 是**论文占比**（604/56,381）不是引文占比（约 0.034%）；MLR-Bench 的"80%"是 **8/10 个由单一 coding agent 执行的任务**，不是 201 题基准的整体比例；*17% Gap* 的 17% 是"无法解析"，其中 **78.5% 是 PDF 解析伪影**，纯幻觉只占该 17% 的 5.1%。
8. **把一手当作正确** —— comparison2 的 RESULTS.md 自己的结论句与自己的表格矛盾。**一手来源解决的是"谁说的"，不解决"说得对不对"。**
9. **用一个指向别处的 DOI 给自己背书** —— `ottosr.com` 首页称 otto-SR "Peer-reviewed in the Annals of Internal Medicine" 并链到一个 DOI，而**该 DOI 指向的是同团队另一篇论文**；otto-SR 本体截至 2026-08-17 在 Europe PMC 仅有 medRxiv 预印本记录，无期刊记录。**引用任何"已同行评审"的声明前，必须打开那个 DOI 看它指向谁。**
10. **把别家论文的性能数字搬来当自己的** —— 第三方工具 RCT-Reviewer 在文档页宣称 "Achieves 71.0% accuracy … within 8% of expert consensus"，而这是 **RobotReviewer 2015 年 JAMIA 论文**在旧版 RoB1 二分标签下对 CDSR 的数字。**性能声明必须能追到"在什么样本上、由谁、用哪个版本测的"。**
11. **把一个厂商的布尔字段当作权威判据** —— OpenAlex `is_retracted:true` 是"被撤论文 ∪ 撤稿公告 ∪ 部分更正公告"的并集，只有 40.5% 是被撤原文。**布尔字段坍缩了上游的分层语义；门必须打在原始集合上。**

### 三、证据指针的保质期

- `research/v2/` 是 **2026-08-17** 的快照。其中标 `unverified` 的条目**永远不得升格为裸数字**，除非有新的一手抓取并在此处留痕。
- 明确到期项：模型分数（6 个月内必然过时）、benchmark 榜单快照、DeepSeek 价目（30 天）、竞品对照表 D-1（每季度）、野外注入 prevalence（6 个月）、撤稿检查的竞品差距（该窗口正在关闭）。
- **凡下游文档引用外部数字而不带口径三元组或抓取日期，即为回归。** 建议 gate 脚本对"无日期的外部数字"直接报错——这与 B3 的"单指标报告应当构建失败"是同一条纪律。

### 四、本文件与下游的关系

- 下游文档**不得引用本文件的裁决作为证据**，只能引用它指向的一手来源。
- 下游文档若与本文件的裁决冲突，**必须在此处开一条重评记录说明理由**，不得静默偏离。
- 与 `GROUND-TRUTH-CORRECTIONS.md` 冲突时，**一律以该文件为准**，并回来修本文件。

### 五、本文件自身的已知弱点（预注册）

1. **B5 的 S1–S4 全部是论证，零实测。** 红队跑完前不得写成已验证。
2. **B1 的核心反向证据（−42pp）是一篇未经同行评审的预印本，仅两个模型、无统计检验、判定器是 LLM-judge。** 我们据此做了架构决策——**这是本文件最大的单点依赖**，M1 的自测曲线是它的解。
3. **B8 当前是"不押"，而项目的所有门都建立在它之上。** 如果那三条代码不落地，B1–B7 的所有"我们会用门保证"的说法都是空的。
   **R2 之后这一条变得更不舒服，而不是更舒服**：规范与文档层的门确实落地了，但 B8 押的证据层与运行层仍是零门；
   而第 1 条前置（gate 完整性）已被证明**在本仓库内不可能自证**——它需要一个当前不存在的外部锚点（`git remote -v` 输出为空）。
   **这是本文件里最应该被下一位攻击者优先攻击的一条。**
4. **B9 缺一个数**：用户可接受的单任务成本阈值目前未知。
5. **本文件没有审计"用户真的需要这个"这一前提**——B3 审的是"市场空白 + 后果严重"，不是"这个具体用户在这个具体任务上会用它"。该前提只能由 M3 的真实任务回答。
6. **本文件与 01-CONTRACTS 的词汇一致性，目前只被半道门覆盖。** B4 的 W-3（`ST-*` 不越界）已由 01 §7.4 **V7.10** 写成可运行断言，
   但那道门当前只跑 01-CONTRACTS 一份文档（`gates/check_contracts.mjs` 的默认参数），本文件是靠人工把它指过来才被检过的；
   W-1（`§TBD` 占位符）、W-2（枚举定义形态）、W-4（旧枚举词只许与它的新符号同行出现）**三条一条都没接进任何门**，现在只是三条 grep 表达式；
   而 W-4b（`unverified` 的语境义）**连 grep 都写不出来**，本轮已如实标注它是人工判据。
   **含义**：C-10 这一类"所有指针都真、术语层整段是假"的缺陷，在本仓库仍然只能靠 fresh 攻击者发现，不能靠门发现——
   这与 B8 的"由门发现的 findings 比例 < 30% 即 P1"是同一条判据打在本文件自己身上。

### 六、重评记录

| 日期 | 赌注 id | 到达的观测 | 新裁决 | 新证据指针 |
|---|---|---|---|---|
| 2026-08-17 | — | 初版建立 | — | `research/v2/`（22 文件快照） |
