# project data (canonical state lives per-project, not in the repo)
data/

# python
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/
dist/
build/
.pytest_cache/
.coverage

# derived index (per-project, under data/) — NOT the src/paperproof/db package
data/**/db/

# local tool state
.claude/settings.local.json
.DS_Store

# agent worktrees (harness-managed, never commit)
.claude/worktrees/
