# 07 · 攻击台账

> 本文件是**记录**，不是论证。每一轮攻击的靶标指纹、种子命中率、findings 全量、裁决与修复状态都落在这里。
> 机器可读全量在 `.attack/`：`r1-findings.json`（164 条）、`r1-findings.sha256`、`r1-target-hashes.json`。
>
> **台账纪律**（来自一手教训 [E: GROUND-TRUTH-CORRECTIONS.md#D3]：前代 `r1-ledger.md` 出现截断与重复小节，
> R3 自己把 "ledger garble" 记为 P3）：可信度产品的证据台账若是自由文本会腐。因此本文件的**承重部分**
> （findings 全量、哈希、命中判定）一律由脚本从 JSON 生成，散文只负责裁决理由。

---

## 轮次总览

| 轮 | 日期 | 靶标 | 攻击者 | 种子 | findings | 状态 |
|---|---|---|---|---|---|---|
| R1 | 2026-08-17 | v2 规划集 8 份（冻结快照） | 10 × Claude 五透镜 + 3 × 跨厂商 | 9 生效 / 6 命中 | 164（去种子后 138） | 裁决完成 |
| R1-p | 2026-08-17 | 同上 + `research/v2/` 一手语料 | 4 × provenance 透镜（补轮） | n/a | 见 §R1-p | 裁决完成，修复已落 |
| R2 | 2026-08-17 | R1 + R1-p 的裁决 | 修复者（主循环 + 4 × 子代理） | n/a | 见 §R2 | **部分完成**——子代理批次因会话额度失败，01-CONTRACTS 与横切项由主循环完成 |
| R3 | 2026-08-17 | R2 + S0 的修复 diff | 6 × fix-audit（五轴 + 门专审），全部未写过这些修复 | n/a | **92**（P1 31 / P2 42 / P3 19） | 裁决进行中，三条最重的 P1 已修 |
| R4 | 2026-08-18 | R3-fix 的修复 diff（文档面 + 门面各一路） | 2 × fix-audit，未写过这些修复 | n/a | 见 `.attack/r4-*.json` | 裁决完成，修复已落 |
| R5 | 2026-08-18 | R4 修复轮 | 1 × fix-audit | n/a | 见 `.attack/r5-findings.json` | 裁决完成；**5 条预测**留档待验 |
| S3 | 2026-08-18 | 自攻：L1-c 词表 + 组稿器 + 归并器 | 自攻（同一心智，独立性仅 instance 级） | n/a | 6（P1 2 / P2 3 / 撤回 1） | 3 修 2 认账 1 撤回 |
| **R6** | 2026-08-18 | **产品层**（53 文件冻结） | 1 × 独立攻击者，未参与实现 | n/a | **15**（P1 8 / P2 5 / P3 1 / INFO 1） | **全部裁决完成**，见 §S4 |
| **E1** | 2026-08-19 | **产品层 vs 六份真实文献** | 外部语料（**第一份不是我们出的题**） | n/a | **7**（首轮 4 条判错 + 3 条产品缺陷） | **全部修复 + 配回归**，见 §S5 |

---

## R3 · fix-audit（2026-08-17）

**这是本项目一直欠的那一轮。** 修复者不能审计自己的修复——R3 的六个上下文没写过其中任何一行。

基线是 R1 的冻结快照（修复前），靶标指纹在 `.attack/r3-target-hashes.json`，findings 全量在 `.attack/r3-findings.json`。

### 结果

**92 条 findings，P1 三十一条。** 按轴分布：

| 轴 | 条数 | 说明 |
|---|---|---|
| `not-closed` | 37 | 缺陷没被关掉——修复动了别处，或只关了一半 |
| `gate-hollow` | 21 | 门的绿灯什么都没证明 |
| `new-defect` | 18 | **修复本身引入的新缺陷**——fix-audit 最该抓的一类 |
| `not-machine-decidable` | 11 | 号称给了机器判据，实际不可判 |
| `weakened-elsewhere` | 4 | 为了让这处成立，别处一条断言被悄悄放宽 |
| `reworded-only` | 1 | 只换了措辞 |

另有 **51 条被明确判定为「真的修好了」**（`fixes_that_hold`）——这个字段不是客套，
它让下一轮不必重查，与「没修好」是同等强度的判断。

### 三条已修的最重 P1（都能在旧实现上逐字复现）

**R3/P1-2 · `S` 从不读 `mechanism_results` 的内容，只判非空。**
于是「GC-2 永远不得写 ST-V」——被 §6.1 表、§6.4、V1.4、V6.2 **四处**规范化、
被两份下游文档当作唯一依据的规则——**在唯一的可执行规范里没有任何载体**。
实测：一条机制全为 GC-2 的 K-L-T claim 直接返回 `verified`。

**而穷举 oracle 看不见它**：向量生成器把 `gate_class` 硬编码成 `'GC-0'`，只让 `mechanism_results` 的**长度**变化。
所以「550 万向量全绿」与产品最承重的那条分界线**完全无关**。

> **这是本轮对「穷举即覆盖」这个直觉最有力的反例：
> 枚举的是维度，不是空间；没进枚举的维度，穷举再多也照不亮。**

已修：`S` 增加「决定性机制含 GC-2 → `meet(base, ST-A)`」；
oracle 把 `gate_class` 提升为一个五值枚举维度（empty / GC-0 / GC-1 / GC-2 / GC-0+GC-2），
向量数从 5,557,248 增至 **13,893,120**。

**R3/P1-3 · `S` 对必填字段缺失 fail-open。**
§1.3 写「必填字段缺任一 → 直接 `not_covered`」，§9.19 写「fail-open 是禁止的；MISSING == FAIL」。
但实现只对 5 个字段抛，其余静默走通——最要命的是 `counter_evidence_searched`：
`0e` 写成 `=== false`，**字段缺失时不触发**，而 §7.2.3 恰称它是「逃不过的只有这一条」。
实测：省略该字段的向量返回 `verified`；`quote_faithful: 'PASS'`（大小写错）同样绕过 `0d`。
oracle 抓不到，因为它只枚举 `true`/`false`，**从不枚举「字段缺失」**。

已修：十个必填字段的 fail-closed 前置检查 + 两处值域校验。

**R3/P1-4 · K-L-T 的「降为 K-L-A 处理」只改了 base，没改 kind。**
于是 `2b` 仍用 `K(K-L-T)=1` 而非 `K(K-L-A)=2`，造出一条**洗白通道**：
同样的证据，声明成 K-L-T 再让锚点检验失败（→ `attributed`），
比诚实声明成 K-L-A（→ `unverified`）拿到**更高**的 status。

已修：引入 `effectiveKind`，`2b` 读它而非 `c.kind`。

### 尚未处理的 P1（诚实登记）

- **R3/P1-1** C-2 只关了一半：§0.2 明令不得声称「不存在 agent→status 写路径」，
  但 §4.1 的 I-W1 **仍逐字保留那句话**——而 §4.1 的标题正是「不变量（原文，可逐字引用）」，
  是全文档集里唯一被授权原样复制的段落。且 03-EVIDENCE-ENGINE 断言「该句已在 R2 删除」，**那句话本身是假的**。
- **R3/P1-5** C-1b 的全函数论证有洞：§3.4 给 G0 的上限是 ST-N，这是 ST-N 的**第三个入口**，
  而 `meet(x, ST-N)` 在 §1.5.1.1 里无定义。实现用一行契约里没有的 early-return 绕过。
  即「穷举证明 S 是全函数」证的是**实现的补丁**，不是规范文本。
- **R3/P1-6** `meet(ST-A, ST-E) = ST-U` 破坏了 V7.2：任何 `G4` 的图形读数 claim 必被打成 ST-U，
  而 PDF 里的图表恰恰是 G4 的典型形态。**V7.2 现在恒假，且没有任何门检查 V7.2。**
- **R3/P1-7** `S` 的输入没有工件：实现消费的 7 个字段在全部 8 份文档里出现 **0 次**；
  W-04 的枚举不含 `mechanism_results` / `flags` / `budget_state`。V1.2 因此不可判定。
- 另有 24 条 P1 待逐条裁决。

**本轮结论：R3 未收敛。** 92 条里只处理了 3 条，且新发现的 `gate-hollow` 有 21 条。
按 `vince-attacker` 的判据，含修复的轮次必须再过一轮 fix-audit——**R4 是必需的，不是可选的**。

---

## R1（2026-08-17）

### 靶标

攻击对象是**冻结快照**，不是活文档——攻击进行期间作者不得改动靶标，否则 findings 的行号失效、
且作者可以在攻击过程中悄悄修补。8 份文件的 sha256 记在 `.attack/r1-target-hashes.json`。

### 攻击者拓扑

10 个 fresh context，每个只拿**一个透镜**，零撰写历史：

| 攻击者 | 透镜 | P1 | P2 | P3 | flags |
|---|---|---|---|---|---|
| A1-coherence-contracts | coherence | 4 | 12 | 4 | 6 |
| A2-drift-hunt | coherence（定义漂移专项） | 5 | 8 | 5 | 6 |
| A3-gaming-status | gaming（status 写权） | 6 | 6 | 1 | 6 |
| A4-gaming-gates | gaming（门实现） | 7 | 13 | 5 | 7 |
| A5-evidence-numbers | evidence（数字核对，带 web） | 3 | 7 | 2 | 8 |
| A6-evidence-pointers | evidence（[E:] 指针可解析性） | 2 | 6 | 4 | 5 |
| A7-reality-dsh | reality（DSH 真实能力） | 5 | 8 | 4 | 6 |
| A8-foundation-b8 | foundation（B8 自认弱点） | 4 | 9 | 5 | 5 |
| A9-foundation-premise | foundation（9 条赌注） | 3 | 9 | 2 | 5 |
| A10-reality-buildability | reality（可建造性） | 6 | 7 | 2 | 5 |
| **合计** | | **45** | **85** | **34** | **59** |

另有三份**跨厂商**报告（DeepSeek）覆盖 core / engine / orch 三组，
用途是买不同的盲区而非更强的能力。

〔R4 修复 · 这条是本门抓到的我自己编造的引用〕本句原带一条指向 `06-SURVEY.md` 的
`MAS-ProVe` 锚（此处刻意不复写指针的原样字面，否则门会把**提及**当成**使用**——
它分不清二者；给代码块开豁免又等于开一条绕过通道，所以正确做法是改写例子），
而 **`MAS-ProVe` 在 06-SURVEY 里根本不存在，在整个语料里也只出现在这条引用自身**。
它的真实出处是 house 的 attacker skill（`vince-attacker/SKILL.md`），
该文件在本仓库之外、不随仓库分发，因此这条依据**在本仓库内不可复核**，
按本项目自己的规矩记为 `unverified`，不得作为承重依据。

指针门此前看不见它：`if (PLAN_DOCS.includes(target)) continue` 让**规划文档之间的引用
一条都没被检查过**——正是本门声称取代 D-1 的那一类，且发生在引用量最大的地方
（01-CONTRACTS 是唯一规范源，被其余八份反复引用）。修复后本条当场现形。

### 种子闸门（SEED CHECK）

攻击前向冻结快照植入 10 处已知缺陷。**漏掉自己种子的攻击者，其"没发现问题"不成立**——
这是 PROVE-OR-FLAG 给不了的那道防线（它过滤假阳性，SEED 抓的是"瞎攻击者被读成靶标干净"）。

判据：**冻结副本中该替换串的出现次数 > 活文档中的出现次数**。
（先用关键词匹配，被 V10 骗过——它的替换串恰好等于一个原有小节标题，两边都是 2 次，
所以关键词"在位"为真但替换其实是空操作。次数差是干净的判据。）

| 种子 | 文件 | 注入的缺陷 | 生效 | 命中 |
|---|---|---|---|---|
| V1 | 01-CONTRACTS | 单调性不变量开"唯一例外"，编排器可把 ST-U 提回 base | ✅ | **9/10 攻击者** |
| V2 | 01-CONTRACTS | GC-2 在 κ≥0.8 时可例外写 ST-V | ✅ | **10/10 攻击者** |
| V3 | 03-EVIDENCE-ENGINE | red-first 加"时间紧张时可先绿后补红"后门 | ✅ | A10 |
| V4 | 04-ORCHESTRATION | 反证检索"预算紧张时可跳过" | ✅ | A1 A3 A7 |
| V5 | 02-ARCHITECTURE | `ignorable` 由"只读"改成"可写"（与一手代码矛盾） | ✅ | **无人命中** |
| V6 | 05-TESTING | held-out"连续两次失败的题目可解冻复用" | ✅ | A4 |
| V7 | 06-SURVEY | 核心引用 arXiv ID 改错一位 | ✅ | A5 |
| V8 | 00-PREMISE | B1 裁决由"不押"无证据上调为"小仓位押注" | ✅ | **无人命中** |
| V9 | 03-EVIDENCE-ENGINE | 实测精度上界被改高到 94.7 | ✅ | **无人命中** |
| V10 | 04-ORCHESTRATION | （替换串与原标题重合，**空操作**） | ❌ 作废 | n/a |

**命中率 6/9。每个攻击者至少命中 2 个 ⇒ 无攻击者作废。**

**这一轮电池自身的缺陷（比任何单条 finding 都重要）：**
三个漏掉的种子形态完全一致——**某个事实数值被悄悄改成错的**（一手代码事实、裁决强度、实测上界）。
它们只能靠回到 `research/v2/` 一手语料交叉核对才能抓到，而十个攻击者全部在**规划集内部**做交叉引用。
A5 带 web 搜索抓到了 V7（外部可查的 arXiv ID），说明**外部核对**这条通道是通的，
断掉的是**内部一手核对**：没有任何攻击者去验证 `[E: gt-*.md#X]` 指针所声称的内容与 `gt-*.md` 实际所写是否一致。

讽刺的是这正是本项目卖给用户的谓词（`source_integrity`）——**电池没有对自己跑这条检查**。
R2 必须补一个 `provenance` 攻击者：随机抽 N 条 `[E:]` 指针，回到被指向的文件核对声称与原文。
在它跑完之前，本轮的"未发现事实性改写"**不成立**。

### findings 普查

去掉 26 条种子衍生后，**真实候选 138 条：P1 24 / P2 81 / P3 33**。

分布高度集中于规范源（符合预期——所有矛盾最终锚定在定义上）：

| 文件 | P1 | P2 |
|---|---|---|
| 01-CONTRACTS.md | 14 | 42 |
| 03-EVIDENCE-ENGINE.md | 4 | 9 |
| 00-PREMISE.md | 2 | 11 |
| 02-ARCHITECTURE.md | 2 | 4 |
| 05-TESTING.md | 2 | 3 |
| 04-ORCHESTRATION.md | 0 | 6 |
| README.md | 0 | 4 |
| 06-SURVEY.md | 0 | 2 |

### P1 裁决

24 条 P1 归并为 **13 个不同缺陷**。按"实现者照着做会造出错的东西"排序：

#### C-1 · ST-V 在契约上不可达（F004 F023 F044 F119 F153 — 五个独立攻击者）

最强信号：五个拿不同透镜、互不通气的攻击者撞上同一处。

`F-14 single-cluster` 的置位条件是 `independent_cluster_count == 1`，**不带 kind 限定**，
而 §7.3 规定它"降一档"。但 §1.5.2 恰恰把 `K(K-D) = 1`、`K(K-L-T) = 1`——
单簇是这两条通道的**正常态**。于是 S 的 2b 不降（`1 < 1` 为假），2d 却必降。

后果是二选一，两边都塌：
- 保留 F-14 无条件降级 ⇒ 封闭式 K-D 与 K-L-T 这两类**唯一能到 ST-V 的 kind** 在单簇下永不可达，
  §2.4 矩阵的两个 ✅ 兑现不了，**产品的绿灯是空的**。
- 给 F-14 加 kind 条件 ⇒ 旗舰红队用例 RT-1（V1.6）必然判成 verified，
  而该用例的定义是"失败即证明我们把 faithfulness 当 factuality 卖"。

附带 F153：2d 写作 `base = min(base, ceiling(flags))`，消费的必须是**状态上界**，
但 §7.3 有四行给的不是上界而是一个**操作**（"降一档"）。类型就不对。

#### C-2 · §0.2 全局公理为假（F003 F025）

§0.2 宣称"不存在任何一条从 agent 输出到 `status` 的写路径，这不是纪律要求，是写权矩阵的结构性后果"。
同一文件 §4.4 自己写着：写权**不能**用沙箱强制，`run_code` 同时绕过内核沙箱与 `ctx.fs` 围栏。
而 00-PREMISE B8 明令这件事"不得在下游文档里被引用为已建立的保障"。
规范源的**第一条公理**违反了自己前提文档的禁令。

F025 追加：用来兜底的"门产物自证签名"正是 §8.4 D-8.4 以同一威胁模型否决过的机制——防篡改论证成环。

#### C-3 · `quote_faithful` 的 100% 承诺有模糊分支（F079 F152）

两个攻击者独立指出：唯一实现里写着 `rapidfuzz partial_ratio ≥ 95` 命中即
"把引语改写成快照里的真实跨度后重判"→ pass。
于是 pass 的含义不再是"逐字引语是快照的子串"，**哈希可判定性没了**，
而这是全项目仅有的两条"可 100% 兑现"承诺之一。

#### C-4 · `counter_evidence_searched` 没有门（F041 F056）

它是 S 的 0e 前置否决项，§7.2.3 称它"逃不过的只有这一条"，是抵御选择性引用的唯一防线。
但 03-EVIDENCE-ENGINE 的门清单里**根本不存在计算它的门**——只剩一个存在性检查，
即"producer 说搜过就算搜过"。防线是自报的。

#### C-5 · 独立簇归并器完全缺席（F057）

`independent_cluster_count` 是 S 的核心输入，但 03 没有为它分配 gate id、gate_class、
EE-0 信封、`power_basis`，也没有任何负向测试。S 的输入有一项无人生产。

#### C-6 · 文献通道的 ST-V 不可达（F054）

03 §3.1.1 规定 K-L 的固定流水线含两个 GC-2 门（G-ENTAIL、G-L2），
而 §6.1 规定 GC-2 永不写 ST-V。三条背书通道去掉一条。
（注意：这与种子 V2 打的是同一处，但方向相反——V2 是给 GC-2 开后门，本条是通道被堵死。
两个攻击者能同时看见开后门与堵死，说明这一段的写法本身承受不住。）

#### C-7 · Tier B 默认留存档下两条 100% 承诺同时塌（F138）

默认档 Tier B 只留"围绕引语裁出来的摘录"，于是 `quote_faithful` 退化为**构造性恒真**
（拿引语去匹配围绕它裁出的片段），`source_integrity` 退化为依赖实时重取
（文档自己的一手数字是 3/5 = 60% 成功）。这条不在任何已知弱点清单里。

#### C-8 · G-DEP 被一行哈希断言整体绕过（F055）

03 §2.4 开篇自陈"这是本节最重要的门，因为没有它 G-RERUN 是一个几乎不会红的检查"，
而 C-3/C-4 的通过条件写法让扰动追踪可被绕开。

#### C-9 · `question_authored_by` 无冻结机制（F043）

它是 K-D 通往 ST-V 的唯一开关（开放式端到端永不可达 ST-V），
但没有任何冻结/哈希入 manifest 的机制——对照 04 给 checklist 配了完整的冻结链路。
谁写的问题由被检查方自报。

#### C-10 · 00-PREMISE B4 的枚举与规范源不一致（F024）

B4 被声明为 binding，却规定了一套与 §1.4 完全不同的六值枚举与四个未入 §9 术语表的字段。
照 B4 实现即触发 V1.1（"出现第七个值即失败"）。
**根因是撰写流水线的错误：我把 CONTRACTS 与 PREMISE 并行撰写，PREMISE 无法引用尚未定稿的枚举。**

#### C-11 · gate-integrity 的信任锚点可被重指（F121）

`gates-baseline` tag 由编写 checks 的同一个 conductor 自己打，且 `git tag -f` 可重指。
B8 自称"必须补的第一个洞"，其补法本身有自指漏洞。

**〔裁决回填 · S0 实测，2026-08-18〕[E: .loop/m0/M0-7.json]**——本条**成立，且比攻击者的原判更重**；该记录 verdict 为 `design-changed`，状态从「未修」改判为**部分修复，不结案**。

- **重指比原判说的更容易，而且不留痕。** 不止 `git tag -f`：实测 git 2.55.0，服务端 `receive.denyDeletes` 与
  `receive.denyNonFastForwards` 对 `refs/tags/*` **全部不生效**（tag 的非快进强推与删除都 ACCEPTED，
  同一次实验里 `refs/heads/*` 双双 REJECTED），且默认 `core.logAllRefUpdates=true` 时 tag 连 reflog 都不写
  （0 条，只有 `always` 才写 2 条）。**tag 不是不可变锚点**，凡把 tag 当可复现性锚点的设计一律被证伪。
  佐证：本仓库 22 个 `gates-baseline-*` tag 中带 PGP 签名的是 **0** 个，`gates-baseline-22` 已是轻量 tag。
  （GitHub rulesets 确实能把 tag 配成受限，但那是远端管理员配置项、带 bypass 豁免，且本轮只读文档没实配。）
- **原文档给出的补法也被证伪。** 02-ARCHITECTURE 原写「由 conductor 从 tag 检出一份干净副本到 `checks/`
  之外的路径执行」——红样本 R3 把 `gate_integrity.sh` 换成只 `echo OK` 的假货并提交、同时给台账追加一行，
  从仓库内入口调用得到 **exit 0**。自提升不成立：发起检出的代码本身就在被检查的文件里。
- **已落的修法**：锚点一律改 **40 位 commit sha**，入口改**仓库外的 pinned runner**
  （sha 由仓库外传入，`git archive <sha>` 展开到仓库外执行，再与工作树逐文件 sha256 比对）。
  同一份被篡改的仓库：仓库内入口 **exit 0**，仓库外 pinned runner **exit 3**。
- **不结案的理由**：本仓库 `git remote` 数为 **0**，那个「仓库外」**今天不存在**；且 pinned runner 的绿是买来的
  （第一版 runner 仍去读仓库内台账，同样被骗过），第一条 baseline 是 TOFU。剩余项 = **外部信任根未配置**。
- **文档落点**：02-ARCHITECTURE §B.9（含完整残余清单）、§B.4「为什么它必须不是插件」第 1 条与其红样本、§D.3 第 0 段；
  04-ORCHESTRATION §8.5 M13 与 §10 第 8 条；01-CONTRACTS §6.5.2 / §9.22 / V6.5 与 03-EVIDENCE-ENGINE §NT-00
  由规范源侧另行回填（本条不代改）。

#### C-12 · DSH 现实：出厂呈现模式假设被 bundle 覆盖（F103 F104）

- F103：全套文档把"出厂呈现模式是 `native`"当固定事实承重引用，据此裁定"P0 层不存在 / `run_code` 不在面上"。
  但本项目强制要装的 `dsh-headless` bundle 把 `tools` 行的 config **整体替换**——
  patch 是**整键替换**语义，不是深合并。假设失效。
- F104：boot 门把"机器级偏好静默改掉并发闸与模型路由"钉在 `$DSH_HOME/cordis.patch.yml` 上，
  对 `settings.yaml` 只字未提，而后者才是该威胁的真实载体。门检查了错误的对象。

#### C-13 · 核心 A/B 实验的主终点对被试变量结构性不敏感（F154 F155）

- F154：主终点 `verified 承重断言数 / 预算` 对 B1 允许的第二处扇出（同一断言的 N 路独立核验）
  **不敏感且方向为负**——N 路核验消耗预算却不增加 verified 断言数。
  被称为"本项目核心实验"的那个实验，测不出它要测的东西。
- F155：04 §7.2 规定 CP-1 / CP-2 是**强制、总是**的人在环检查点，
  但 05 §T4 的 A/B 设计里两个检查点完全不存在。

---

## R1-p · provenance 补轮（2026-08-17）

R1 的 SEED CHECK 判定电池在一类缺陷上是瞎的（三个漏掉的种子全是「事实值被悄悄改错」）。
本轮就是补那个洞：四个攻击者随机抽 `[E:]` 指针，**回到 `research/v2/` 一手语料逐字核对声称与原文**。

**结果**：仅 01-CONTRACTS 一份就核了约 170 条指针，抓到 7 处失真——
2 处 `misattributed`（锚指错小节）、3 处 `overstated`（措辞强于语料能支撑的）、2 处口径替换。
全部已修（见下方 R2）。

**最重要的一条不是任何单条 finding，是它揭示的系统性缺陷**：
这套指针的锚点命名空间是**歧义的**。同一个 token 在同一文件里可以指向四个不同结构层——
「逐条发现」小节、「载荷数字核验表」行号、「结论摘要」条号、「设计含义」节号。
实例：`ext-academic-apis.md#D2` 在一处指设计含义 D2，在另一处指逐条发现 D2。
这不是笔误，是约定本身没写下来。已在 §9 挂起裁决。

**另一个独立审计**同时发现：`[E:]` 指针的**锚点定义机制根本不存在**——
语料里零 `<a id=>`、零锚点登记表，锚纯粹是「标题/表格首列/行首加粗」这一写作习惯的副产品。
这条直接催生了 `gates/check_pointers.mjs`（见下）。

---

## R2 · 修复（2026-08-17，进行中）

### 已完成

**01-CONTRACTS（规范源，由主循环亲自修，未委派）**

| 编号 | 缺陷 | 修法 |
|---|---|---|
| C-1 | `F-14` 被 2b 与 2d 消费两遍，导致 **ST-V 全局不可达** | §7.3 整表重写：每条 flag 显式带**作用类型**（ceiling / step-down / indirect / none）；`S` 第 2 步拆出 2d（吃 ceiling，用 meet）与 2d′（吃 step-down，叠加）；`F-14` 降为**仅作记录**，独立性只由 2b 的 `K(kind)` 判定；补 §7.3.1「已在别处返回的 flag」表，杜绝重复计算 |
| C-1b | `min()` 作用在明写不可比的 ST-A / ST-E 上，`S` 不是全函数 | 新增 §1.5.1.1 完整定义 `降一档` 与 `meet`，取 `meet(ST-A, ST-E) = ST-U`（格论下确界）；新增 V1.7 穷举断言 |
| C-2 | §0.2 全局公理为假（自称「不存在 agent→status 写路径，结构性后果」） | 改写为两层表：工具面**结构性**、文件面**无强制**（`run_code` 双绕过）。准确表述与 M0 收口一并写出。这条同时是对 00-PREMISE B8 禁令的服从 |
| C-3 | `quote_faithful` 的 `≥95 → 改写后重判 → pass` 分支吃掉哈希可判定性 | 判定只剩两个出口，`pass` 当且仅当精确子串；近似命中判 fail + F-28 + 新 flag F-28a，门只写修复建议 `quote-repair.json`（新增 W-16），由 producer 改 claim 后重跑 |
| C-7 | 默认档 Tier B 下两条 100% 承诺同时塌 | 新增 §8.6.2.1：承认 Tier B 的离线复核是**构造性恒真**；收口为「判定一次、此后由哈希链**背书**而非**重判**」；明确 `status` 是**带时间戳的量**，必须携带 `computed_at` / `revalidated_at` |
| C-9 | `question_authored_by` 是 K-D 通往 ST-V 的唯一开关却由被检查方自报 | 改为三条硬条件：冻结文件 `questions.frozen.json`、其 sha256 入 manifest 且**写入事件 seq 早于**首条 producer `tool/call`、question_id 精确匹配。把 p-hacking 变成可判定的时序事实 |
| F006 | `counter_evidence_searched == false` 在规范源里有 ST-U 与 ST-N 两个输出 | 重定 §1.4.1 分界线：**不是能力，是程序是否跑完**。ST-U = 跑完了没找到；ST-N = 没跑完（无论跑不了还是没执行）。该输入明确落 ST-N |
| F012 | `F-<数字>` 命名空间与前代 finding 编号冲突，V7.1 会误判 | 新增 §2.3.0：前代加 `PG-` 前缀，本项目攻击轮加 `R<轮次>-` |
| F014 | `source_integrity` 值域缺 `na`，**K-I 通道整条死掉** | 补 `na` + §1.2.1.1 唯一准入条件 + 新断言 V1.8 |
| R1-p | 5 处溯源失真 | `#B`→`#C1`、`#E`→`#C`+`#K`、38.6pp 的样本从「541,000 次判定」更正为「MT-Bench 2,391 对」、删去语料原文没有的「英文」二字、`F-23` 出处改到真正的跨语言样本、§0.4 的「无」改回语料的「未找到（`unverified`）」 |

**02-ARCHITECTURE**：fixture 计数三处散文写「五个」而 D.4 表列六行，同时 §D.3 要求断言「恰好有 N 个」——
照散文实现会写成 `== 5` 而表要求 6，**这道门会永久红**。已统一为 6 并补裁定说明 L3 为何拆成同步/异步两个样本。

**00-PREMISE / 06-SURVEY**：5 个 raw agreement 数字未并排 κ，违反 V9.4。
一手研究确实没报 κ，所以修法不是删数字，而是如实标注口径与「只能读作上界」。

**README**：`46 个当场纠正` 不匹配任何自算值（§3.3 表是 37，语料全库 corrected 是 50）——
已改为 37 并写明口径；八份文档的行数与断言数全部回填为实测值。

### 落地的门（本轮最实质的产出）

修复之外，本轮把四条判据从散文变成了**可运行的门**，全部配负例套件并证明会红：

| 门 | 查什么 | 负例套件 |
|---|---|---|
| `gates/check_contracts.mjs` | 词表↔作用表双向完备无重复（V7.9）、状态符号不越界（V7.10）、禁止聚合字段（V1.5） | `test_check_contracts.sh` · 6 红 |
| `gates/check_pointers.mjs` | `[E:]` 指针的**文件存在性 + 锚点可解析性**，棘轮欠债表，禁止把 00-PREMISE 裁决当证据 | `test_check_pointers.sh` · 6 红 |
| `gates/check_doc_metrics.mjs` | README 与各文档的**自述数字**必须与实测相符 | 红样本已验 |
| `gates/check_publishable.mjs` | 发布前：无硬编码密钥、无本机绝对路径、无被排除的第三方内部调研 | — |

统一入口 `gates/run_all.sh`，退出码判定。当前 **5/5 门通过，两套负例 7/7**。

**其中一条值得单列**：`check_contracts.mjs` 里原本有一条 `D-1「[E:] 指针格式可解析」`，
它**只检查指针非空**——`[E: 我瞎编的.md#根本不存在的锚]` 能过，而且只跑 8 份文档里的 1 份。
一个独立审计上下文把它判为空心门。
**在一个以「空心门是头号失败模式」为论点的项目里造出一道空心门，是本轮最该记住的教训。**
该条已移除并由 `check_pointers.mjs` 取代；其负例套件的 R-1 正是这里放过的那一类。
原处保留了一段注释而非删干净——「我们曾经有一道空心门」本身值得留在代码里。

指针实测：**1346 个 (目标, 锚) 实例，可解析率 90.0%**，108 条已知欠债登记在 `.attack/pointer-debt.json`，
棘轮方向只许下降。

### 未完成（诚实交代）

- **03-EVIDENCE-ENGINE 的 C-4 / C-5 / C-6 / C-8 未修**：四个修复子代理全部因会话额度失败，零编辑。
  这四条分别是：反证检索没有门、独立簇归并器完全缺席、文献通道 ST-V 不可达、G-DEP 被哈希断言绕过。
  **它们是 S 的输入生产方，不修则 status-function 阶段无法验收。**
- **00-PREMISE B4 的枚举漂移（C-10）未修。**
- **gate-integrity 自指（C-11）已部分修，不结案**——〔S0 实测，[E: .loop/m0/M0-7.json]〕锚点从 tag 改成
  40 位 commit sha、入口改成仓库外 pinned runner，红样本跑通（仓库内入口 exit 0 / pinned runner exit 3）；
  但**外部信任根未配置**（本仓库 `git remote` 数为 0），剩余项见上文 C-11 的裁决回填。
- **05-TESTING 的 A/B 主终点不敏感（C-13）未修。**
- **v1 覆盖率 diff 未完成**（裁决者同样因额度失败）。
- **R3 fix-audit 未跑**——R2 本身没完成，现在跑等于审计一份半成品。
  按 `vince-attacker` 的收敛判据，**本轮不构成收敛**：`coverage_gaps.notes` 记 `fix_audit: 待（R2 未完成）`。

### 本轮 coverage_gaps

- **独立性档次**：R1 的 10 个攻击者是 `instance` 档；3 份 DeepSeek 报告买到 `model` 档但只覆盖三组文档。
- **R1-p 覆盖面**：01-CONTRACTS 核了约 170/197 条唯一指针；其余三组的覆盖率低于此。
- **fix-audit 状态**：`待` —— 不是 `run`，也不是 `not-applicable`。R2 的 diff 存在但不完整。

### 本轮诚实交代（coverage_gaps）

- **`provenance` 透镜未跑** ⇒ 本轮不能声称"未发现事实性改写"（见上文种子分析）。
- **独立性档次**：10 个 Claude 攻击者是 `instance` 档；3 份 DeepSeek 报告买到 `model` 档，
  但只覆盖三组文档，未覆盖 05-TESTING 与 00-PREMISE。
- **fix-audit 状态**：`not-applicable`（R1 靶标不含任何上一轮修复）。
- **未跑**：综合轮（R+1 交互缺陷）。10 个单透镜的 findings 已落盘，交互分析待 R2 后补。

## R3-fix · 修复轮（本轮）

R3 fix-audit 出 92 条，其中 P1 31 条。本节记录修了什么、**没修什么**、以及修复过程中
由修复本身暴露出来的新缺陷。按 attacker skill 的判据，含修复的轮次必须再过一轮
fix-audit —— **R4 是必需的，不是可选的**。本节不宣称收敛。

### 一、最重的一条：我的头号证据是空的（已量化）

R3 判词：「**550 万**向量穷举 oracle 对 S 的**映射本身**几乎零覆盖……对 status.mjs 做
10 个单向抬高状态的变异，9 个存活。」
〔R4/更正〕本引文原把 R3 的「550 万」写成「1389 万」。R3 下判词时 oracle 是 550 万向量，
1389 万是 R3 修复阶段扩维后的规模。下表的击杀率测的是**当前的 1389 万版本**，
所以表里的数字对，被改错的只有这句引文——而引文错了就等于替攻击者说了他没说的话。

本轮把它从别人的断言变成**我自己每次都会重新测出来的数字**（`gates/check_status_mutants.mjs`，
注入 15 个抬高状态/抹掉约束的语义变异）：

| 门 | 击杀率 |
|---|---|
| 穷举 oracle `check_status_exhaustive`（1389 万向量） | **6/15 = 40%** |
| 新增规范符合性门 `check_status_spec`（33 条黄金用例） | **15/15 = 100%** |

存活于穷举 oracle 的包括：删掉 0e 反例检索否决（§7.2.3 称「逃不过的只有这一条」）、
删掉 0d 引语否决、删掉 0f 预算耗尽否决、删掉 0g 决定性机制为空的否决、
删掉 2a 反例命中吸收态、清空整张 ceiling/step-down flag 表、让 K-I 直达 ST-V、
把 K-D 开放式放行到 ST-V。

**为什么**：穷举 oracle 断言的是**性质**（全函数 / 纯 / 单调 / 值域 / 六值可达），
黄金门断言的是**映射**（这个输入该得哪个状态）。一个恒返回 `verified` 的实现，
性质全部满足。**枚举得再多，没断言映射就照不亮映射。**

这条教训对本项目的方法论有直接后果：此后任何「穷举了 N 个向量」的证据，
必须同时给出变异击杀率，否则 N 这个数字不构成辨别力的证明。

### 二、门的空心（gate-hollow，9 条 P1）

| 缺陷 | 修法 | 负例 |
|---|---|---|
| `check_m0` 对 `design-changed` 不要求任何证据（12 条记录里 6 条落在豁免里，全部改写成编造仍 exit 0） | `resolved` 与 `design-changed` 同属「已测量」判决，证据要求完全相同 | R-10 |
| M0 重跑可被「命令必然失败 + `output_file` 指向陈旧文件」绕过 | 新鲜度断言（mtime 必须晚于本次重跑起点）+ 退出码必须声明 | R-11 / R-12 / R-12b |
| 默认只重跑 1 条，索引 `12 % 11` **恒等于 1**，永不轮转 | 默认**全跑**；抽样必须显式索取且标记为不完整 | — |
| 指针棘轮的键不含出处文档 ⇒ 109 个已登记坏 pair 是 109 张**跨文档通用免死金牌** | 键改为 (出处文档, 目标, 锚) 的**多重集**（记次数） | R-8 / R-9 / R-11 |
| `parsePointer()` 静默丢弃不匹配的 `[E:]` 内容（D-1 放过带 `.md` 的，本门放过不带的） | 不许静默丢弃；同时认领四类此前看不懂的合法形态 | R-10 |
| 无任何门把 §7.3 作用表与实现绑起来 | `check_status_spec` 双向绑定 ceiling/step-down 两列 | 变异 M-07 / M-08 |
| 自述数字门与 README 的假描述擦肩而过 | 增加**通用**检查：相对链接必须指向真实存在的文件 | 已实证会红 |

**修复过程中由负例抓出的门自身 bug**：`check_m0` 把命令拼成 `${command} > out 2>&1`，
当命令含 `;` 时重定向只作用于最后一条语句，前面的输出全部丢失。现有记录都走
`output_file` 分支所以侥幸没踩到——**兜底路径一直是坏的而没人走过**。
由绿样本 R-12b 发现，不是由读代码发现。教训：**负例套件里必须有绿样本**，
否则「全都判红」也叫全过。

**以及一次险些收下的假证明**：给自述数字门加完链接检查后，我插入死链测到退出码 1
就差点当作「会红」。实际上门是**崩溃**了（`failed` 变量未定义），不是判红。
形状与本项目要防的完全一致：看起来门起作用了，其实是门死了。

### 三、S0 证据的方法论失败（not-closed 的根）

M0 门默认全跑后暴露：**12 份记录里 8 份的证据无法复验**——脚本生成在会话
scratchpad 下，从未入库，随会话销毁。其中 M0-1 / M0-2 是推翻设计前提、承重最大的两份。

修法不是放宽门，是**把复现路径入库并重测**：

- `gates/repro/m0_1_pdf_attributes.py` —— 纯 Python 生成 7 段对抗 PDF（1 可见 + 6 种隐藏手法），
  逐库探测属性可得性。零第三方语料、无时间戳、字节确定；库版本在 `run_m0_1.sh` 里钉死。
  **结论比原记录更强**：不仅 H0（多数库拿不到颜色/字号/图层）被证伪（7 个 (库,属性) 组合给出可判定数值），
  而且四个库**全都**会把隐藏文本读进下游（pdftotext 4/6、pypdf 6/6、pdfplumber 6/6、PyMuPDF 5/6）——
  不做属性过滤的抽取器不是能力不足，是**主动引入风险**。PyMuPDF 是唯一在默认路径尊重 OCG 默认 OFF 的。
- `gates/repro/m0_2_normalization.py` —— §1.2.2 归一化不保子串的最小复现，
  把 S0 记录的 18 格结论钉成**声明式期望**，任一格偏离即退出 1。
  其中 BUG-2（跨行连字符）的期望值写作 `(True, False, False)`——
  **现行 §1.2.2 并没有修好它**，原记录就已认账；写成期望而不是悄悄改绿，
  是为了让这条已知缺陷在每次运行里都可见。

**如实降级**：依赖不可分发第三方语料的部分（RT-5 阈值标定、同工具组 91.7–100%、
中文网页 5.0/17.5%、DeepSeek-V3 span 枚举）全部标为 `reproducible:false`，
**不得再被任何设计当作承重前提**。承重的只剩定性结论。

现状：M0 门 **24/24 条证据全部重跑且哈希一致**（此前是「11 条里验 1 条」）。

### 四、规范源的内部矛盾（01-CONTRACTS）

§4.1 I-W1 仍逐字保留 §0.2 明令禁止的那句（而 §4.1 是全文档集**唯一被授权原样复制**的段落，
那句被禁的话正沿复制路径向下游播散）· `meet(x, ST-N)` / `降一档(ST-N)` 无定义
（全函数论证的洞，被 2c 里一行契约外的补丁掩盖；移除补丁后穷举 oracle 立刻在 `{G1,[F-01]}` 上抛异常）·
V7.2 在 C-1b 之后恒假（改为上界断言，反而更强）· §4 W-04 自称穷举却漏掉 S 的全部判定输入
（七个谓词在所有规划文档里出现 0 次）· §3.4 的 G1 格只有散文没有状态（实现自行发明了 ST-N）·
§1.3 的「可以 100% 兑现」与 §1.2.2.2 直接矛盾 · §1.2.3 被实测证伪的「偏保守」处方 ·
§7.2.2 / V7.8 的抽取库前提 · §6.5.2 / V6.5 / §9.22 的 tag 锚点（tag 可被 `git tag -f` 原地重指，
远程亦然，本机 22 个 tag 0 个带签名）。以上 9 组。

**另有 3 组必须单列，因为它们零实现、零门覆盖**〔R4 更正：本节初稿标题写「12 组」却只枚举了 9 组，
省掉的恰恰是这 3 组——**选择性记账，不是笔误**〕：

| 改动 | 落地状态 |
|---|---|
| §2.2.1 K-L-T 改为两个合取项（包含检验 ∧ 极性作用域 L1-c） | 〔R5-10 销账〕**已有实现有门**：`polarity_scope_passed` 进了 S 的必填合取项（缺失抛 ContractGap）、进了穷举 oracle 的向量维度、有黄金用例守着；上述 grep 现命中 22 行。本行原写「仅文档、零命中」，是 R4 那张「诚实性补丁」表**自己失真**——补的时候是真的，做完没回来销账 |
| V2.6 同步为合取结果 | 〔R5-10 销账〕同上，随 §2.2.1 一并落地 |
| §9.2 载荷槽类型标注（entity/metric/sample/value/comparator） | **仍是仅文档**，无 schema、无门。三组里只有这一组还开着 |

这 3 组在 R4 被判为 `not-machine-decidable`：写进了唯一规范源，但没有任何机器判据接住。
本项目对自己的判据是「没有门守着的规范等于没写」，因此它们**不计入已关闭**。

### 五、明确**没有**关闭的

- **R4 fix-audit 未做。** 本节全部内容都是修复，按判据必须再过一轮独立审计。
- R3 的 42 条 P2 未逐条处理。
- BUG-2（跨行连字符还原窗口内截断的引语）**仍未修**，已钉进复现脚本的期望值。
- 外部信任根不存在（本仓库 `git remote` 数为 0），gate-integrity 的「仓库外」这一半按设计关不上。
- 03-EVIDENCE-ENGINE 的 L1-c 假阳/假阴率、X-2 的四个裁定阈值全部未测（`power_basis` 已诚实降为 `unmeasured`）。
- v1 覆盖率 diff 仍未验证。
- **零行产品代码**：`src/status.mjs` 是规范的可执行副本，不是产品。〔S2 后已不成立，见文末〕


## R4 · fix-audit（对 R3-fix 那一轮修复的独立审计）

两路独立攻击者（都没写过被审的修复）：文档层、机器层。本节先记文档层，机器层待回。

### 一、R4 的总判词：**修复普遍是「追加而非替换」「只改一半」**

这不是六条孤立的发现，是**一个可复用的失败模式**，本轮在四处以同一形状出现：

| 只改了 | 没改 | 后果 |
|---|---|---|
| §1.5.1.1（追加更正） | §1.5.1.2 仍逐字写「四元素集……第 0 步的七条前置否决覆盖了全部吸收态入口」 | 被推翻的原句在**别处继续承重**，而 V1.7 引用的正是 §1.5.1.2 |
| §2.2.1（K-L-T 改两个合取项） | §1.5（自称「唯一的计算式」）、`src/status.mjs`、两道门 | 规范与实现分叉整整一轮；且**新写的黄金门里有一条用例在为被废止的旧判据发绿灯** |
| §1.3 结尾的「100% 兑现」 | 同一文件**更早 40 行**的 §1.2.2.1 原样存活 | 修复注自己写着「同一文件 50 行后原样幸存」，然后**只向后扫** |
| tag→sha 的十处散文 | 03 的 NT-00、05:631 —— **唯二两个可执行判据** | 改了说明，漏了判据 |

**为什么会这样**：更正写成「〔R3 修复〕原文写作 X，现改为 Y」这种带出处的追加，读起来像是完整的，
但它只覆盖了作者当时正在看的那一处。**带出处的更正给了一种「已经处理过了」的错觉**，
而 grep 全文才是唯一可靠的动作。此后凡改一句承重表述，必须先 `grep` 该表述的全部出现位置，
把清单写进修复注，逐个销账。

### 二、R4-03：一个实例，实测扩为 15 条的家族

R4 报：V7.2 / §7.3.1 的主语是「含 **F-10** 的 claim」，而 S 第 1 步读的是**另一个字段**
`chart_extracted`，两者无任何绑定 —— `flags:['F-10'], chart_extracted:false` 返回 `verified`。

**实测把它扩到整族**：§7.3.1 的每一个 flag 都有同样的暴露面。裸置位后逐条实测：

```
F-29  §7.3.1 说「0e → ST-N」  →  实测 verified
F-28  §7.3.1 说「0d → ST-U」  →  实测 verified
F-31  §7.3.1 说「2a → ST-C」  →  实测 verified
F-05  §7.3.1 说「0b → ST-C」  →  实测 verified
F-16  §7.3.1 说「0a → ST-U」  →  实测 verified
F-11  §7.3.1 说「0f → ST-N」  →  实测 verified
```

根因是**主语错位**：这些 flag 在设计上是**输出标注**（门判定后贴的标签），
但 schema 允许它们作为**输入**出现。规范以 flag 为主语，实现以驱动字段为条件，
两套词汇之间没有桥 —— 所有以 flag 为主语的断言全部落空。

修法不是给 F-10 打特例，是把 §7.3.1 那张表本身变成可执行约束（新 §7.2.5）：
**flag 置位 ⟹ 其驱动条件成立，否则 ST-N**。15 条驱动条件逐条入实现、入黄金用例，
并配 10 条黄金用例（含 3 条防过修的「驱动条件成立时必须放行」）。

### 三、两道门为什么都看不见它（同一轮内的复发）

- 穷举 oracle：`withAutoFlags` 把 F-10 从 `chart_extracted` **派生**，反例向量在 1389 万里**构造性不可达**；
  且 flags 维度里**根本没有 §7.3.1 的任何 flag**。
- 规范符合性门：§7.3 绑定正则只吃主表，**§7.3.1 那 15 个 flag 一个不解析**。

本项目在 R3 刚记下「**枚举的是维度，不是空间；没进枚举的维度，穷举再多也照不亮**」，
**同一轮内以同一形状复发**。已修：flags 维度加入 §7.3.1 代表元、新增 `polarity_scope_passed` 维度、
spec 门增加 §7.3.1 解析与「每个 flag 必须在实现中有载体」的断言。

**但扩完维度之后的实测，把这条教训改写了。** 给 oracle 加上 §7.3.1 flag 与
`polarity_scope_passed` 两维后，反例向量**已经可达**——然而针对这三条新约束的变异体
（M-16 删 flag 一致性 / M-17 删 F-10 绑定 / M-18 把 K-L-T 退回单合取项）**仍然全部存活**。

原因：穷举 oracle 断言的是**性质**（全函数 / 纯 / 单调 / 值域 / 六值可达）。
删掉 flag 一致性检查后，这些性质**一条都没破**。向量走到了那里，
**却没有任何断言在那里等着它**。

所以 R3 那句话只说对了一半。完整的教训是：

> **扩空间是必要条件，不是充分条件——还得在那里放一条会响的断言。**

由此确立本项目的分工，并写进 `gates/run_all.sh` 的注释：
穷举 oracle 管**性质**，规范符合性门管**映射**，变异测试**量化两者各自的辨别力**。
任何一方单独绿灯都不构成「S 正确」的证据。

### 三之二、本轮变异测试终值（18 个变异体）

| 门 | 击杀率 |
|---|---|
| 规范符合性门 `check_status_spec`（50 条黄金用例） | **18/18 = 100%** |
| 穷举 oracle `check_status_exhaustive`（扩维后） | **6/18 = 33%** |

（分母从 15 涨到 18 是因为本轮新增三条约束各配了一个变异体；oracle 的击杀数没变，
所以比率从 40% 降到 33%——**这不是 oracle 变差了，是它的覆盖面被更诚实地量了一次**。）

### 四、R4 明确**没能**攻破的（同样重要）

- §7.2.2 的 PDF 结论：攻击者亲跑 `run_m0_1.sh`，四库数字与文档逐字相符，夹具字节确定、版本钉死。
  **这是全项目最硬的一条证据。**
- 台账数字除两处外全对（两处已更正：R3 引文的「1389 万」应为「550 万」；§四标题的「12 组」实列 9 组）。
- C-1b 的 meet/降一档 吸收态定义与实现逐点相符。
- W-04、§3.4 G1、V7.8、README 删假描述四条是真修好的。
- 「零行产品代码」这条自陈在 R4 交付时推不翻。〔S2 后已不成立，见文末〕

### 六、R4 机器层：空心的恰恰是我最引以为据的几处

**19 条发现 / 10 条 P1。** 全部亲手复现过，无一转述。

**① `touch` 五个字符打穿新鲜度断言。**
重跑的新鲜度判据原本比对 mtime，注释还自夸「这条是三者里唯一无法用『记录多写一个字段』
绕过的——它比对的是墙上时钟」。R4 把 command 写成 `touch <那个 2020 年的伪造文件>`：
mtime 被命令自己刷新，门打印「25/25 条哈希对上」并 PASS。

> **墙上时钟是被测方能写的。任何「事后观察某个属性」的判据，
> 只要那个属性在被测方的写权限内，就不是判据。**

改为**事前控制**：跑命令前先把 declared 目标移走（跑完归还，本门不破坏工作树）。
命令真产出它则重新出现且内容正确；`touch` 只得到空文件；什么都不做则文件不存在。
陈旧文件这条路被**物理切断**，而不是被检测。三种绕过变体实测全部判红。

**② 红样本是过定的 —— 修复其实一条都没被钉住。**
把新鲜度断言**整块停用**，负例套件仍 14/0。原因：R-11 的命令是个不存在的命令，
它因**退出码断言**先判红，证明的是另一条机制。同样地，「默认全跑」这条本轮最被强调的
修复（24/24 vs 1/11）**完全没有回归保护**——夹具只造一条可重跑证据，`rerunnable.length`
恒为 1，「全跑」与「抽一条」在夹具上**逐字节等价**，负例套件在结构上不可能区分二者。

现为 17/17，且每条修复都按「停用它，套件必须变红」逐条验证过：
停用事前控制 → 2 条红样本失败；改回恒定抽样 → R-13 失败。
**红样本必须证明它钉住的是自己声称的那条机制**，这条判据本轮补进负例套件的写法约定。

**③ 最深的一条：变异测试本身是循环的。**
R4 逐条比对后指出，手写的 15 个变异体与 33 条黄金用例是**同一个人按同一张清单写的
一对一映射**（M-01↔GC-2、M-02↔0e、M-03↔必填字段……无一例外）。
变异集只覆盖了黄金用例已覆盖的分支，**100% 是构造出来的，不是测出来的**；
R4 自建的独立变异集上击杀率 **2/10 = 20%**。

**这正是整套攻击机制要防的相关性错误，而它发生在专门用来度量辨别力的那道门里。**

已改：变异体按来源分三类并**分开报数**——`hand`（与黄金用例同源，打折看）、
`derived`（由 §3.4 / §1.5.2 / §7.3 三张表逐格机械派生）、`external`（R4 独立构造）。
**此后唯一可引用的数字是独立来源上的击杀率**，手写那部分不再单独引用。

同时补上 R4 指出的三张无绑定的表（本轮此前只给 §7.3 建了绑定）：
§3.4 等级表、§1.5.2 的 K 表、§8.6.2 的 tier 耦合，各自建立规范↔实现双向绑定；
黄金用例 33 → **66 条**。R4 那 5 个双门存活的独立变异体现已全部抓住。

**④ 退化变异体同样是不诚实。** 机械派生的第一版把「G5 的上限抬到 ST-V」也生成了——
而 G5 的上限**本来就是** ST-V，改了文本没改语义，必然「存活」。
退化变异体**虚高分母，让门看起来比实际差**，与虚高击杀率是同一枚硬币的两面。
生成器现在读实现的当前值，逐个跳过并在报告里列出被剔除的退化项。

**⑤ 规划文档之间的引用从来没被检查过。**
`if (PLAN_DOCS.includes(target)) continue`，注释写「规划文档内部互引，不由本门管」。
实证：往 05-TESTING 追加一条指向 01-CONTRACTS 的伪造锚，门退出码 0。
**那正是本门声称取代 D-1 的那一类，且发生在引用量最大的地方**
（01-CONTRACTS 是唯一规范源，被其余八份反复引用）。
修好后当场现形一条——**是我自己编造的**：本台账 §R1 引 `06-SURVEY.md` 的 `MAS-ProVe` 锚，
而该锚在整个语料里只出现在那条引用自身，真实出处是仓库外的 attacker skill。已改注为 `unverified`。

**⑥ 修复过程中我自己犯的两个。**
- 事前控制的代码被我放在 `execSync` **之后**：注释写「事前」，位置是事后，
  于是命令刚产出的文件被立刻移走，真实记录全部误红。
  **负例套件看不见这个 bug**——负例的命令本来就不产出文件，只有真实记录能暴露它。
- 在散文里把坏指针**当例子引用**，门分不清「使用」与「提及」，
  从 `[` `E` `:` 一直匹配到下一个 `]`。不给代码块开豁免（那会变成绕过通道），改写例子。

### 五、仍未关闭

- **机器层 R4 已回**（见 §六）。
- **R5 是必需的**：本节全部内容仍是修复。
- L1-c 的假阳/假阴率、X-2 的四个裁定阈值、槽类型 schema —— 全部零实现零实测。
- 台账初稿把这三组**从枚举里省掉**（标题写 12 组只列 9 组），已更正并单列。**选择性记账，不是笔误。**


### 七、R4 机器层第二波：excerpt 层的编造，与"分母被排除"的可解析率

**P1-7 · 现存最大的洞（已修）。** `excerpt` 与 `answer` 与重跑输出**完全无绑定**。
实证：把 M0-1 的结论改成与实测**完全相反**（"四个库全都拿不到颜色/字号/图层，H0 成立"），
`command` 与 `raw_output_sha256` 一字不动，门打印「**PASS，24/24 条哈希对上**」，退出码 0。

> 哈希证明的是**命令跑过**。而记录里唯一被人读、唯一驱动 `doc_action` 与文档改写的那部分，
> 不受任何约束。**R3 把编造从 verdict 层赶走，编造原样搬进了 excerpt 层。**

修法按 `excerpt` 的自述字面执行——它自称是「输出里承重的那几行逐字」：
**它的每一行都必须逐字出现在重跑输出里**（允许节选与重排，不允许出现输出里没有的行；
`〔…〕` 编者注按本仓库约定排除）。红样本 R-14 + 防过修绿样本 R-14b。

修的过程本身又暴露两件事：
- 门第一次运行就判红了**两条真实记录**——M0-1 与 M0-2 的 excerpt 是「轻度改写的摘要」而非逐字
  （实际输出是「H0 被证伪。**以下** 7 个……组合……**：**」，我写成了「H0 被证伪。7 个……数值**。**」）。
  **没有放宽门，改的是 excerpt。**
- 短内容行差点被误杀：`grep -c` 类命令的承重输出就是一个 `1`，
  初版的 `length >= 4` 把它判成「不含可核对内容」——**门在惩罚最干净的那种证据**，
  这是本项目第四次同类误报。

**P1-9 · 「不再静默丢弃」是假的（已修）。** 本门 R3 时宣称「`parsePointer` 不再静默丢弃」，
实测**仍在丢**：全 9 份文档 1635 个段里 **145 个（8.9%）** 落在「逗号续写省掉井号」这一形态上
被无声吞掉，而宣称的 92.2% 可解析率的分母正好把它们排除在外。

最刺眼的证据：同一个锚 `假独立佐证登记`，在 04-ORCHESTRATION 写成 `#假独立佐证登记` 被正常解析，
在 00-PREMISE 少个井号就被丢弃——**指针的可解析性取决于作者有没有敲那个符号**。

修后：指针总数 1487 → **1618**（131 个此前不可见的锚现形），
可解析率从 92.2% 诚实落到 **90.7%**，欠债重登记为 128 种 / 150 次。

**P2-13 · 自述数字门的已知边界（已修）。** oracle 向量数已涨到 **27,786,240**，
而 README / run_all / oracle 注释三处仍写「550 万」——**5 倍偏差而本门 PASS**，
因为它只认「N 份调研文件 / N 行 / N 条断言」三种句式。
现由 oracle 落 `gates/.oracle-stats.json` 工件、数字门读它比对；**工件缺失判红**
（第一版写成「缺失则跳过」，于是工件没生成、门照样 PASS ——本项目第二次踩空集）。

### 八、变异测试：退化变异体与"来源分档"的实效

按来源分档后**当场打出 3 个此前不可见的存活者**（`D-G5` / `D-K-K-D` / `D-K-K-L-T`），
全部落在「规范表机械派生」这一档——手写变异集在结构上永远照不到的地方。

**但这三个是退化变异体，不是真实通道**：G5 的上限本来就是 ST-V，K(K-D) 与 K(K-L-T) 本来就是 1，
「抬到 ST-V」「降到 1」什么也没改，必然「存活」。
退化变异体**虚高分母、让门看起来比实际差**，与虚高击杀率是同一枚硬币的两面。
生成器现在读实现的当前值逐个跳过，并在报告里列出被剔除项。

终值（剔除退化项后）：

| 来源 | 规范符合性门 | 穷举 oracle |
|---|---|---|
| 手写（与黄金用例同源，**打折看**） | 18/18 | 6/18 |
| 规范表机械派生（独立） | 11/11 | 2/11 |
| R4 独立攻击者构造（独立） | 2/2 | 0/2 |
| **独立来源合计（唯一可引用）** | **13/13** | **2/13** |

### 九、一条我必须认的方法论错误

**审计进行中我一直在改被审文件。** 攻击者三次观察到目标移动，
并逐字指出 `check_m0.mjs` 的新注释引用了它自己的实验输出（那个「25/25」就是它的屏幕）。
仓库里已有 `.attack/r1-target-hashes.json` / `r3-target-hashes.json` 这个冻结机制，**本轮没用**。
后果是它的一部分发现失去了确定的基线，不得不分「快照」与「当前树」两套结论重测。
**此后凡派出攻击者，必须先冻结目标哈希并把哈希文件交给它。**

### 十、攻击者给出的贯穿性判断（照录）

> 修复的方向基本都对，但有一个反复出现的模式：**每一处都把断言写对了，
> 却没有为那条断言配一个会红的负例**。新鲜度、默认全跑、三张规范表、excerpt 绑定，
> 全落在「有断言无红样本」或「有门无覆盖」同一个格子里。而本该量化辨别力的变异测试，
> 因为变异集与黄金用例同源，把这个格子涂成了绿的。

这是「门必须先证明会红才算数」这条教训的第四次复发，**而且这次发生在元层**——
负例套件与变异集本身没有先证明会红。
本轮的对策已落地：每条修复都按「停用它，套件必须变红」逐条验证过；
变异集按来源分档，只引用独立来源的数字。


## R5 · fix-audit（对 R4 修复轮的独立审计）

**27 条：15 P1 / 8 P2 / 4 P3。** 全部亲手复现过。R5 的总判词与 R4 相同——
「修复普遍是追加而非替换、只改一半」——**而且复发位置就在 R4 点名过那几处的邻格**。

### 一、重跑验证的上界（R5-01，接受而非修复）

R4 用 `touch`（5 字符）打穿旧的 mtime 判据，我改成事前控制；
R5 用 **`cp`（2 字符）** 打穿新判据——命令自己重造 declared 目标即可。
最彻底的变体是把被哈希的字符串**逐字写在 command 里**
（`printf '伪造内容' > "$TGT"; shasum …`）：命令确实确定性、哈希确实对上、
事前控制确实生效——**而它与测量毫无关系**。

> **重跑验证只能证明命令是确定性的，永远证不了输出是一次真实测量。**
> 这不是可以靠加判据修掉的洞，是这个机制本身的上界。同一条路换门牌号而已。

已写进 `check_m0.mjs` 的门头，并禁止报告暗示「哈希对上 = 记录属实」。
**真正把上界推高的是证据形态，不是门**：`gates/repro/` 那两个自包含确定性脚本，
R4 与 R5 两轮都攻不动（各连跑三次逐字节一致、脚本本身可读可审）。
⇒ 方法论定论：**承重证据应当是入库的自包含脚本，而不是一条一次性命令加一个哈希。**

### 二、我的报告在撒谎（R5-02，已修）

命令末尾加 `; rm -rf "$D"` 即可让整条证据**静默**退出重跑池：
§2 的结构检查用剥掉自清理的 `cmdSansCleanup` 放行，§3 的重跑池用**原始** command
测 DESTRUCTIVE 剔除，两套口径不一致就是一条通用豁免；而 R-9 的守卫不测 DESTRUCTIVE。

**真实树上已经生效**：带 64-hex 哈希的证据实际有 **26 条**，重跑池只有 24 条，
M0-4[0] 与 M0-5[2] 从未被验过——**而报告逐字打印「全部 24 条」**。
那个「全部」是假的。

修后 **26/26**。两条被解禁的证据当即判红，根因是它们把哈希打印到 stdout
而把被测内容留在临时目录里；已把命令末尾的 `shasum -a 256 <file>` 改成 `cat <file>`，
使哈希与 excerpt 两条断言同时成立。**带哈希却进不了重跑池的证据现在一律判红**——
那个哈希从未被任何人验过，它比「声明不可复现」更坏：后者至少诚实。

### 三、我上一轮新建的门，四处空心

| 断言 | R5 的证伪 | 修法 |
|---|---|---|
| §7.2.5「§7.3.1 的**每一个** flag」 | 规则句是全称的，表只抄了 §7.3.1——漏掉 §7.3 主表的 `F-34` / `F-28a`，裸置位 → verified | 补进驱动表 |
| 台账「15 条驱动逐条入实现、入黄金用例」 | 16 行逐行删除，**9 行门不判红**（8 行删后裸置位直接 verified） | 逐行补黄金用例；实测 **17/18 被钉住 + 1 行冗余**（F-10 被 §7.2.4 双向绑定覆盖），不谎称 18/18 |
| §8.6.2 ↔ 实现绑定 | 断言写作 `TIER_CEILING.C !== specGrade.G2`——比的是 §3.4，**根本没读 §8.6.2 的文本**；把 §8.6.2 改成「≤ G4」门仍绿。**R4 说「只绑了一张表」，我把 1 补到 2 却报成 3** | 改为真读 §8.6.2 文本 |
| §7.3.1「每个 flag 在实现中都有载体」 | 判据是 `!src.includes(id)`——**纯字符串包含**。删掉 F-33 的驱动行只留一行注释，门仍 PASS | 改为**行为判据**：裸置位必须使 S 偏离基线状态。注释改变不了行为 |

第四条尤其难堪：它的修复注旁边刚写完「主语与被读字段没有绑定，正是 F-10 那条 P1 的形状」，
**而它自己检验的恰恰是主语字符串在不在文件里**。

两条修复都已按 R5 的原攻击复验会红。

### 四、`check_m0` 不是 GC-0（R5-07，已更正）

门头第一行原写「GC-0：离线、确定性、零模型」。而 C-12a[2] 与 M0-3a[2] 的命令是
`dsh --profile headless "<自然语言 prompt>"`，即**真实模型调用**。
更微妙的是这两条的**全部输出是 2 字节 `1\n`**（sha256 = `4355a46b…`），
哈希匹配约携带 **1 bit** 信息——非确定性被命令末尾的 `grep -c` 洗成了确定性。
而它们正是 README 诚实声明里两条 M0 阻塞项的**唯一**闭合证据。
正确定级：结构检查是 GC-0，重跑验证的门类**取决于被重跑的命令**。

### 五、「追加而非替换」的三次复发（已全部销账）

- **W-04 没跟上**：`polarity_scope_passed` 成了必填合取项，而那份自称「S 消费的**全部**判定输入」
  的清单里没有它（且写「七个」列了八个）。没进 W-04 就**没有写者契约**——
  producer 直接填 `true` 即可拿 ST-V。现列九个谓词，写者一律指派为门代码。
- **03 的缺口表第一行**仍写「与规范源不一致，**这是一处真实的规范冲突**」，而 01 侧两处本轮都已改。
- **台账自己刚立的诚实性补丁失真了**：§四那张「另有 3 组零实现零门覆盖」表——
  正是 R4 更正「选择性记账，不是笔误」之后补的——第一行仍写「仅文档，grep 零命中」，
  **实测 22 行命中**。补的时候是真的，做完没回来销账。三组里现在只剩 §9.2 槽类型还开着。

### 六、R5 的五条「一实现就会崩」预测（S2 的输入）

这五条不是缺陷，是**对尚不存在的实现的证伪性预测**，逐条记下以便将来对账：

1. **V1.2 与 §8.6.2.1 现在就矛盾**（已落笔的冲突，非预测）。V1.2 说「重跑 S 逐字节相同，
   因为输入全在 CAS 与注册表快照里」；§8.6.2.1 说默认档 Tier B 下 `source_integrity` 的
   字节同一性**依赖重取、实测 3/5**，并直接得出「一条 Tier B 的 claim 会随时间衰减」。
   `source_integrity` 是 S 的必填输入 ⇒ V1.2 的括号理由为假。
   `src/status.mjs` 看着满足 V1.2，是因为它「所有输入由调用方备齐」——
   **纯函数性保住了，代价是把非确定性推给一个还不存在的调用方**。
   两道门都构造性站在 S 的输入侧，**没有任何门能看见这条**。
2. **L1-c 会被钳形夹死**。它守着产品**唯一**一条文献 ST-V 通道，实现落点为 0，标定集不存在。
   假阳偏高 ⇒ 合法转录降到 ST-A ⇒ §2.4 矩阵 K-L-T 那个 ✅ 兑现不了
   （**将是 C-1、C-6 之后同一失败模式的第三次**）；假阴偏高 ⇒ P1-C 原始构造复活。
   而 03 已自认一个结构性假阴洞且明说「没有被本条修复」：跨句极性
   （「我们检验了三种方法。**没有一种**达到 92%。」）——学术散文里最常见的否定形态。
3. **K(kind) ≥ 2 的独立性防线会在真实语料上塌**。G-CLUSTER 零实现、零金标集、零假合并率；
   而真实文献里独立簇被本项目自己记录过的三条机制系统性压塌（F-02 转引链、
   F-23 跨语言链——一手样本是 11 家中文媒体全部回溯到同一条 Nikkei Asia、F-22 自证回路），
   再叠预印本/会议/期刊三版同文。认真归并 ⇒ K-L-A 与 K-I 拿不到 2 簇；不认真 ⇒ K=2 名存实亡。
   **旁证已落笔**：本轮把 `nominal_source_count` 补进 W-04 的理由是「要与独立簇数并排展示」——
   要并排给用户看，本身就是在为「这两个数会差很多」做准备。
4. **§7.2.5 会在第一条真实记录上反噬**。它自己的诊断是「这些 flag 是**输出标注**」，
   那么真实管线就会在门判出 `contaminated` 后贴 F-05，两者一起写进 status.json；
   复核期重跑时只要 `source_integrity` 变了（§8.6.2.1 已证明它**会**变），
   驱动条件不成立 ⇒ ST-N，而 §7.3.1 承诺 ST-C。
   〔本轮已补两面黄金用例：driver 成立时走 §7.3.1 承诺的那一步，不成立时 fail-closed。〕
5. **fail-closed 的稳态是全 ST-N，而唯一的出口本轮刚被留着。**
   10 个必填字段 + 9 个谓词 + 18 条驱动一致性 + 5 条前置否决，任一处不一致即 ST-N。
   第一个真实 run 几乎必然压倒性 `not_covered`。压力会指向最省力的出口：
   **`question_frozen` / `rerun_gate_passed` / `anchor_containment_passed` /
   `polarity_scope_passed` / `inference_gate_passed` / `attribution_verdict`
   六个纯自报谓词，一条 deny 规则都没有**（只有 `status` 与 `sub_mode` 有）。
   把 `counter_evidence_searched` 与 `polarity_scope_passed` 默认写 true，绿灯就回来了，
   **而没有任何门能发现**——门读的是记录，记录是自报的。
   这正是 §7.2.5 对 flags 做的诊断在**谓词**上的完整复制，而修复只覆盖了 flags 那一半。
   §7.2.3 那句「逃不过的只有这一条」会在第一次真实 run 的第二周失效。

**第 5 条是 S2 的头号设计约束**：进产品代码前必须先给这六个谓词配写者契约与 deny 规则，
否则整套 fail-closed 会在第一次真实压力下从最软的地方漏光。

### 七、R5 没能攻破的

- `gates/repro/` 两份脚本（第三轮攻不动，各跑三次逐字节一致）。
- check_pointers 的规划文档互引分支本身有牙齿（基线绿 / 伪造锚红 / 真实锚绿）。
- R-13 / R-13b 这一对是真的（夹具造两条可重跑证据，`--sample 0` 放行、全跑判红，两侧实测）。
- §7.2.4（F-10 ⟺ chart_extracted）是唯一一条双向都有黄金用例、删掉立刻判红的绑定。
- meet / stepDown 的六元素集定义与 §1.5.1.1 逐点相符；oracle 的五条性质断言真跑真绿。
- 「零行产品代码」这条自陈**在 R5 交付时仍成立**；此后 S2 已建 profile 骨架与 `check_profile.mjs`。


## S2 · 产品层（「零行产品代码」这条自陈到此作废）

R4 与 R5 都逐字确认过「零行产品代码」推不翻。本节记录它是怎么不成立的，
以及**新代码从第一行起就是按前五轮的结论写的**。

### 一、R5 第 5 条预测是 S2 的第一件事，不是最后一件

R5 预测：六个纯自报谓词是 fail-closed 唯一的出口，第一次真实 run 压倒性 `not_covered`
之后压力会指向它们，而**没有任何门能发现**。

所以产品代码的第一块不是业务逻辑，是 `src/writer-contract.mjs`：
把 §4 的 W-01..W-16 编码成可执行的所有权判定，配 `denyProducerSubmission()`
（**白名单，不是黑名单**——黑名单在加字段时静默失守，白名单失守的方向是红得响亮）。

`gates/check_writer_contract.mjs` 守的是一条结构不变式，而不是「有没有写 deny 规则」：

> ∀ f ∈ S 读取的字段：`FIELD_OWNER[f]` 存在，且 ≠ producer

**读集从 `src/status.mjs` 的源码自动提取**，因此「新加一个 S 读的字段却忘了配主人」
会判红，而不是静默扩大自报面。7 条负例逐条钉住（含空集闸）。

**`kind` 是唯一例外，且例外本身是可机器检验的论证**：它必须由 producer 声明
（那是 claim 的内容 W-03），而它不构成通行证，是因为**每种 kind 的特权都由门谓词把守**——
`KIND_GATED_BY` 把这条论证写成表，门逐条断言把关谓词存在、是门代码字段、且真的被 S 读。

### 二、L1-c 从「零实现的承重件」变成有标定集的门

R5 第 2 条预测：L1-c 守着产品**唯一**一条文献 ST-V 通道，实现落点为 0，标定集不存在。

`src/gates/g-polarity.mjs` 按 03 §3.1.1.2 实现了四类受控算子表 + 保守作用域，
`gates/check_polarity.mjs` 是标定集的第一版——**两侧各有样本**：
假阴侧 6 条（P1-C 中英构造、NEG-C/Q/R 各一）、假阳侧 5 条（肯定句、载荷自带否定、
否定在别的子句、英文绿控、`band` 内含 `and` 不得误判）。
只测一侧的门会朝那一侧漂，而两侧漂移恰是 R5 说的钳形夹的两个爪。

**实现期发现了一条规范缺陷并回改了 03**：规范的作用域规则四类算子统一为
「算子起点到**子句**结束边界」，但 NEG-C 的语义是限定**后件**，
而中文最常见的条件句是「如果……，主句」——从句以逗号收尾，
于是被限定的主句正好落在作用域**外**。实测 `如果使用更大的批量，该方法可达 92%。`
取载荷 `92%` 时 L1-c 通过。现 NEG-C 的作用域延到句末，由 NT-L-34 钉住。

**已认账的假阴保持可见**：跨句极性（「我们检验了三种方法。没有一种达到 92%。」）
L1-c 看不见，`knownLimitation` 每次运行都把它打出来——
一条被静默掉的已知缺陷会在下一次重构里被当成「本来就该这样」。

### 三、§1.2.2 有了两个独立实现，并互为证据

`src/normalize.mjs`（JS，产品）与 `gates/repro/m0_2_normalization.py`（Python，S0 复现）
是同一算法的两个独立实现。`gates/check_normalize_parity.mjs` 断言两者逐格一致，
**且用例从 Python 侧提取**——由已冻结的 S0 结论出题，不是 JS 自己出题。

> 单一实现的自洽不构成证据（恒返回 pass 的实现同样自洽）；
> 两个独立实现在同一组用例上逐格一致，才是。

### 四、端到端管线：P1-C 在产品层闭合

`src/pipeline.mjs` 是第一条端到端链路：producer 提交 → GC-0 门链 → 派生字段 → S → status.json。
`gates/check_pipeline_e2e.mjs` 12 条断言，其中三条是产品主张的直接检验：

| 用例 | 结果 | 意义 |
|---|---|---|
| 合法转录 | `verified` | §2.4 矩阵 K-L-T 的 ✅ 不是空头支票 |
| **从否定句里取 92%** | **`attributed`** | **P1-C 在产品层闭合**，带 `payload_in_negated_scope` caveat |
| producer 夹带 `polarity_scope_passed: true` | **拒绝** | R5 第 5 条预测的出口被堵 |
| 没做反证检索 | `not_covered` | §7.2.3「逃不过的只有这一条」真的逃不过 |

`gate_report` 带 W-08 要求的自证签名（`generator_version` + `inputs_hash`），
并断言同输入两次运行的 `inputs_hash` 相同——前代四份 gate_report 无法复核正是因为缺这个。

### 五、实现期自己犯的两个（都被自己的门抓住）

- **转抄不是书写，可两者在记录里长得一样。** 管线初版把「自己算的」与「从别的写者接住的」
  混在一起交给 `denyGateWrite` 自检，于是 `budget_state`（编排层 W-13）被判成
  「门在写它不拥有的字段」。那是误报，但它指出了真问题——现在两者分开，
  且转抄字段逐一标注来源写进 `provided_by`，让「这个值是谁写的」在代码里可读。
- **不输出的检查等于不存在。** 写者契约门的「字段有主」检查因为 `kind` 是带论证的例外，
  条件写错后**既不 PASS 也不 FAIL，什么都不打印**。

### 六、S2 当前状态〔R6-10 判定：本节数字在被引用时已全部作废〕

> **下面这段是历史记录，写于 S2 收尾，此后没有跟着树更新。R6 逐条核出它错在哪里，
> 原文保留不改，因为改掉它就等于抹掉「台账会腐」这条本项目自己反复宣称的教训的实证。**
>
> R6-10 的核对结果：
> - 台账写「产品代码 1046 行、门 14 道」，冻结态实为 **1683 行 / 20 道门**（R6 结束时 1927 行 / 22 道门）；
> - 「仍未做」列的四项（G-CTR-SCAN、G-CLUSTER 真实归并、抓取工具与 `data.meta` 锚点、组稿器）
>   在冻结时**全部已在文件表内**；
> - 台账**根本没有 S3 节**，而任务书要求核的「20 门 / 1641 行」这两个 S3 数字在冻结的台账里不存在。
>
> 攻击者的判词值得逐字留下：一个以「数字落笔即验证」为卖点的项目，
> 自己的台账数字对不上，是最伤的一类缺陷。`gates/check_doc_metrics.mjs` 守着 README，
> **不守台账**——这道缺口在 S4 里补上了（见 §S4 五）。

产品代码 1046 行（`src/` 5 个模块 + `profile/`），门 14 道全绿，
负例：M0 19 条 / 指针 12 条 / 写者契约 7 条 / L1-c 11 条 / 端到端 12 条 / 黄金用例 82 条。

**仍未做**：G-CTR-SCAN（X-1/X-2 的实现）、G-CLUSTER 的真实归并（现为按 key 去重的最小实现，
R5 第 3 条预测未被触碰）、抓取工具与 `data.meta` 证据锚点、组稿器、
以及 R5 第 1 条预测（V1.2 与 §8.6.2.1 的冲突）——那条**没有任何门能看见**，
两道门都构造性站在 S 的输入侧。

---

## S3 · 自攻（2026-08-18）

对 `src/gates/g-polarity.mjs` 的受控算子表施加 11 种否定/限定形态，载荷固定取 `92%`。
机器可读全量：`.attack/self-attack-s3.json`。

| # | 轴 | 一句话 | 处置 |
|---|---|---|---|
| SA-1 | not-closed | NEG-S 表是有限枚举，8 种常见形态直通 ST-V | **已修**：补 7 词 + 新增 NEG-L/NEG-I 两类，标定集 11 → 18 |
| SA-2 | new-defect | 假阳：双重否定被误拦（「不是没有达到 92%」） | **已修**：同子句 NEG-S 偶数次相消 |
| SA-3 | not-machine-decidable | 词表是有限枚举这件事**没有任何门守着** | **未修，认账** |
| SA-4 | gate-hollow | 组稿器只认阿拉伯数字，中文/全角数字绕过整条证据链 | **已修**：NFKC + 中文数字类，标定集 9 → 13 |
| SA-5 | not-closed | G-CLUSTER 只按**自陈**关系归并，不自陈就是独立簇 | **未修，认账**（S4 部分闭合） |
| SA-6 | withdrawn | 〔已撤回〕先写后跑，写错了 | 撤回 |

**本轮唯一一条先写后跑的断言，也是唯一一条错的。** SA-6 初稿断言「同文标题微调后 exact 合并失效」，
实测相反：标题微调仍被合并成 1 簇，而同作者不同论文正确地**未**合并。
在一份讲诚实的记录里留一条没跑就写的断言，比不写更坏——所以撤回，并把这条教训留在这里：
**自攻同样要先跑再写。**

修 SA-2 的过程中，**该修复自己制造了一个新假阴**：`falls short of` 与 `short of` 是同一短语的
两次重叠匹配，被相消规则当成双重否定放行。由 NT-L-43 抓住。修复引入缺陷这件事在本轮又发生了一次。

**独立性交代**：S3 是自攻，攻击者与实现者是同一心智，独立性只到 `instance` 级。
它找到的洞是「同一个人换个角度也能看见」的那一类；R6 随后证明这个级别的独立性
**系统性地看不见**真正要命的那一类——见下节。

---

## S4 · R6 裁决与修复（2026-08-18）

### 一、R6 是什么

一次真正独立的攻击：攻击者未参与任何实现，靶标为 53 文件的冻结哈希清单
（`.attack/r6-target-hashes.json`），findings 全量在 `.attack/r6-findings.json`。
**15 条**：P1 8 / P2 5 / P3 1 / INFO 1，另有 4 条 flags、5 条 coverage_gaps。

### 二、它对核心承诺的裁决：**假**

> 「从抓取到成稿，中间没有任何一步允许 agent 直接写结论。」

这句话印在 `README.md`、`src/run.mjs` 的文件头、以及 `gates/check_full_chain.mjs` 的门名里。
R6 给了**三条互相独立的反例路径**，其中最短的一条不需要后门、不需要伪造证据、不触发任何门：

| # | 路径 | 形态 |
|---|---|---|
| R6-01 | `__` 前缀后门 | `__rerun/__frozen/__inference/__attribution` 先被读进 ctx、**再**从提交里删掉，写者契约白名单从未见过它们。落盘的 claim 已剥干净，`status.json` 的 `provided_by` 还把这些值标注成 `gate(W-04)`——不只是绕过，是**洗白** |
| R6-02 | fail-open 缺省 | 六个把关谓词里四个是 `?? true`；`src/research.mjs` 更直接写字面量 `true`。`KIND_GATED_BY['K-D']` 的两个谓词**没有任何门在算** |
| R6-03 | payload 遮蔽 | `{...statusRecord, ...payload}` 的展开顺序——producer 把 payload 槽命名为 `status` 即改写读者看到的标记。S 判 unverified，成稿印「已验证，来源 9/独立簇 9」 |

而 R6-09 说清了为什么 22 道门全绿时这三条同时成立：
**`check_writer_contract` 的不变式只作用在 `status.mjs` 上，对 S 的供给方完全失明。**
读集从 `status.mjs` 源码正则提取，门从不看 `run.mjs` / `research.mjs`；
`denyProducerSubmission` 只看 `submission`，从不看 `ctx`。
于是「谁写这个字段」的证明链**在管线入口就断了**，`PROVIDED_BY` 那张表标注的写者是**注释**，
不是被任何东西检验的事实。

当时真正成立的，是一句弱得多的话：**在 `status.mjs` 内部，S 不读 producer 可写的字段**。
这条被真正钉住了——但它离核心承诺差着整个供给侧，而**没有一道门站在那一侧**。

### 三、逐条处置

| # | 严重度 | 一句话 | 处置 |
|---|---|---|---|
| R6-01 | P1 | `__` 前缀后门 | **已修**：`assertNoProducerContamination` 锚在**原始对象**上、发生在读取之前；`src/` 内禁止任何 `__` 字段访问，由门静态断言 |
| R6-02 | P1 | 四个谓词是 fail-open 缺省 | **已修**：新建四道门**真的去算**——`g-rerun`（从 CAS 取脚本执行两次比对摘要）、`g-freeze`（问题哈希 + 冻结时刻早于每条证据的 `retrieved_at`）、`g-inference`（前提须为本 run 已达 ST-A 的 claim、不自指）、`g-attribution`（缺省 `unclear` 而非 `support`） |
| R6-03 | P1 | payload 遮蔽状态标记 | **已修**：`sealStatus` 让门字段压过 payload，且冲突**当场抛**；`denyProducerSubmission` 再堵一层 |
| R6-04 | P1 | L1-c 有限枚举对开放类，扩表追不上构造 | **部分修**：新增 NEG-P **前置位白名单**（fail-closed），见下 |
| R6-05 | P1 | 逐字节相同的快照算作 2 个独立簇 | **已修**：归并规则⓪按 `object_sha256`；管线补递 doi/title/authors/self_cite_group |
| R6-06 | P1 | 求确证的 query 被记作反证检索 | **已修**：`replicated/reproduced` 移出 NEG-LIT 进中性表（允许出现 ≠ 构成反证）；新增 (e′)：声称找到就必须抓过它 |
| R6-07 | P2 | 组稿器两条区间豁免放行承重数字 | **已修**：豁免改为**自证身份**（年份须带「年」或括号；序号须带「第」/Table） |
| R6-08 | P1 | `source_integrity` 只核第一条证据 | **已修**：逐条核，取最坏值（完整性是合取，不是抽样） |
| R6-09 | P1 | 写者契约门对供给侧失明 | **已修**：新建 `gates/check_supply_contract.mjs`，结构层 4 条 + 行为层 6 条反例 + 1 条绿控 |
| R6-10 | P2 | 台账无 S3 节、数字全部作废 | **已修**：本节 + §S2 六的作废声明 + 台账数字进门（见五） |
| R6-11 | P3 | L1-c 门的分侧计数硬编码（6+5≠18） | **已修**：改为动态计数 |
| R6-12 | P1 | **取证插件从未接进 profile** | **已修**：补 `lib/index.js` + `dsh.bundle` 键 + `install.sh` 打包安装；`--dump-config` 现含 `tool-academic-fetch` |
| R6-13 | P2 | patch 注释点名一道**不存在**的门 | **已修**：改指 `check_profile.mjs`，并把三条 boot 路径的实测差异写进注释 |
| R6-14 | P2 | V1.2 与 §8.6.2.1 互斥，无门可见 | **改为条件式 + 新建 `check_retention.mjs`**，见下 |
| R6-15 | INFO | 「没能攻破」：证据层是真的 | 无需处置，见六 |

### 四、两条修不干净的，说清楚修到哪

**R6-04（L1-c）。** 攻击者的判词是对的，而且它对**任何**黑名单都成立：

> 扩表的速度追不上构造的速度。

所以补词表只是止血。真正的动作是**换方向**：在最危险的那一个句法位置——
紧邻数值载荷之前的那个词——把默认从「不在黑名单就放行」翻成「不在白名单就判红」。
`less than 92%` / `at most 92%` / `below 92%` / `低于 92%` / `不足 92%` 全是同一个位置在改写同一个数字。
翻转之后性质变了：自然语言新增一种否定形态，从「一条免费的 ST-V 直通车」变成「一次降级」。

**仍然开着的**：前置位之外的改写（后置的 `, but this was later retracted`、跨句否定）
不在其内。整句范围仍然靠黑名单，SA-3「词表是有限枚举这件事没有门守着」原样成立。

**R6-14（V1.2）。** 这条**不能**修——留存到期是物理事实。能做的是把那句话的**前提**变成可检验的：

> V1.2 成立 ⟺ 该 claim 引用的每一个 CAS 对象仍在场且字节未变。

`01-CONTRACTS` 的 V1.2 已改写成条件式并注明无条件形式为假；`gates/check_retention.mjs` 逐条核前提，
并在删掉 CAS 对象时点名哪张卡失去了字节。**一条看得见的衰减，和一条看不见的，是两件事。**
它仍然覆盖不到跨时间、跨机器的重跑——门每次运行都把这句话打出来。

### 五、程序性问题：靶标连续第三轮在审计期间移动

R6 的 coverage_gaps 第一条原文：开始前 53/53 哈希一致，审计中作者实时改了 5 个冻结文件、新增 4 个文件。
`src/orchestrator.mjs` 与 `src/research.mjs` 是在 R6 进行中出现的，而 **`research.mjs` 把 R6-02 / R6-03 / R6-08
三条缺陷逐字复制了一遍**。攻击者的结论：

> 这说明缺陷是结构性的，不是笔误。

这句判断直接决定了修法：不是在两个文件里各改一遍，而是让**两个文件都无法自己造 ctx**
（`src/gate-ctx.mjs`），并由门断言这一点。

R6-04 / R6-07 的**冻结态**数字因移动而失去基线；攻击者对两态各跑一遍并都记在案，
本台账采用当前态数字，冻结态数字在 `.attack/r6-findings.json` 里原样保留。
**这是第三次了**，纪律没有靠自觉建立起来；`.attack/r6-target-hashes.json` 存在但没有任何东西强制它。

台账自身的数字此后进门：`gates/check_doc_metrics.mjs` 此前只守 README，
R6-10 正是从这条缝里进的。

### 六、R6 没能攻破的（同样是记录）

- **证据层是真的。** `gates/repro/m0_1` 与 `m0_2` 连跑三次输出哈希逐字节相同；
  `check_m0` 的 26 条证据全部重跑且哈希一致；`check_normalize_parity` 的 JS/Python 双实现逐格一致，
  **且用例从 Python 侧出题**。这三块经得起「重跑对哈希」。
- **`status.mjs` 内部。** S 确实不读 producer 可写字段（`kind` 除外），
  `check_writer_contract` 的读集自动提取 + 空集闸真的钉住了这条。
- **负例套件不是空心门。** M0 19 条 / 指针 12 条 / 契约 7 条逐条判红。

### 七、S3 与 R6 的重合度是一条数据

S3 自攻找到 6 条，R6 独立攻击找到 15 条。**重合的只有两条**（SA-1/SA-3 ≈ R6-04，SA-5 ≈ R6-05 的一部分），
而 R6 的 8 条 P1 里有 **6 条 S3 完全没看见**——全部集中在供给侧（R6-01/02/03/09）与接线（R6-12/13）。

这正好是 attacker 方法论说的那件事的实测：同一心智的自攻，其盲区在模型层面**系统性不可见**。
S3 一整轮都在攻自己刚写的词表，而没有想到去问「造 ctx 的那段代码算不算门的一部分」——
因为那段代码是我自己写的，我知道它「想」做什么。R6 不知道，所以它去读了。


## S5 · 外部标定测试（2026-08-19）

前六轮全部是**内部**攻击：攻击者读我们的文档、攻击我们的实现，但题目自始至终
由我们出。03 §5A.0 一直自认 `power_basis = unmeasured`，说的就是这件事。

这一轮换了对象：三个真实研究话题、六份真实文献的逐字快照、13 条 claim，
每一条都取自这些数字在**现实世界里真实被引用的方式**。
产物在 `tests/external/`，`node tests/external/cases.mjs` 可重跑。

### 一 · 头条：13 条里 0 条到达 `verified`

跑真实文献能达到的最高状态是 `attributed`。§3.4 的天花板要求 G5
（全文 + JATS/TEI 结构化锚），而六份真实文献一份都给不出——
Europe PMC 给摘要（G3），Nature 那篇是 PDF 抽取的全文但无稳定锚（G4）。

**这就是与正式发表论文之间最大的那道差距**：论文直接把数字当事实陈述，
我们跑同样的数字最好只能标「已归因」。缺的不是算法，是真正的 API 集成。
这条不能靠再加门解决，写在这里以免下一轮又去加门。

### 二 · 抓住的三条（都是现实世界的普遍写法）

| | 现实世界怎么写 | 我们判 |
|---|---|---|
| T1-1 | 「median GDT_TS 92.4 (Jumper et al., *Nature* 2021)」 | `unverified`——该数字在 Nature 全文 **0 次命中**，出自 *Proteins* 那篇 |
| T2-3 | 声称 36%，锚句取的是 47% 那一句 | `unverified`——判据与数字错配 |
| T3-1 | 「$2.6 billion (DiMasi 2016)」 | `unverified`——原文写的是 `$2558 million` |

T1-1 不是构造出来的攻击：第一次 WebSearch 时，**搜索引擎返回的摘要本身**
就是这么写的。

### 三 · 首轮判错的四条，及它们各自暴露的结构问题

| 用例 | 首轮 | 根因 |
|---|---|---|
| T2-1 `36` vs 原文 `Thirty-six` | 假阴 | 包含判定是裸 `includes` |
| T2-2 `replication` vs 原文 `replicated` | 假阴 | 同上 |
| T2-4 「只有 36% 的心理学研究可复现」 | 假阳 | **没有任何门看框架** |
| T3-3 三篇「独立」来源 | 假阳 | **只检查 `evidence[0]`** |
| T3-4 否定在下一句 | 假阳 | 跨句极性只报告不判定，且输入来自生产方 |

**T2-1/T2-2 的代价不是「保守一点」。** 合法转录被拦掉，作者会去改 payload
迎合门，最后写出的是门喜欢的句子，不是准确的句子。一条把正确写法也拦掉的
规则，会把人推向错误写法——这句话在这一轮出现了两次（另一次是组稿器的 E-3）。

**T3-4 的结构问题比它本身重。** `followingSentence` 原先由抓取记录递进来
＝由生产方控制。跨句极性一旦参与判定，它就是判定输入，而生产方递个空串
就能让转折否定消失。这是 R6-01/02 的同族洞，长在一个 R6 没查的字段上。
现改为由门从快照算，`check_supply_contract` 的 B-7 断言：
递空串、递伪造句、递真话，必须得到同一个结果。

**T3-3 暴露的是同一个「只核第一条」家族的第三次复发**（R6-08 是完整性，
这次是锚点与极性）。现在每条证据都要自证支持，不支持的不计入独立簇，
但**仍留在名义来源数里**——「引了三篇只有一篇支持」不能看起来跟
「只引了一篇」一样。现为 `来源 3/独立簇 1`。

### 四 · 顺带找到的三条产品缺陷

- **E-1（P1）** `evidence_grade` 归门代码所有（W-04 写着），却**没有任何门在算它**：
  `buildGateCtx` 从抓取记录读，缺省 `?? 'G5'` —— **最高档**。
  真实抓取工具 `academic_fetch` 根本不设这个字段 ⇒ 真实链路上天花板恒为 ST-V。
  R6-02 的同族形态，**七轮攻击都没找到，被一次外部数据跑出来了**。
  → 新建 `src/gates/g-grade.mjs`，按 §3.3 从 `content_kind` + `locator` 算，未声明按 G3。
- **E-2（P1）** 多条证据取 `Math.max`（最好那条），而同一行注释写着「取全体**最坏**值」。
  代码与自己的注释相反。→ 改 `Math.min`，配回归。
- **E-3（P2）** 组稿器把 `{{claim:c1.cost}} 十亿美元` 里的「十亿美元」判成裸数字——
  那是**单位**。中文量纲写法根本写不出来。

### 五 · 新增的门与合取项

- `src/gates/g-containment.mjs` —— 按 slot_type 分档放松：value/comparator 认
  **数值等价**（英文数词 / 中文数字 / percent 统一），entity 认**同词干**，
  metric/sample **一律逐字**（T2-3 正是靠它们拦住的）。
  放松是 fail-open 方向，所以负例 11 条多于正例 6 条。
- `src/gates/g-frame.mjs` —— **第一道不看转录、只看框架的门**。
  它不宣称能判断框架对不对（GC-0 做不到），只认一个可机器判定的指纹：
  原文在同一句里分号并列多个同量纲读数。触发后要求 `discriminator`：
  一段逐字取自锚句、且不出现在任何兄弟读数里的文字，两侧都是字符串包含。
  它成为 **K-L-T 的第三个合取项**——§2.2.1 / §1.5 / W-04 谓词表（九→十）/
  黄金向量 / 穷举 oracle 全部同步。

### 六 · 这一轮我自己犯的错（三次，全部由本仓库的机制抓住）

1. **G-FRAME 的量纲判定只认 `%` 字面量**，于是 “Thirty-six percent” 与兄弟句的
   “47%” 不同类，**整道门在它本该触发的那个用例上静默放行**。
   一道在自己的目标用例上不触发的门，等于不存在。由它自己的标定集抓住。
2. **兄弟读数范围取整篇正文**，在全文快照上必然误伤；收窄到锚句所在那一句后
   仍有 bug：锚句自己以句号收尾时向后扫描没停。由负例 F-6 抓住。
3. **M-18 的变异锚点被这次改动打断**（那一行多了一个合取项），
   变异器于是静默地少跑一个变异体、击杀率虚高。
   抓住它的是变异测试自己的锚点唯一性检查——**承重的度量工具必须先能证明自己没坏**。

### 六之二 · 变异测试的最终数字

补完两条黄金向量后重跑（32 个变异体 × 5557 万向量）：

```
规范符合性门 check_status_spec：合计击杀 32/32（100%）
    手写（与黄金用例同源，**打折看**）：19/19
    规范表机械派生（独立）：11/11
    R4 独立攻击者构造（独立）：2/2
穷举 oracle  check_status_exhaustive：合计击杀 8/32（25%）
```

**唯一可引用的数字仍是「独立来源 13/13」**——手写那部分与黄金用例同源，
其击杀率不构成辨别力的证据（R4 判词，照旧生效）。

新增的 M-19（去掉框架检验）现在被杀，且是被本轮新补的那两条向量杀的——
红得是为了它们自己那个理由。

### 七 · 空心门，第四次

给 S 加了第三个合取项后，我把**全部**黄金向量的 `frame_gate_passed` 一律填成
`true`。于是没有任何一条向量能区分这个合取项在不在，
新增的变异体 **M-19（去掉框架检验）在规范符合性门下存活**。

约束进了 S、进了规范、进了 W-04，黄金集里却没有一条案例能证明它起作用。
补了两条向量（框架不过 + 1 簇 → ST-U；框架不过 + 2 簇 → ST-A）后 M-19 被杀，
且两条各自判红一次——红得是为了它们自己那个理由。

**这是本仓库第四次栽在空心门上。** 前三次的教训写在 `hollow-gate-lesson`，
这一次说明那条教训还没有变成肌肉记忆：**新增一条约束时，同时新增一条能证明
它起作用的样本**，不是可选步骤。

### 八 · 仍然没有闭合的

- G-FRAME 只看分号枚举。**没有这个指纹的框架塌陷，它看不见。**
  「36% 的研究可复现」若写在一篇只给了 36% 一个数的综述里，这里全绿。
- L1-c 的词表、转折词表，仍是有限枚举（SA-3 认账的那条边界照旧）。
- 跨句极性只认**转折句首**。非转折句首的否定仍只报告不判定。
- ~~反证检索的 X-2 判据在自然语言 query 上判红 2/3~~ **已修，但通过率没变，见下。**
- G-CLUSTER 只按自陈关系与内容哈希归并（SA-5 照旧）。
- 13 条 claim 的语料仍然只有六份文献、三个话题。**它证明的是「能找到东西」，
  不是「覆盖了什么」。**


## S6 · X-2 判据的双侧重构（2026-08-19）

§S5 八列的第一条未修项。它是这一轮唯一一条**真实用户一用就会撞上**的缺陷——
其余几条都在攻击面，这条在可用性面。

### 一 · 原判据错在哪

两条边界本身是对的：下界防「泛泛一句 refute 就算做过反证检索」，
上界防「堆词保证零命中」。错的是**把一张受控词表当成了那条上界**：

- (a′) 要求**每一个**锚槽字面出现在 query 里。而 `metric_frame` 常写中文
  （「研发成本」），query 写英文——同一个东西在两侧的措辞永远对不上。
- (b′) 把 `drug` `development` `cost` `psychology` `rate` 判成越界 token。
  这些词就写在被引的那篇原文里。

### 二 · 改成什么

**词汇的合法性锚到 claim 自己的材料上，而不是一张词表。** 两条边界**取材不同**：

- 下界 (a′)：只看 载荷 ∪ 槽值 ∪ **锚句**。
  它要证明的是「这条 query 是关于**这条 claim** 的」——
  若把整篇快照算进来，一条关于同一篇论文里**另一个数**的 query 也会通过，下界就废了。
  其中 **实体槽仍是硬要求**：删掉「AlphaFold」的检索会捞回别的系统的结果，
  那不是这条 claim 的反证（标定用例 X-8 守着这一条，它在改造中途红过一次）。
  松只给 `metric_frame`，由「落在材料上的 ≥2 个 token」代偿。
- 上界 (b′)：额外把**整篇快照**算进来。它要挡的是无中生有的填充，
  而写在原文里的词不是无中生有。

两侧都按**词干**比，不按字面（`drug` vs 原文 `drugs`）——
与锚点包含门那边同一个理由。

标定集 10 → 15 条，新增五条全部围绕跨语言与代偿的边界。

### 三 · 诚实的度量：通过率没变

首轮那三条人会写的 query，修复后重测仍是 **1/3**：

| query | 修复前 | 修复后 | 现在的理由 |
|---|---|---|---|
| `AlphaFold CASP14 GDT_TS refute` | PASS | PASS | — |
| `psychology replication rate refute` | FAIL | FAIL | 见下 |
| `drug development cost refute` | FAIL | FAIL | 见下 |

两条原因，必须分开说：

1. **(b′) 那一半确实修好了，但本仓库的夹具看不见。**
   为版权裁剪快照时，恰好把 `psychology` / `drug` / `development` 所在的那句删了
   （见 `tests/external/snapshots/SOURCES.md`）。用未裁剪的原句实测，
   `drug development cost $2558 million refute` 与
   `new drug development cost estimate disputed` **都通过**。
   夹具让改进看不见，不等于改进不存在——但也不能反过来拿它当已修的证据，
   所以标定门里那两条（X-11/X-12）用的是完整的原句。
2. **(a′) 那一半是故意不让它过的。**
   `drug development cost refute` 不指明挑战的是哪个数——
   DiMasi 那篇给了 $1395M / $2558M / $2870M **三个**成本数。
   这与 G-FRAME 要求 `discriminator` 是同一条道理：
   **说清楚你说的是哪一个读数。** 把下界降到「1 个锚」会让 `cost refute` 通过，
   那条下界就废了，所以不降。

**结论一句话**：真正被修的是「词表把原文里的词判成越界」，
没被修的是「query 不指明对象」——后者不是缺陷。

### 四 · 顺带

`check_ctr_scan.mjs` 的汇总行里「假阳侧 4 / 假阴侧 6」是**硬编码**的，
补到 15 条后 4+6 ≠ 15。一条自述数字与实测不符，
而它就长在检查别人数字的那道门里。已改成由用例表算出来。


## S7 · 两条未闭合项的推进（2026-08-19）

§S5 八列了六条未闭合。X-2 那条在 §S6 处理完，这一节是另外两条。

### 一 · G-FRAME 的适用范围：`;` → 同句内的一切并列

初版只认分号。分号枚举确实是学术摘要里「同一个量的多个读法」最标准的写法，
但不是唯一的：

- `36%, 47%, 39% across the three criteria` —— 逗号枚举
- `92% precision and 87% recall` —— `and` 并列

后者尤其该触发：一条 claim 若写「模型达到 92%」并锚在这句上，
**它确实该说清楚是哪个指标**。现在按 `;` `，` `,` ` and ` ` 与 ` 一起切，
仍然限定在锚句所在的那一句内。标定集 9 → 13 条。

修这条时踩了一次：为了不让置信区间的上下界被当成竞争读数，
初版改成「整个子句含 CI 就整条排除」——于是
`47% of original effect sizes were in the 95% confidence interval …`
这条**合法的**竞争读数也被整条丢掉（F-3 由此红过一次）。
正确的粒度是**只抹掉区间那个括号**，句子的其余部分照常参与判定。

### 二 · G-CLUSTER 的近似归并（SA-5 的第二步）

⓪ 只抓逐字节相同。真实转引链里，二手来源几乎总会改一两个词——
一个字节之差哈希就分开，11 家转载又变成 11 个独立簇。

新增 ⓪-b：**5 字符 shingle 的 Jaccard 重合度 ≥ 0.82**。
确定性、离线、可复算，不需要任何自陈字段，producer 也伪造不了
（要伪造就得真的把正文改得不像）。

它与 ⓪ 有一个关键差别：**它可能错并**。所以：

- 归并的对在 `applied_rules` 里标 `level: 'near'`，人审队列看得见是哪条规则合的。
- 加了**数字守卫**：文本重合度再高，只要两边的数字不同就不合并。
  这条由标定用例 C-N6 逼出来，也是这条规则最危险的假阳：
  `CASP14 benchmark` 与 `CASP15 benchmark` 只差一个字符、Jaccard 接近 1，
  却是两个不同的评测集。**数字恰恰是本项目全部判定的核心对象**——
  把两个数不同的句子并成一簇，等于亲手制造一次合成共识，
  正是这道门要防的东西。

仍然抓不到：**重写型转引**（二手来源用自己的话复述同一个数，
文本重合度低于阈值）。那需要语义比对，不是 GC-0 的量级。
`knownLimitation` 已改写成正面列举「能抓什么 / 抓不到什么」，
而不是只说一句「未闭合」。

### 三 · 这一节我又栽了一次：同名遮蔽

近似归并里我写了个 `const jaccard = (a, b) => ... a.size ...`，
而文件里**已经有一个同名函数**，按数组算（`.length`），
被 ⑥ 低置信候选调用。我的定义在 `cluster()` 作用域内遮蔽了它，
于是 `undefined` 被当成 falsy —— **低置信身份候选一条都生不出来，
人审队列静默清空**。

标定用例 C-9（「标题高度相似 → 不合并，进人审队列」）抓住了它，
而且是靠**候选数那一列**，不是靠簇数——簇数完全正常。
一条只检查主结果的断言会漏掉它。

**同名不同签名的遮蔽，是最不响的一类破坏。**

### 四 · §S5 八的剩余项，逐条给现状

| 未闭合项 | 现状 |
|---|---|
| X-2 自然语言 query | **已改**（§S6）。通过率没变，原因分两半说清了 |
| G-FRAME 只看分号 | **已扩**到同句内一切并列（本节一） |
| G-CLUSTER 只按自陈 | **已加**近似归并（本节二）；重写型仍抓不到 |
| L1-c 词表有限枚举 | **已闭合一半**（§S8）。外部语料有了，标签仍是我打的 |
| 跨句极性只认转折句首 | **未闭合**，且这是刻意的：非转折句首的否定算进来是假阳 |
| 13 条 / 6 篇 / 3 话题 | **已扩**到 19 条 / 9 篇 / 4 话题，含真实中文（§S9）。覆盖面仍不是代表性样本 |

〔本段自纠 · S9 之后〕这段结论句原写「后三条不是还没来得及修」，
而 §S8/§S9 之后表里的后三行已经全都动了——**表改了，结论句没跟上**。
一段描述表格的散文与表格不一致，正是本项目一直在抓的那类腐。改写如下。

**当前手段够不着的，是这几条**（与「还没来得及修」要分开说）：

| 真正够不着的 | 缺什么 |
|---|---|
| G-CLUSTER 换了数字表述的转引 | 「约 26 亿」对 `$2558 million`，需要语义比对（§S11 收窄了范围） |
| L1-c 标定集的**标签**仍由作者打 | 需要另一个人来标，不是另一份语料 |
| 覆盖面不是代表性样本 | 需要的是更多真实文献，且要有抽样口径 |

**加门解决不了这三条**：第一条不是 GC-0 的量级，后两条是取材与抽样问题。
写在这里，是为了下一轮不要再去给它们加门。

〔§S10 之后删掉了一行〕原表第一行是「枚举逗号截断作用域」，
理由写「区分两种逗号需要句法分析」。**那个理由错了**，见 §S10。

刻意不闭合的一条：跨句极性只认转折句首。
非转折句首的否定算进来是假阳（`…is $2.7 billion. No funding was received.`），
它每次运行都被打印出来，但不参与判定。


## S8 · L1-c 的外部标定集（2026-08-19）—— SA-3 循环性闭合一半

SA-3 认账过一条：L1-c 的算子表是有限枚举，而它的 18 条标定集**全部由作者出题**。
我按自己想得到的否定形态写词表，再按同一批想法写标定集，
于是标定集永远测不出「我没想到的那些形态」——与变异测试当初的循环性同构。

`tests/external/l1c-external.mjs` 的 17 句**逐字取自真实开放获取论文摘要**
（Europe PMC，`OPEN_ACCESS:Y`，带 PMID 可核）。句子里的否定形态是真实作者写的。

### 一 · 它第一时间纠正的是**我的判据**，不是门

初稿把「句中含否定」直接等同于「L1-c 应判 fail」，13 句里报出 6 条假阴。
逐条读完，6 条里只有 1 条是真的。差别在一个此前从没写下来过的判据：

> **载荷是落在被否定的谓词里，还是被独立地肯定断言？**
>
> `The 30-day mortality was 16.1% and did not differ according to the delay`
> —— `16.1%` 是被**肯定**断言的，否定作用在「随延迟而变」上。
> 一条 claim 写「30 天死亡率为 16.1%」是完全合法的转录，**放行是对的**。

若不写下这条判据，我会拿着一个虚高的「6/13 假阴」去改门，
把合法转录一起拦掉——**外部语料在这里救的不是覆盖率，是方向。**

判据被迫写死成可机械套用的一句话（否则「改标签迎合实现」就是循环性本身）：

```
载荷落在空结果算子所辖的那个子句内（主语 + 谓语 + 附着的括号）→ 应 fail
载荷落在另一个子句里                                        → 应 pass
子句边界 = and / ; / 分隔从句的逗号 / 句末
```

统一套用后它改了**我最初打错的两条标签**（42223567、42340512），
两条都是被「同结构却给了相反标签」这件事自己抓出来的。

### 二 · 真正的门缺口：一整类不含否定词的空结果表述

`Follow-up BCVA and ΔBCVA were comparable.`

整句结论就是「无差异」，而 `comparable` 不在任何算子表里。
这条暴露的不是「否定词漏了几个」，而是**有一整类表述根本不含否定词**——
学术写作报告 null result 最常见的措辞之一是
`were comparable / similar / equivalent`，而不是 `did not differ`。

新增 **NEG-N（空结果表述）**，并且它的作用域是**整个子句（含算子之前的主语）**：

> `Follow-up BCVA and ΔBCVA were comparable.`
> 空结果的语法位置在谓语，被否定的主语在它**之前**。
> 只往后延的作用域够不着 BCVA —— 这是词表之外的第二个缺口：**方向**。

但作用域也不能扩到全句，那会打中并列的肯定断言
（`mortality was 16.1% and did not differ …`）。
两条真实句子把边界卡在了同一个位置：**子句**。新增 `clauseStart()`。

红样本验过：删掉 NEG-N，外部集立刻红 2 条。

### 三 · 仍然未闭合的一条，有确切名字

**枚举逗号截断作用域。**

`no ... difference in A, B, or C (p = 0.343, …)` ——
A/B/C 是并列的检测项，中间的逗号是**枚举分隔**而非子句边界；
`clauseEnd` 把它当成边界，作用域止于第一个逗号，够不着句末括号里的 p 值。

修它要能区分「枚举逗号」与「分句逗号」，那需要句法分析，不是 GC-0 的量级。
**不修、不改标签、每次运行都打印出来。** 文件里用 `KNOWN_OPEN` 白名单显式豁免，
并且——这一条同样重要——**若它哪天不再出现，本文件判红**：
要么它被修好了（该更新白名单），要么本文件失效了。

### 四 · 诚实的边界

- 17 句来自单一检索式，偏医学与心理学，**不是有代表性的样本**。
  它给出的是「漏掉的形态叫什么名字」，不是覆盖率。
- **句子是外部的，标签是我打的。** 外部语料解决了句子的来源问题，
  没解决标签的来源问题——那需要另一个人来标。**SA-3 只闭合了一半。**


## S9 · 真实中文文献（2026-08-19）

§S5 八的最后一条：`13 条 / 6 篇 / 3 话题`。这条只能靠加真实文献补——
**加门补不了取材问题**，§S7 四已经把这句写在那里了。

T4 是第一步：三份 Europe PMC 上的中文摘要（`LANG:"chi"`，PMID 可核）。
选它们是因为系统里有一整套 CJK 处理——归一化、中文否定表、中文数字——
**此前只在作者自己造的句子上验过**，那正是 SA-3 那条循环性的另一面。

六条用例全部判对，代价是四条缺陷：

### 一 · `numericForm` 没做 NFKC —— 同一个坑挖了两次

`73．55％` 用的是全角句点 U+FF0E 与全角百分号 U+FF05；作者转录成半角
`73.55%` 是中文期刊语境下的标准做法。不归一化则两侧永远对不上，
一条完全合法的转录被判成假。

**组稿器那边早在 E-3 就修过同一个问题**（扫描前 NFKC），
锚点包含这边漏了。同一类缺陷在两个模块各犯一次，中间隔了整整一轮——
这说明「全角」这件事没有被记成一条跨模块的约束，只被记成了一次修复。

### 二 · 中文空结果同样把主语放在算子之前

`但对SPH、平均草茎长度（MSL）、LSP、SSP生物学产量和经济没有显著影响。`

「没有」是 NEG-S，作用域只往后延，够不着它前面的 SPH。
与 §S8 的英文那一批同构——**这是同一个方向问题在另一种语言上的复现**，
说明 §S8 找到的不是英文的特例，是空结果表述的普遍语法位置。
中文空结果短语归入 NEG-N，取子句作用域。

### 三 · G-FRAME 挑错了子句 —— 一道理由错了的红

原实现取「第一个是锚句子串的子句」。在
`…分别为100％，75％，和50％，结果发现，…杀虫率分别为73．55％和78．45％`
这种句子上，它选中了 `分别为100％` —— 一个与载荷无关的片段，
于是报「discriminator 不是锚句子句的逐字片段」。

判红了，但**理由完全是错的**。这比放行更值得记：
放行至少是沉默的，而一个错误的理由会把人引去改一个没坏的地方。
现改为取**含本 claim 载荷**的那个子句。

### 四 · 夹具的错，以及守卫写在错的位置

`sentenceWith` 不认中文句号 `。`，把两句当成一句，前一句的否定打中了
后一句的载荷。**那是夹具造出来的假阳，不是门的**——若不查清就去改门，
就会为了迁就一个坏夹具而放松真实的判定。

修它的时候我又加错了一个小数点守卫：查的是句号后**第二个**字符，
于是 `results; 47%` 里的分号不算句末，锚句一口气吞掉三个并列子句，
T2-2 与 T3-6 双双回归。**守卫写在错的位置，比没有守卫更难查**——
它让一段看起来更严谨的代码做出更宽的行为。

### 五 · 门在一条我没为它设计的用例上触发

Wouters 那句同时给了中位数 $985.3M 与均值 $1335.9M。
我写 T3-6 时只看见中位数，**是 G-FRAME 告诉我这句需要 discriminator 的**。

这是本项目第一次出现「门发现了作者没注意到的东西」——
此前每一条发现都来自攻击者或外部语料，门自己只是在守已知的东西。

顺着它又查出 ` and ` 的切分问题：`research and development` 是名词短语
内部的 and，切开会让含载荷的半句丢掉 `median`。判据改成
**两侧各有一个数才切**——本门要找的就是并列的读数，这个判据与它的目的同构。

### 六 · 现状

外部标定测试 **19 条 claim / 9 份文献 / 4 个话题**，全部判对。
`0 条达到 verified` 这条没变，理由也没变：真实文献给不出 G5 需要的结构化锚。

**覆盖面仍然不是代表性样本。** 4 个话题、9 份文献，偏医学/心理学/AI，
中文那三份全部来自农学。它证明的仍然是「能找到东西」，不是「覆盖了什么」。


## S10 · 「够不着」这个结论本身也要被攻击（2026-08-19）

§S8 三把一条缺口记成「当前手段够不着」：

> **枚举逗号截断作用域。**
> `no ... difference in A, B, or C (p = 0.343, …)` —— 逗号是枚举分隔而非子句
> 边界，`clauseEnd` 当成了边界，作用域够不着句末括号里的 p 值。
> 修它要能区分「枚举逗号」与「分句逗号」，**那需要句法分析，不是 GC-0 的量级**。

这个结论我写了两次（§S7 四的表里也有一行），两次都没有回头问一句：
**够不着的到底是哪一步？**

真正需要的不是区分逗号，是**让作用域够到句末括号**。绕开逗号即可：

> `(p = …)` / `(95% CI, …)` / `(HR 0.79, …)` 是同一个断言的**统计附注**，
> 它属于这句报告的那个发现，不属于它前面碰巧最近的那个子句。

于是空结果算子的作用域延伸到**句末括号**，不管中间隔了几个逗号。
它不误伤并列的肯定断言——`mortality was 16.1% and did not differ … (p = 0.816)`
里的 16.1% 不在句末括号内，仍在作用域之外。

外部标定集 17 条现在**零偏离**。红样本验过：撤掉这段延伸，立刻红 1 条。

### 这一节真正的收获不是那三行代码

**「当前手段够不着」是一个结论，而结论要被攻击。**

它在这个仓库里有特殊的危险性：本项目的全部纪律都建立在「把边界写清楚」上，
而「写清楚的边界」一旦落纸，就获得了一种不该有的稳定性——
它看起来像是被审视过的，于是下一轮没有人再去审视它。
我在两个不同的小节里重复了同一句错误的判断，中间隔了几个小时，
两次都是**照抄自己上一次的结论**。

诚实记账的反面不只是隐瞒，还有**把一次判断固化成事实**。

### 顺带两处自述文案的腐

- 白名单 `KNOWN_OPEN` 清空后，判定逻辑走进了「不再出现 ⇒ 判红」分支——
  那个分支本意是「修好了要回来更新白名单」，但空集是合法状态。
  改成：非空时要求其中每条真的仍然出现（防假豁免），空集直接放行。
- PASS 行原写「除已知的枚举逗号一条外全部符合」，闭合后就不准了。
  **文案里写死具体豁免项，等于把一条会腐的自述数字塞进 PASS 行。**


## S11 · 抓不到 ≠ 装作没看见（2026-08-19）

§S7 二给 G-CLUSTER 记了一条：**重写型转引**——二手来源用自己的话复述同一个数，
文本重合度低于任何合理阈值，shingle 归并抓不到，需要语义比对。

这条判断本身是对的（与 §S10 那条不同，这次「够不着」确实成立）。
但它漏了一步——**§S10 的教训在这里换了个形式**：

> 「不能自动归并」不等于「只能装作没看见」。

有一个信号是确定性的、离线可算的：**两条证据带着同一个罕见数字**。
`$2558 million` 与 `at $2558 million` 出现在两篇文本完全不同的文献里，
要么是重写型转引（同源），要么是真正的独立复现——
**本门不替人做这个判断，但它可以把这一对推到人面前。**

新增 ⑥-a：命中即进 `identity_merge_candidates`，标 `rule: 'same-rare-number'`，
**只提议不执行**——与标题相似那一档同理，理由也相同：
自动执行低置信合并，会把假合并率变成没人看得见的旋钮（§5A.3.5 的原话）。

罕见的判据保守到近乎刻薄：年份、小整数、`100/50/95/99` 一律不算，
有效数字少于三位不算。理由是**队列被噪声淹没等于队列不存在**——
一个每次运行冒出几十条候选的人审队列，没有人会看第二次。
标定用例 C-R2（`100 patients in 2019` 撞车）守的正是这一条。

### 边界现在写成了三档，而不是一句「未闭合」

| 档 | 内容 |
|---|---|
| 自动归并 | 同身份键 · 逐字节同文 · 改写过的转载（shingle ≥0.82，带数字守卫） |
| 只提议不执行 | 标题高度相似 · **同一罕见数字** |
| 仍然抓不到 | 换了数字表述的转引（「约 26 亿」对 `$2558 million`）· 不带数字的同源叙述 |

第三档才是真正需要语义比对的部分。**原来那句「重写型转引抓不到」把三档
压成了一句**，于是中间那一档——本来做得到的那一档——被一起放弃了。

这是同一个错误的第二种形态：§S10 是把「够不着」当成了结论不再审视，
这里是把「够不着」的**范围**说得比实际更大。两次都是同一个动作：
**一句写得太顺的边界，比一个没写的边界更难被推翻。**


## S12 · 把「同一个坑挖了两次」变成一条门（2026-08-19）

同一个坑，本项目挖了两次：

| | 症状 | 修法 |
|---|---|---|
| **E-3**（§S5 四） | 组稿器漏掉全角数字，`９２％` 绕过整条证据链 | 扫描前 NFKC |
| **T4-1**（§S9 一） | 锚点包含把全角原文 `73．55％` 与半角载荷 `73.55%` 判成不匹配 | **同一个** NFKC |

两次相隔一整轮。原因不是谁粗心——是**「文本比对前要归一化」这件事
只被记成了一次修复，没有被记成一条跨模块的约束**。
一条只活在某个模块里的约束等于没有约束：下一个模块会重新踩。

### 新门：`gates/check_normalization.mjs`

它是**行为层**的，不看实现：把同一段文本的全角形态与半角形态分别喂给
每一个做文本比对的入口，要求它们给出**同一个判定**。

覆盖 5 个入口 × 4 组样本：G-L1-b 锚点包含 · G-L1-c 极性 · G-FRAME ·
X-2 反证 query · W-10 组稿器裸数字。

不看实现这一点是刻意的：换实现、换模块、加新模块都不影响它，
只要新模块进了那张表。**而「进不进那张表」是一次人的判断**——
本门守不住「有人加了新模块却没登记」，这条写在这里。

红样本验过两条：分别撤掉 `numericForm` 与组稿器的 NFKC，各红 4 处。

### 这一节的类型不同于前面几节

§S5–§S11 修的都是**具体缺陷**。这一节修的是**缺陷的复发机制**：
E-3 与 T4-1 不是两个 bug，是一个 bug 被修了一半。

判断标准也不同。前面几节问「这条修好了吗」，这一节问的是
**「这一类下次还会不会再来」**——而那个问题只有门能回答，注释回答不了。
E-3 的修复当时写了详尽的注释说明为什么要 NFKC，
那段注释在 `composer.mjs` 里，`g-containment.mjs` 的作者（也是我）没看见。


## S13 · 自述数字门第三次被自己的边界打中（2026-08-19）

`gates/check_doc_metrics.mjs` 的头注从 R4 起就写着一句：

> **自述数字门只覆盖它列举过的那几种数字**，这句话本身就是这道门的已知边界。

写下这句之后，它又被同一件事打中了第三次：

| 轮 | 漏掉的数字 | 怎么发现的 |
|---|---|---|
| R3 | 「456 个指针、占 33.8%」 | 独立攻击者 |
| R4 | 穷举 oracle 向量数（550 万 vs 实测 2778 万，5 倍偏差） | 独立攻击者 |
| **本轮** | **门数本身**（README 写 26/26，实际已到 30/30） | 我自己在写 §S12 时顺手核对 |

门数是 README 的**头条数字**——一个读者最先看到、也最会拿去引用的数——
而它一直不在这道门的视野里。26 → 30 的过程跨了五次提交，无人察觉。

### 修法与那两次相同，但这次把「谁来数」也定了

工件由 `run_all.sh` 自己落（`gates/.gate-stats.json`），门读工件。

**为什么不让 doc_metrics 自己去数**：数门的方式有很多种——数 `run` 行、
数文件、按 scope 过滤——每一种都会和实际跑的那一套漂移，
而漂移的方向恰好是「看起来对、实际不对」。
唯一不会漂的是**这次真的跑了几道**，所以由跑的人来记。
与 oracle 向量数同一条纪律：工件缺失即判红，本门不替它宣称任何数字。

红样本两条：改坏 README 的门数 → 红；删掉工件 → 红。

### 这道门的边界仍然存在，而且不会消失

三次都是「某个新数字进了 README，而门不认识它」。
补第四种句式挡不住第五种——**这条边界是这类门的结构性质**，
不是可以修完的缺陷。真正在守的是另一样东西：
每次有人在 README 写下一个新的自述数字，**同时给它一道门**。
那是纪律，不是机制。写在这里，是承认这一点。


## S14 · 一份跑得起来但没人跑的测试（2026-08-19）

`tests/external/cases.mjs` —— 19 条真实 claim、9 份真实文献、4 个话题 ——
此前只**打印**结果。19 条判定哪天翻了，没有任何东西会红。

**它是一份报告，不是回归。** 而这一整轮里，我用它做过五次「改完看看有没有
退化」的判断，每次都是**肉眼比对上一次的输出**。
肉眼比对上一次的输出，正是这个项目声称要取代的那种工作方式。

补上逐条期望后接进 `run_all.sh`（现 31 道门）。

### 期望不是「跑出来是什么就写什么」

这一点必须写清楚，否则这份回归就是循环的：

- **T2-4 / T3-3 / T3-4** 的期望是在修复**之前**定下的——
  当时实测与期望相反（那三条正是首轮的假阳）。
- **T1-1 / T2-3 / T3-1** 的期望来自对真实文献的核对
  （`92.4` 在 Nature 全文 0 次命中；36% 与 47% 分属不同判据；
  DiMasi 原文写的是 `$2558 million`）。
- **T4-1 / T4-6** 的期望来自「合法转录不得被拦」这条方向性判据。

只有 T3-6 是门先发现、我后补期望的（§S9 五），已在那一节写明。

### 顺带又一个会腐的自述数字

PASS 行原写「9 份真实文献」——**长在一份专门讲「数字要有出处」的测试里**。
已改成由 `SRC` 表算出来。

这是本轮第四个硬编码自述数字（前三个：L1-c 标定集的「假阴侧 6/假阳侧 5」、
X-2 的「假阳侧 4/假阴侧 6」、G-CLUSTER 的「压塌侧 5/独立侧 3/低置信 1」）。
四个都长在门或测试的 PASS 行里。**PASS 行是自述数字最容易腐的地方**——
它只在绿的时候被打印，而绿的时候没有人读它。


## S15 · 自攻：我对自己的新门做了这个项目明令不做的事（2026-08-19）

修复轮之后必须有 fix-audit——本仓库的规矩。这一节是对 §S5–§S14 全部改动的自攻。

### 一 · 头号发现：三道新门没有一个进负例套件

开发 `check_normalization` / `check_frame` / `check_containment` 时，
我确实验过红样本，方式是：

```
T=$(mktemp -d); cp -R src gates "$T/"; perl -0pi -e '...' ...; node ...
```

**跑完就没了。** 而 S0 阶段这个仓库自己得出的结论原话是：

> 承重证据应当是**入库的自包含脚本**，而不是一条一次性命令加一个哈希。

我对自己的新门做了这个项目明令不做的事，而且做了三次。
更难堪的是：`test_check_supply_contract.sh` 就摆在旁边，
它的文件头写着「本仓库的头号教训：门必须先证明会红才算数（已三次栽在这上面）」。

新建 `gates/test_new_gates.sh`：8 条红样本 + 3 条绿控，接进 `run_all.sh`（现 32 道门）。

### 二 · 负例套件立刻抓出一个空心样本

`check_frame` 的 F-14 用一句合成文本测「名词短语内部的 `and` 不该切」：

```
The median capitalized research and development investment was estimated at $985.3 million.
```

看起来正好覆盖那条判据。**实则不能鉴别它**——把「两侧各有一个数才切」
改成「一律切」之后，F-14 仍然通过：切开后前半句
`The median capitalized research` 根本没有数字，本来就不算兄弟读数。

有鉴别力的是**真实的那一句**（Wouters 2020 原文），它的后半句带着均值 $1335.9M，
切错之后含载荷的半句会丢掉 `median`。已补为 F-15，F-14 留在集子里当对照，
注释写明它是空心的。

**这是本轮第二次「合成样本看起来覆盖了、实际没有」**（第一次是 §S5 七的空心门）。
两次的共同点：合成样本是我按自己对缺陷的理解造的，
而**我对缺陷的理解正是那个缺陷的来源**。真实语料没有这个问题——
它不是按我的理解生成的。

### 三 · 另外两条自攻，结论是「没问题」

- **删掉任一新门，`run_all.sh` 会不会静默变绿？** 不会：删掉
  `check_normalization.mjs` 后 32 道里红 6 道（缺文件直接报错），不是静默。
- **新门的红样本会不会因为无关原因判红？** 三条绿控守着：
  未改动的树上三道门必须全部 exit 0。若某条红样本其实是被崩溃触发的，
  绿控不会动，但 `red_case` 的**理由匹配**会拦住它
  （只看退出码把崩溃当拦截，R3 栽过一次）。
